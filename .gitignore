# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local environments and secrets
.env
.env.*
!.env.example

# Local build/output scratch
node_modules/
.next/
.next-build/
*.tsbuildinfo
tmp/
output/
_ir_review_tmp/
dist/
build/
coverage/

# Local DB backups/dumps (실데이터·PII 포함 — 절대 커밋 금지)
backups/
*.dump
*.sql.gz

# Editor/workspace noise
.idea/
*.swp
*.swo
.vercel

# Gate 0 스파이크 로컬 산출물
spike-samples*/
spike-out*/
spike-labels/pages/

# Gate 2 layout-eval 원시 응답 캐시 (API 재과금 방지, 로컬 산출물)
apps/web/src/lib/server/layout-eval/eval-cache/

# 픽셀포트 보관소 — git·검색(rg) 제외. 참조 금지(신 디자인 재구축이 정본)
temp/
