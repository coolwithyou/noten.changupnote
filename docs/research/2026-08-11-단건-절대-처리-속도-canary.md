# 단건 딥분석 절대 처리 속도 canary

- 조사일: 2026-08-11
- 대상 공고: `7224ea46-f047-46e3-b383-6df98e8cc36c` (`bizinfo/PBLN_000000000124928`)
- 모델/전송: `claude-opus-5` / `claude-cli`
- 판정: **단건 절대 처리 속도 개선 확인. 품질 guard를 포함한 동일 held-review repair-loop가 33분 03초에서 10분 21초로 68.7% 단축됐다.**

## 비교 원칙

단순 신규 분석과 독립 검수 blocker를 반영하는 재분석은 모델 지시와 repair 횟수가 다르다. 최종 속도 판정은 같은 Fable/Sonnet 검수 출처, 같은 누적 blocker 3건, 같은 Kordoc 입력을 사용한 held-review repair-loop끼리 비교했다.

기존 기준 run은 `run-2026-08-10T195446.044Z-2e4ee2`다.

- 총 `1,983,295ms` (`33분 03초`)
- primary repair 1회
- criteria `19/19` 원문 근거 확인, 22축 완전
- Kordoc `1,982,553ms`, 후보 425개, 모델 요청 25회
- Kordoc `partial`, 신청서 관련 비필수 미해결 1건
- 검수 출처 `run-2026-08-10T151614.936Z-7b7ce0`, 누적 blocker 3건

총시간의 99.96%가 Kordoc에 걸렸으므로 딥분석 모델 자체보다 작은 후보 묶음의 반복 호출과 repair 때의 중복 Kordoc 실행이 단건 병목이었다.

## canary 결과

| 경로 | run | 총시간 | 기준 대비 | primary repair | Kordoc |
|---|---|---:|---:|---:|---|
| 기존 동일 repair-loop | `run-2026-08-10T195446.044Z-2e4ee2` | 33분 03초 | 기준 | 1 | 신규 25요청, partial |
| 신규 전체 분석, 40개 묶음 | `run-2026-08-11T000251.156Z-491b7a` | 17분 06초 | `-48.3%` | 0 | 신규 15요청, complete |
| 신규 전체 분석, 신뢰도 0.75 guard | `run-2026-08-11T002320.535Z-5366c4` | 21분 06초 | `-36.2%` | 0 | 신규 16요청, 안전한 partial |
| 동일 repair-loop, Kordoc 재사용 | `run-2026-08-11T004746.688Z-6466ad` | 7분 45초 | `-76.6%` | 1 | 새 모델 호출 0, complete |
| 동일 repair-loop, 의미 guard 포함 | `run-2026-08-11T010029.805Z-3bacd4` | 10분 21초 | `-68.7%` | 2 | 새 모델 호출 0, complete |

신규 전체 분석 canary는 후보 425개 가운데 실제 판정 대상 404개를 모두 처리했다. 기존 25회 요청이 15~16회로 줄었고, 공고 내부 후보 묶음 2개를 동시에 처리했다. 두 번째 canary는 저신뢰 긍정 판정을 바로 수락하지 않고 재판정으로 보내 accepted field가 223개에서 213개로 줄었다. 신청서 관련 남은 1건은 비필수 미해결이라 자동 성공으로 숨기지 않고 `partial`로 안전 종결했다.

held-review repair-loop는 원본 SHA, 엔진/분석 버전, 모델, transport, 미처리 여부가 같은 Kordoc 불변 산출물만 재결속했다. 최종 run의 재결속은 `1ms`, run 요약 비용은 `$0`, `reusedFromRunId=roundtrip-2026-08-10T151616.052Z-dc416f`다. 재사용 산출물 안의 요청 수와 명목 비용은 원본 실행 provenance이며 이번 run에서 발생한 호출이 아니다.

## 품질 회귀와 구조적 차단

첫 동일 repair-loop canary는 7분 45초로 가장 빨랐지만, 원문의 `영천시 소재 1년 이상`을 `biz_age >= 12개월`로도 발행했다. 이는 소재 기간에서 업력을 추론한 의미 회귀다.

프롬프트 문구에만 의존하지 않고 중앙 validator를 `deep-analysis-validator-v9`로 올렸다.

- 소재·입주·사업장 문맥과 기간이 함께 있는 근거는 별도 설립·업력 문구가 없는 한 `biz_age`로 발행할 수 없다.
- 위반 시 `semantic_misattribution`으로 실패하고 `biz_age` 제거, 축을 `inspected_no_condition`으로 변경, `premises/text_only` 보존을 자동 repair에 지시한다.
- 실제 최종 canary에서 첫 결과가 거절돼 repair가 1회 추가됐고, 최종 결과는 `biz_age=inspected_no_condition`, 소재 1년은 `premises/text_only`로만 남았다.
- criteria `19/19` 원문 근거 확인, 22축 완전, ambiguous/input_missing 0건이다.

Kordoc에는 프롬프트의 불확실 기준과 코드의 수락 기준이 달랐던 문제도 있었다. 긍정 판정 즉시 수락 기준을 `0.55`에서 `0.75`로 맞추고, 0.75 미만은 재판정 또는 미해결로 남긴다. 이 변경은 신규 전체 Kordoc canary에서 검증했으며, 최종 held-review run은 공정한 기존 대비를 위해 검증된 과거 불변 Kordoc을 재사용했다.

## 최종 품질 그래프와 운용 상태

최종 run `run-2026-08-11T010029.805Z-3bacd4`의 품질 그래프는 다음과 같다.

- input seal: passed
- deep contract: passed
- application source: passed
- field adjudication: passed (`404` candidates, `225` accepted, unresolved `0`)
- independent review: passed
- analysis readiness: passed

속도 측정과 분리해 새 run에 Fable 5 검수와 Sonnet 5 블라인드 감사를 다시 붙였다. Fable은 criterion 19건을 모두 `correct`, 빈 축 11개를 모두 `confirmed_absent`로 판정했다(`87.4s`). Sonnet은 동결된 감사 4항목을 모두 `concur`로 판정했고 불일치·unsure는 0건이었다(`114.8s`). 따라서 기존 검수 blocker를 반영한 재분석을 스스로 통과로 간주하지 않고도 독립 검수 노드가 실제 `passed`로 닫혔다.

실행 종료 뒤 runtime은 `paused`, generation `41`, local owner 없음, deep/application active lease 각각 0건이며 남은 `claude -p` 프로세스도 없다.

## 결론

단건 절대 처리 속도는 개선됐다.

1. 신규 전체 분석도 33분 03초 대비 17~21분으로 줄었다.
2. 실제 반복 루프의 동일 held-review repair는 10분 21초로 68.7% 줄었다.
3. 속도만 빠른 7분 45초 결과에서 발견한 의미 회귀를 중앙 validator가 자동 교정했으며, 그 추가 repair 비용을 포함해도 기존보다 22분 42초 짧다.
4. 10건 동시 처리에서는 공고 10개를 인플라이트로 두되 실제 `claude -p`는 프로세스 전역 기본 4개로 제한한다. 따라서 단건 최적화와 대량 안정성을 같은 자원 상한 아래 유지한다.

제품 단계의 deep promotion, matching canary, field materialization, workspace canary는 이번 단건 속도 범위에서 실행하지 않아 `not_evaluated`다. 분석·Kordoc 품질 통과가 프로덕션 승격을 자동 실행하지는 않는다.
