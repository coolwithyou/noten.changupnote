# 개선 배포 후 동일 3건 재측정

결론: **분석 시간은 18.8% 단축됐으나 품질 판정은 HOLD**다.
기존 TECHFEST 보류 3개와 질문 기준일 문제는 해소됐지만, RHWP 입력 연결·지원 범위 문제와
조건 추출 검수 지적 3개가 확인됐다. 안성에는 정보 한계 범위 분류 오류 1개가 남았다.

후속 정정: 처음에는 parsed 좌표만으로 RHWP 후보 2개를 ‘라벨 셀 오추천’으로 판단했다.
같은 원본 SHA의 실제 RHWP 쓰기 대상 확인 결과 이 해석은 부정확했다. 정상 법인등록번호
입력은 유지해야 하며, 별도 ‘해당 시’ 후보의 잘못된 연결과 이미지 입력 지원 범위가 문제다.
아래 TECHFEST 절에 정정 근거를 기록했다. 기존 실행·검수 receipt와 시간 측정은 변경하지 않았다.

## 측정 범위

사용자의 재측정 요청에 따라 [직전 실행](2026-09-14-동일3건-재분석-상세관측.md)과
같은 안성·베트남 TECHFEST·단양 3건을 실행했다. 준비 전후 input·attachment manifest SHA가
직전과 일치했다. 대상 대체나 입력 수정, 기존 분석 재사용 없이 전부 새 모델 응답을 생성했다.

- 실행 checkout: `61c6e8d1bc9375da56618e620dc12b33a9ebc9b7`.
  [배포한 개선 소스](2026-09-14-재분석-발견사항-코드수정-및-서비스반영.md) `0034d36` 이후 차이는 보고서뿐이다.
- 모델: 기존과 같은 `claude-opus-5`, 로컬 `claude-cli` Max 구독.
- prompt `lab-deep-v26`, validator `deep-analysis-validator-v19`, RHWP v14.
- primary 동시성 1, RHWP 후보 동시성 2, first-pass effort 미설정 유지.
- 딥분석과 RHWP 필드 분석을 함께 실행했다. 단양은 신청 양식이 없어 RHWP not_applicable이다.
- 기반 모델 교체 비교가 아니라 동일 모델에서 개선된 실행 계약의 전후 비교다.

manifest `697884dde4bc8bcbe77dcce811d48a4f609ead76e48c5d6a13cfc3e28675aec7`,
grant `61c66ea5f29a63f9fdd0e6560d9ab4e08dd6320bdd0528f937c6e512621d54da`,
receipt `a4317d92d3b89df9120d1921588d523c98c9ba646a8bb6cdcc3e5eceed2ed43c`.
실행은 16:53:39.024–16:57:56.682 KST, completed·systemic failure 없음이다.

## 실측 시간

| 공고 | 직전 전체 | 이번 전체 | 변화 | 직전 RHWP | 이번 RHWP | 이번 primary / repair / RHWP 요청 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 안성 수출물류SOS | 57.462초 | 61.574초 | +7.2% | 41.110초 | 49.362초 | 1 / 0 / 3 |
| 베트남 TECHFEST | 201.505초 | 124.106초 | -38.4% | 79.995초 | 117.373초 | 1 / 0 / 5 |
| 단양 클리닝 | 56.862초 | 66.970초 | +17.8% | 0.229초 | 0.334초 | 1 / 0 / 0 |
| receipt 전체 | **317.499초** | **257.658초** | **-18.8%** | — | — | **3 / 0 / 8** |

전체 59.841초 단축은 실제 receipt 시간 차이다. 이전 9/13 측정 262.366초와 비교하면
4.708초(-1.8%) 단축이다. 단일 시행이므로 코드 효과와 모델 응답 변동·캐시 효과를 분리한
인과적 성능 검증으로 확대하지 않는다.

TECHFEST는 primary 131.132초 + repair 69.597초에서 primary 123.359초·repair 0회로 바뀌었다.
이번 설명에는 직전과 동일한 직접 부정 문장이 생성되지 않았으므로, 이전 오탐 문장의 회귀
검증은 오프라인 fixture 증거이고 이번 live 증거는 불필요 보정 없는 새 결과의 종결이다.
‘코드 수정만으로 정확히 69.6초 절감’이라고 단정하지 않는다.

RHWP 시간 합은 121.334→167.069초로 오히려 37.7% 늘었다. TECHFEST 내장 문서의
한 batch 응답이 79.599초·출력 18,387토큰이었고 재판정 2회가 약 11.8초·12.1초였다.
필드 요청 수는 이전과 같은 8회이며 전체 요청만 repair 제거로 12→11회가 됐다.
큐 대기 합은 약 0.497ms로 병목이 아니다. 전체 출력 토큰은 99,611→94,698개다.
primary와 RHWP가 병렬이므로 둘의 시간을 합산해 전체 시간으로 표시하지 않는다.

## 개선 적용과 품질

### 안성

입력 추천 36개, field-ready 문서 2개, 미해결 0개, 기존 구조 경고 4개와 partial을 유지했다.
primary repair는 0회이며 자격 원문 한계로 matching conditional을 유지한다.

다만 `sourceLimitations[2]`가 평가 방식 자체의 미제공을 `evaluation_precision`으로 분류했다.
인용한 ‘운송비 60%·기업당 600만원’은 지원액 규칙이며 이미 제공된 평가 방향을 입증하지
않는다. 원문 동일 SHA 대조와 gpt-6-astra high 자문에서 의미 분류 오류로 확인했다.
다른 `eligibility_details` 한계가 남아 있어 이번 결과에서 잘못된 ready 승급은 없었다.

### TECHFEST

기존 보류 후보 `영어 발표평가`, `담당 업무`, `참 고` 3개는 첫 판정에서 위치에 결속된
근거로 not_input이 됐다. 공동대표 입력 복구도 유지됐다. 예전의 missing/non-contiguous
인용 실패가 이번에는 재현되지 않아, 실패 이력 전달·동일 실패 중단 기능의 live 경로까지
검증한 것은 아니다. 해당 경로의 증거는 이전 오프라인 회귀다.

첫 참가 이력 질문은 ‘2022년 1월 1일부터 이 모집공고의 공고일까지’와 `per_notice`를
보존했다. 평가 방향·동점 순서는 제공되고 수치 배점만 없는 한계는 `evaluation_precision`으로
분류돼 matching ready가 됐다. 기존 두 참가 이력 조건의 사업 범위를 합치지 않았다.

자동 RHWP 판정은 unresolved 0·coverage complete, 문서 2개·입력 추천 39개다.
다음 2개를 처음에는 원문 구조와 gpt-6-astra high 대조에서 라벨 오추천으로 판단했으나,
후속 native 검증에서 **라벨 anchor와 실제 쓰기 target을 구분해 정정**했다.

| 후보 | 정확한 위치 | 실제 쓰기 대상과 정정 |
| --- | --- | --- |
| 법인등록번호 `851ed55f5c86157703600e13` | block106,row5,col0 | 정상 라벨 anchor. 실제 target은 오른쪽 row5,col4 ‘해당 시’ 값 셀이며 유지해야 함 |
| 별도 ‘해당 시’ 후보 | block106,row5,col4 | 값 안내문이 별도 라벨로 해석됨. 실제 target은 row4,col7의 ‘베트남 법인 또는 지사 설립 여부’ 라벨로 잘못 연결됨 |
| 이미지 `6543ab5d9e851d91c7177ed8` | block108,row11,col0 | 실제 target은 오른쪽 row11,col2의 중첩 이미지 host. 라벨 덮기 문제는 아니며 일반 textarea 쓰기 계약의 지원 범위를 확인해야 함 |

이전·이번의 구조 보존 parsed 문서는 바이트 단위로 동일하다. 위치를 정확히 인용했다는
사실만으로 실제 쓰기 대상의 역할·지원 범위를 증명할 수 없다. 따라서 기존 보류 3개 해소와
입력 연결·지원 범위 문제를 함께 보고하며, 추천 수·자동 complete를 품질 통과로 해석하지 않는다.

후속 검증은 `@rhwp/core` WASM에 원본 HWPX SHA
`5d9ad6200091e341c945f1c746b1512ded6f2d85f0aa21bd6f0eac5b566fe22c`를 입력하고
제품의 필드 변환·anchor resolver를 호출했다. 정상 법인등록번호 target에 시험값을 쓰고
HWPX 저장·재열기 후 값 및 이웃 라벨 보존도 확인했다. 글리프 폭은 근사했으므로 구조·native
편집 증거이며 브라우저 시각 UAT는 아니다. 원본과 기존 분석 산출물은 수정하지 않았다.
증거: `spike-out/hold-fixes-20260914/native-targets-before.json`, `native-positive-control.json`.

### 단양

조건 7개, repair 0회, conditional 유지다. 제공된 요약·평가표의 범위 한계를 보존했고
독립 검수에서 조건 오류가 없었다. 신청 양식 없음 판정과 RHWP 모델 호출 0회도 유지했다.
이번 전체 시간은 10.108초 늘었으며 대다수는 primary 응답 대기였다.

## 독립 검수

같은 `gpt-5.6-sol`/codex-cli 구독, 동시성 2, hard timeout 300초·stall 180초·최대 2회로 실행했다.
검수 manifest `55352926f8f383b4486ba0dd6821bee5ca96596aa9d4aecad1bd9c1d90141c18`,
aggregate `c593b5bb314729cd590f49638e924e45ed65830e79b413d7b617ae53724eb9fc`.

| 공고 | 검수 시간 | 재시도 |
| --- | ---: | ---: |
| 안성 | 55.593초 | 0 |
| TECHFEST | 100.158초 | 0 |
| 단양 | 40.922초 | 0 |

동시 실행한 검수의 전체 시간은 **100.160초**다. 직전 정체 복구를 포함한 257.743초보다
짧지만 이번에는 정체가 발생하지 않아, 재시도 구현 자체의 속도 향상으로 해석하지 않는다.
종료 후 검수 lock은 0개다.

39개 조건 중 correct 36·needs_edit 3, 빈 축 42개 confirmed_absent로 aggregate는 HOLD다.
지적은 TECHFEST에 집중됐다.

1. 중복 지원이 없다는 참가서약과 사후 환수 문구를 현재 신청 exclusion으로 승격.
2. 지식재산권 침해·소송이 없다는 서약을 명시적 신청 제외 효과 없이 exclusion으로 승격.
3. 영어 발표라는 평가 진행 방식을 독립적인 preferred 역량 조건으로 추출하고 기존 평가
   항목과 중복시킴.

gpt-6-astra high의 추가 원문 대조에서도 세 지적이 타당했다. 다만 현재 추출 프롬프트에는
‘서약’과 ‘영어 발표 역량’을 넓게 조건 예외로 허용하는 문구가 남아 있다. 단순 모델 지시
위반보다는 추출·검수 기준에서 근거의 명시성과 자격·평가 효과의 경계가 충분히 일치하지
않은 오류다. 모든 서약을 일괄 제외하지 말고 명시된 효과에 따라 판단해야 한다.

이 검수는 primary 조건·빈 축 대상이다. RHWP 오추천이나 sourceLimitations 분류의 정확성을
대신 검증하지 않으므로 수동 구조 대조·독립 자문 결과를 별도로 반영했다. confirmed_absent도
제공 입력 내 검사 결과이며 미제공 원문에 조건이 없다는 보증은 아니다.

## 남은 개선과 검증 경계

다음 우선순위는 입력 추천의 라벨/값 셀 역할 검증, 서약·절차와 eligibility/preferred의
효과 구분, `evaluation_precision`의 제공된 평가 방향 근거 검증이다. 인용 존재·신뢰도만으로
통과시키지 않되, 모든 라벨 후보를 일괄 제외하는 방식도 피해야 한다.

시간 단축 측면에서는 큐 동시성보다 긴 batch 출력과 필요 이상인 재판정이 우선 관찰 대상이다.
TECHFEST 79.6초 batch의 출력 중복을 줄이는 별도 비교 실험은 후보이며 아직 실행하지 않았다.
이번 재측정 중 실행 코드는 수정하지 않았다.

모델 결과는 로컬 불변 artifact이며 서비스 승격·배포는 수행하지 않았다. 실제 RHWP 입력값
쓰기·저장·재열기 UAT도 수행하지 않았다. 종료 후 runtime은 paused, generation 405,
owner/expiry null, 활성 deep/application lease 0건이다.

증거 디렉터리: `spike-out/remeasure-20260914/`.
`formal-observation.json`은 봉인 receipt·LabRun과 비봉인 요청 sidecar의 경계를 구분한다.
`monitor.jsonl`은 10초 간격의 완료 요청 관측이며 모델의 내부 추론 실시간 관측이 아니다.
`target-performance.json`, `comparison.json`, `manual-quality-audit.json`, `review-timing.json`,
`review-aggregate.json`, `post-runtime.json`에 비교·검수 증거를 보존했다.
`preserved-artifacts.json`으로 이전 14개·이번 7개의 선택된 불변 산출물 SHA를 검증했다.
