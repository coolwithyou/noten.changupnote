# 구독 딥분석 반복 실패 구조 진단 및 개선 설계

- 작성일: 2026-08-14
- 진단 기준 checkout: `f5960d3`
- 구현 기준: `37b7afc` (P0~P4 + Gate R Adapter/issuer/recovery + 조건부 매칭 준비도 + Kordoc 반복 보류 수렴)
- 상태: **deep-v19 exact 10건과 Kordoc ready 5건의 exact canary 종결, 나머지 live 실행은 별도 승인 전 NO-GO**
- 적용 범위: 로컬 `analysis-lab`의 `claude-cli` 구독 실행
- 운영 경계: Cloud Run worker의 `observe_only`와 운영 API 경로는 변경하지 않는다.
- 선행 조사: `docs/research/2026-08-13-딥분석-처리속도-트랙-리뷰-정리.md`

## 0. 이 문서의 역할

이 문서는 조사 결과를 보존하는 보고서이면서 이후 구현의 계약이다. 구현은 이 문서가 정한
Interface, 불변식, 단계별 exit gate를 통과하는 범위에서만 진행한다. 새 기능이 유용해 보여도
현재 실패 원인을 직접 제거하지 않거나 exit gate에 필요하지 않으면 이번 트랙에 넣지 않는다.

특히 다음 세 문장을 정본으로 둔다.

1. 구독 실행에서 명목 USD는 관측값이지 실행 권한이나 중단 기준이 아니다.
2. 순수 unresolved가 일부 남아도 확인된 축이 충분하면 `conditional` 매칭 후보이며, `held`는 후보 발행까지 보류하는 `deferred`다.
3. 프로세스 종료, 잡 lifecycle, 공고 품질, 통계 게이트, 승격 자격을 하나의 성공/실패 축으로 접지 않는다.

## 1. 현재 판정

### 1-1. 실행 판정

legacy 10건/30건 batch, 자동 review repair, 대량 검수·감사, 승격·배포는 계속 중단한다.
사용자 승인 아래 `deep-v19` sequence 0~9를 변경된 매칭 준비도 provenance와 exact-next Adapter로
종결했다. 승인된 10건 범위 뒤에는 같은 plan을 자동 연장하지 않는다. 후속 모델 실행은 새
proposal/plan과 현행 worker 증거, exact 범위의 새 사용자 승인·authority를 모두 갖춘 뒤에만 실행한다.

### 1-2. 사용자 결정: 구독 USD 상한 폐기

`claude-cli` 구독 실행은 요청별 API 과금 경로가 아니므로 `--max-cost-usd`로 신규 작업을 중단하지
않는다. 기존 `costUsd`는 다음 용도로만 보존한다.

- API 환산 사용량 관측
- 모델·prompt·문서 복잡도 비교
- 비정상 fan-out 진단
- API 운영 경로의 실제 비용 guard 산정 자료

구독 실행 안전장치는 비용 대신 이미 존재하는 다음 자원을 기준으로 한다.

- 전역 Claude CLI 프로세스 4슬롯
- 요청별 timeout
- Max window exhaustion 감지
- 명시적 코호트 크기와 공고 ID
- 사용자 pause와 runtime lease

P1에서 confirmations도 Max window marker를 만나면 신규 착수를 중단하고 진행 중 작업을 회수한 뒤
별도 exit로 종결하도록 맞췄다. 이는 새 quota가 아니라 이미 정의된 구독 소진 신호의 의미 일치 수정이다.

이번 구현에서는 새 범용 wall-time scheduler, 호출량 quota, 별도 daemon을 추가하지 않는다. 먼저
잘못된 USD 중단을 제거하고 상태·실험 계약을 바로잡는다. 이미 확인한 905후보 fan-out은 P4의
도메인 candidate/request admission으로 제한하고, 모든 lane에 적용하는 generic quota는 별도 증거와
명확한 SLA가 생길 때만 설계한다.

### 1-3. 현재 실행 admission

Gate R이 아직 완료되지 않았으므로 legacy live entrypoint는 transport·runtime lease와 무관하게 정적
fail-closed 상태다. 공용 admission은 단건 분석, smoke, CLI/web batch, 자동 대상 선정, 구독 agent,
review/audit/adjudication/confirmations, 수동 repair의 최종 모델 binding을 막는다. promotion은 모델 호출과
별개이므로 approve와 실제 write 직전에 별도 mutation admission을 둔다. 관리자 ops도 child process를
spawn하기 전에 HTTP 423으로 거부하고 실행 버튼을 비활성화한다.
런타임 lease acquire/renew도 차단하되 기존 lease release는 허용한다. 신규/refresh/stale 대체
cohort와 불변 snapshot freeze는 cohort mutation admission으로 선정·저장 전에 중단하고,
정상 저장 cohort의 읽기 전용 조회는 유지한다.

현재 일반 허용 경로는 모델 호출 없는 `lab:agent` plan-only, batch dry-run, P3의 `createPlan/replay`,
exact 30을 불변 proposal로만 봉인하는 `lab:experiment:prepare -- --series=deep-v19`, publishable 후보의
실제 HWP/HWPX를 모델 없이 분류하는 `lab:roundtrip:preflight`다.
환경변수나 기존 lease로 이 차단을 열 수 없다. 구현된 `lab:experiment:issue`와 `lab:experiment`는 각각
사용자 approval SHA와 발급된 authority SHA가 있을 때만 열리는 별도 fail-closed 표면이며, 한 approval이
CAS winner authority 하나와 plan의 exact-next target 하나만 열도록 결속한다.
Kordoc도 legacy route가 아니라 사용자 승인에 명시된 proposal·sequence·source SHA 한 건만
`lab:roundtrip:canary`가 열며 API fallback과 다른 첨부 편입을 허용하지 않는다.

### 1-4. 실제 10건 확대와 매칭 후보 의미

2026-08-14 `deep-v18` sequence 2~11을 exact receipt chain으로 순차 실행했다. 10건 모두 terminal로
종결했고 provider/API fallback/window exhaustion/runtime leak은 0건이었다. 구 정책 결과는 publishable
6건, held 4건, model repair 2건이며 repair 후 신규 issue는 0건이었다. 마지막 receipt는
`227d1f8a...`, 전체 누적 12건의 formal verdict는 `CONTINUE`, runtime은 `paused` generation 91이었다.

held 4건을 축 단위로 재검토하면 3건은 확인된 축 17~21개와 순수 unresolved만 가진 조건부 후보이고,
1건은 확인된 축 1개·`input_missing` 21개인 자료 부족 공고였다. 이를 제품 규칙으로 다음처럼 고정했다.

- validator 오류와 unresolved를 섞지 않는다. 실제 계약·근거 오류는 기존 repair/failed 경계를 유지한다.
- 순수 `ambiguous`/`input_missing`만 남고 확인된 축이 2개 이상이면 `matchingReadiness=conditional`과
  `primaryValidationOutcome=publishable`을 기록한다. matcher는 확인된 criterion으로 후보를 만들고 남은
  축은 사업자 질문·추가 입력으로 해소한다.
- 확인된 축이 1개 이하이거나 unresolved 축에 criterion mismatch가 남으면 `held/deferred`로 보류한다.
- `publishable`은 deep primary를 매칭·검수에 사용할 수 있다는 뜻이다. Kordoc 완료는
  `withApplicationRoundtrip`을 명시해 별도 application artifact가 종결된 경우에만 주장한다.
- 기존 sequence 0~11 run artifact는 불변이므로 사후 덮어쓰거나 재분류하지 않는다. 이 정책은
  `daa7b51` 이후 새 분석부터 적용한다.

2026-08-15 새 정책을 봉인한 `deep-v19` proposal `6d8eb80a...`, plan `63ee3bb8...`의 sequence 0~9를
순차 실행했다. exact terminal 10건, provider/window/runtime 실패 0건이며 결과는 publishable 9건
(`ready` 4, `conditional` 5), held/deferred 1건이다. repair는 4건(model 3, deterministic 1), repair 후
신규 issue는 0건이고 formal verdict는 10건에서 `CONTINUE`다. 명목 비용 telemetry는 $9.1011785,
deep-primary 모델 실행시간 합계는 3,186,706ms였으며 transport는 전부 `claude-cli`, 해당 10건 실행의
API fallback과 Kordoc 결합 실행은 0건이다. 딥분석 마지막 receipt는 `fe988f55...`, 당시 runtime은
`paused` generation 111, active deep/application lease 0으로
복귀했다. publishable 9건의 Kordoc은 매칭 분석과 결합하지 않고 후보 확정 뒤 별도 bounded batch로
처리한다.

같은 날 모델 무호출 Kordoc preflight proposal `a604b9b8...`은 publishable 9건을 ready 5,
not_applicable 1, source_unavailable 3으로 분류했다. ready 5건은 구조 필드 1,235개 중 LLM 경계 후보
730개와 결정적 판정 505개로 축소됐다. 사용자 승인 아래 가장 작은 sequence 7/source `73b82ccf...`
한 건만 `claude-cli/claude-opus-5`로 실행했고 receipt `2503e071...`로 complete 종결했다. 실제 결과는
44개 구조 필드, 추천 입력 27개, 모델 요청 3회, 재판정 2라운드, 미해결 0, 구조 경고 0이며 API fallback은
없었다. 명목 `costUsd=$1.2393705`는 구독 telemetry이고 실행 비용 gate가 아니다. runtime은 `paused`
generation 113, active deep/application lease 0으로 복귀했다. 나머지 ready 4건은 자동 진행하지 않는다.

후속 승인 범위의 작은 단일 원문 sequence 3은 receipt `f1ddb830...`의 `partial`로 종결됐다. sequence 3은 107개 구조 필드 중 추천 입력 63개,
LLM 경계 후보 39개, 요청 2회, 재판정 1라운드였고 모델 판정 미해결은 0이다. partial 원인은 HWP의
넓은 셀 두 곳에서 같은 `선택 항목 4`의 정확한 쓰기 위치를 확정하지 못한 구조 경고 2건이다. 이는
매칭 판단 실패나 API/transport 실패가 아니며, 안전하게 확정된 63개 필드는 사용할 수 있고 두 위치는
사용자 확인이 필요하다. 명목 `costUsd=$0.3847755`는 구독 telemetry다. runtime은 `paused` generation
115, active deep/application lease 0으로 복귀했다.

별도 재개 승인 뒤 sequence 6/source `5886b64e...` 한 건을 같은 `claude-cli/claude-opus-5` 계약으로
실행했고 receipt `06ce82ce...`의 `partial`로 종결했다. 142개 구조 필드 중 추천 입력 79개, LLM 경계
후보 65개, 결정적 판정 77개, 모델 요청 3회, 재판정 1라운드였고 모델 판정 미해결은 0이다. partial
원인은 HWP의 넓은 셀 한 곳에서 `선택 항목 1`의 정확한 쓰기 위치를 확정하지 못한 구조 경고 1건이다.
확정된 79개 필드는 사용할 수 있고 한 위치만 사용자 확인이 필요하다. API fallback은 없었고 명목
`costUsd=$1.2819525`는 구독 telemetry다. runtime은 `paused` generation 117, active deep/application
lease 0으로 복귀했다.

남은 ready 대상을 진행하기 위해 canary Interface를 exact source 1개에서 proposal에 봉인된 exact source
집합으로 최소 확장했다(`2adf053`). 별도 batch나 실행 경로는 추가하지 않고 동일 runtime·transport·receipt
Module에서 다중 원문의 누락·추가를 모델 호출 전에 차단한다. sequence 0의 원문 7개를 실행한 receipt
`d47cb185...`는 `partial`로 종결됐다. 7개 원문·534개 구조 필드 중 추천 입력 67개를 확정했고 7개 모두
LLM 판정을 마쳤으며 skip·API fallback은 0이다. 다만 광주테크노파크 제출 양식의 `주관기관명`과
`자격요건 자율점검 - 해당 여부` 두 칸은 자동 재판정 2라운드 뒤에도 입력 대상 여부를 확정하지 못했다.
구조 경고는 0이고 이 두 후보만 사용자 확인이 필요하다. 명목 `costUsd=$3.375682`는 16회 구독 모델 요청의
telemetry다. runtime은 `paused` generation 119, active deep/application lease 0으로 복귀했고 승인 중단
조건에 따라 sequence 1은 착수하지 않았다.

sequence 0의 미해결 2건을 추적하면 transport나 HWP 파서 실패가 아니라 판정 수렴 규칙의 공백이었다.
모델은 두 후보를 최초·1차·2차 모두 사용자 입력으로 보면서 confidence를 각각 0.70·0.72에 고정했고,
시스템은 전역 확정 임계 0.75 때문에 같은 문맥을 세 번 호출한 뒤에도 `uncertain`으로 남겼다. 새 정보가
없는 동일 재질문이므로 모델 호출을 더 늘리는 보정은 채택하지 않았다. `37b7afc`부터 독립된 두 판정이
모두 `is_user_input=true`, confidence 0.65 이상이면 값을 자동 결정하지 않는 optional 사용자 확인 입력으로
보존하고 두 번째 판정에서 수렴한다. 전역 0.75 임계와 저신뢰 `is_user_input=false`의 review 계약은 유지해
false positive·false negative를 일괄 확대하지 않는다. 기존 receipt `d47cb185...`는 당시 실행의 불변 증거라
수정하지 않으며 새 규칙은 이후 분석에 적용한다.

별도 사용자 승인과 `cunote-codex-dev` 재인증 뒤 마지막 ready 대상 sequence 1을 exact HWPX 2개로
실행했다. receipt `dd5db646...`는 `complete`이며 기술기획 양식 204개 구조 필드에서 추천 입력 91개,
제품기획 양식 204개에서 추천 입력 89개를 확보했다. 두 문서 모두 미해결 후보 0, 구조 경고 0,
skip·오류·API fallback 0이다. 각 문서는 LLM 경계 후보 109개와 결정적 판정 95개로 나뉘었고,
재판정 2라운드 뒤 `resolved`로 종결됐다. 모델 요청은 문서별 6회, 합계 12회였으며 명목
`costUsd=$5.466815`는 구독 telemetry다. run artifact SHA `4aeb6ae4...`와 receipt 결속을 재검증했고
runtime은 `paused` generation 121, active deep/application lease 0으로 복귀했다. 따라서 ready 5건의
실제 결과는 complete 2(sequence 7·1), partial 3(sequence 3·6·0)이며 미착수 ready 대상은 없다.

## 2. 관측된 실패

이 절과 §3의 “실행 경로·Module”은 구조 개선 전 진단 기준 checkout `f5960d3`의 상태를
설명한다. 현재 구현 기준 `8856e4f`의 상태·admission 계약은 §1-3과 §8~§11을 정본으로 삼는다.

### 2-1. 확대 10건

고정 코호트 `cohort.deep-v17-cp2b-expand10-20260814.json`의 결과는 다음과 같다.

| 항목 | 결과 |
|---|---:|
| publishable / held | 7 / 3 |
| repair 사용 런 / 시도 | 6/10 / 7회 |
| pilot 포함 누적 repair | 6/15 |
| first-pass issue | 22건, 실제 교정 대상 10건 |
| 배치 wall | 64.54분 |
| deep / Kordoc 명목 USD | 14.4990 / 31.6714 |
| 총 명목 USD | 46.1704 |
| Kordoc 상태 | complete 4 / partial 4 / not_applicable 2 |

명목 USD는 더 이상 구독 실행의 실패 판정에 쓰지 않는다. 다만 Kordoc이 관측 부하의 68.6%를
차지했고 후보 수·요청 수가 꼬리 지연과 강하게 연결된 사실은 lane 분리와 preflight 필요성의
근거로 계속 사용한다.

### 2-2. 직접 반복을 만드는 상태 충돌

진단 기준 checkout의 실행 경로는 하나의 held 결과를 다음처럼 서로 다르게 해석했다.

```text
runValidatedLabPrimary
  -> outcome = held
runLabAnalysis
  -> primaryValidationOutcome = held
  -> error = primary_validation_held
runLabBatch
  -> target-error / errorRuns += 1
batch CLI
  -> target가 있으면 exit 0
subscription agent
  -> successful-only index에는 held 없음
  -> retryErrors=true partition에서는 다시 pending
```

따라서 같은 실행이 품질 계층에서는 안전 종결, 저장 계층에서는 error, 프로세스 계층에서는 성공,
에이전트 계층에서는 성공 런 누락과 재시도로 해석된다. 이 모순이 반복 실패의 P0 원인이다.

### 2-3. 테스트가 놓친 이유

- quality graph 테스트는 held를 직접 만든 fixture로 검증한다.
- batch 테스트는 non-null `error`를 모두 `target-error`로 보던 당시 계약만 검증했다.
- agent 테스트는 `error: null`인 합성 런과 미리 만든 graph를 주입한다.
- 실제 `runValidatedLabPrimary -> LabRun -> batch -> agent` 경로를 통과하는 held fixture가 없다.

따라서 기존 테스트가 모두 GREEN이어도 seam 사이 상태 의미는 깨질 수 있다.

## 3. 근본 원인

### 3-1. 실험을 소유하는 깊은 Module 부재

진단 기준 checkout의 Module 경계는 코호트 파일, 단건 LabRun, batch runner, quality graph, CLI,
readiness 문서에 분산돼 있었다. 호출자는 다음 지식을 직접 운반했다.

- 어떤 코호트가 어느 실험의 연속 표본인지
- 어떤 lane을 실행해야 하는지
- held와 error 중 무엇을 재시도해야 하는지
- 어떤 exact run과 graph를 판정에 묶어야 하는지
- pilot 이후 다음 표본으로 진행할 통계적 권한이 있는지
- promotion에 어떤 end-to-end 근거가 필요한지

이 Interface는 얕다. 호출자가 Implementation의 순서와 불변식을 알아야 하므로 국소 수정이 다른
호출자에서 다시 깨진다.

### 3-2. 계획 단위와 실제 작업 단위 불일치

제어면은 공고 1건을 작업 단위로 보지만 실제 실행은 동적으로 팽창한다.

```text
공고
  +-- deep primary
  |    +-- deterministic normalization
  |    `-- 0..2 model repair
  `-- 문서 0..N
       +-- parse/classify
       +-- 후보 chunk 0..N
       `-- adjudication 0..2 rounds
```

공고가 끝난 뒤에야 비용·요청 수·부분 실패를 알 수 있고, 중간 성공 문서나 pass를 독립적으로
재사용하기 어렵다. 범용 DAG 엔진을 도입하지는 않지만, 최소 Experiment Module 내부에서는 lane과
stage receipt를 구분해야 한다.

### 3-3. 추출 계약과 matcher 표현 계층 혼합

현재 평면 criterion에는 actor, track, phase scope가 없다. validator는 source span과 note 정규식으로
scope 손실을 탐지하고 extractor에게 `text_only` 또는 전체 repair를 요구한다. 확대 10건의 실제
교정 issue 10개 중 최소 4개는 prompt가 아닌 deterministic/validator 문제이며, 추가 3개는 matcher
표현 한계가 강하게 개입했다. `repair 6/10`을 곧바로 모델 repair율로 사용하면 안 된다.

이번 구현에서는 DB/matcher schema를 즉시 바꾸지 않는다. 먼저 repair 계측과 ownership을 분리하고,
scoped evidence IR은 shadow projection으로만 도입 여부를 판단한다.

### 3-4. 코호트 층과 통계 gate의 불완전성

현재 층은 source x 공고별 최대 markdown bytes다. 총 입력량, 문서 수, 실제 문서 형식, 후보 수,
예상 request 수, actor/track 복잡도를 반영하지 않는다. pilot 0/5는 운영 canary 통과일 수 있으나
모집단 repair율 GO는 아니다.

pilot과 확대를 별개 수동 명령으로 만들지 않고 하나의 사전 동결된 sequence에서 누적 판정해야 한다.

## 4. 설계 대안과 선택

### A. 기존 runner 국소 보강

held 필드, exit code, 비용 옵션만 수정한다. 빠르지만 코호트·run lineage·통계 진행 권한을 여전히
호출자가 조립한다. P0 응급 수정에는 필요하지만 최종 구조로는 부족하다.

### B. 범용 workflow/DAG 엔진

동적 fan-out, retry, checkpoint에는 유연하다. 그러나 node DSL, plugin registry, 분산 실행까지
Interface가 팽창하고 현재 도메인의 불변식이 다시 caller 설정으로 빠져나갈 위험이 크다.

### C. Gate-first `AnalysisExperiment` Module

외부 Interface는 작게 유지하고, 도메인에 고정된 stage graph와 receipt는 Implementation 내부에 둔다.
이번 트랙의 선택안이다.

## 5. 최종 목표 Interface

호출자와 테스트가 사용하는 외부 seam은 세 동작이다.

```ts
interface AnalysisExperiment {
  prepare(spec: ExperimentSpec): Promise<ExperimentPlan>;

  execute(input: {
    planId: string;
    expectedPlanSha256: string;
    authority: ExecutionAuthority;
    signal: AbortSignal;
  }): Promise<ExperimentReceipt>;

  inspect(experimentId: string): Promise<ExperimentSnapshot>;
}
```

일반 운용 명령은 다음 하나를 목표로 한다.

```bash
pnpm lab:experiment -- --series=deep-v18
```

caller가 직접 지정하지 않는 값:

- retry/reanalyze flag 조합
- pilot/expand wave 크기
- latest-success 스캔 규칙
- repair와 review 순서
- lane별 성공 판정
- 다음 wave 권한

이 Interface는 최종 목표다. 이번 P3에서는 live `execute`, runtime authority, cancellation, `inspect`
read model을 구현하지 않는다. Gate R 이전에는 모델 호출 없는 `createPlan/replay` seam만 연다.

### 5-1. `ExperimentSpec`

```ts
type ExperimentSpec = {
  seriesId: string;
  objective: "deep-primary-repair-rate";
  cohort: {
    seriesManifestSha256: string;
    orderedGrantIds: string[];
    maxSampleSize: number;
    strataVersion: string;
  };
  policy: {
    promptVersion: string;
    validatorVersion: string;
    gatePolicyVersion: string;
    model: string;
    transport: "claude-cli";
  };
};
```

구독 spec에는 USD ceiling을 넣지 않는다. 명목 USD는 receipt usage에만 기록한다.

최종 Interface는 향후 `application-field`, `end-to-end` objective를 추가할 수 있지만 이번 P3에는
`deep-primary-repair-rate`만 구현한다. 기존 15건 replay는 pilot5와 expand10 두 source snapshot을
순서대로 묶은 하나의 legacy series manifest를 만들고 그 manifest SHA를 plan에 결속한다.

### 5-2. 독립 상태 축

```ts
type ExperimentLifecycle =
  | "planned"
  | "running"
  | "finished"
  | "aborted"
  | "infrastructure_failed";

type NoticeOutcome = "publishable" | "held" | "failed";

type LaneOutcome = "passed" | "partial" | "held" | "failed" | "not_applicable";

type GateVerdict = "CONTINUE" | "GO" | "NO_GO" | "INCONCLUSIVE" | "INVALID";

type PromotionEligibility = "eligible" | "ineligible" | "not_evaluated";
```

`success`, `ok`, `error` 하나로 이 축들을 접지 않는다.

### 5-3. receipt 결속

Experiment receipt는 최소한 다음을 exact 값으로 보존한다.

- series/attempt/wave와 parent receipt SHA
- cohort SHA와 전체 고정 순서
- grant ID, input SHA, attachment manifest SHA
- git SHA, package runtime SHA
- prompt/validator/model/transport
- exact run ID와 run artifact SHA
- deep/application lane outcome
- deterministic repair와 model repair call 수
- issue transition과 hold 이유
- 요청 수, input/output token, wall time, 명목 USD telemetry
- gate policy와 누적 판정

기존 15건에는 validator/git/package/attachment manifest와 deterministic/model repair 구분 provenance가
모두 존재하지 않는다. fixture에 값을 추측해 채우지 않고 `legacy_partial` 또는 `unknown`으로 보존한다.
미래 live receipt의 필수 필드와 legacy shadow replay의 가용 필드를 같은 완전성으로 주장하지 않는다.
또한 quality graph의 `evaluatedAt`은 호출 시각에 따라 바뀌므로 전체 graph JSON hash를 결속하지 않고,
`policyVersion + runId + 결정적 lane/node projection`만 판정 근거로 사용한다.

pilot5와 expand10은 처음부터 Nmax=30의 한 sequence로 선동결된 표본이 아니며 합계 strata도 완전하지
않다. 따라서 기존 artifact replay는 `mode=legacy_shadow`, `provenanceStatus=legacy_partial`로 표시한다.
이는 과거 관측이 0/5에서 계속 관찰했어야 하고 누적 6/15에서 중단했어야 한다는 NO-GO 재현에는
쓸 수 있지만 formal GO를 발급할 수 없다. legacy shadow의 GO 경계 도달은 `INVALID`로 막는다.
완전한 provenance와 strata 조건의 positive GO는 별도 synthetic manifest로 evaluator만 테스트한다.

prospective plan은 실행 전 알 수 없는 run ID·경로·artifact SHA를 요구하거나 창작하지 않는다. plan에는
고정 순서, grant/input/attachment manifest, 예상 git/package/validator와 policy만 봉인한다. 실제 run
binding과 실행 provenance, repair ownership·신규 issue 전이는 replay observation에서 받아 plan과 exact
비교한다. run ID·경로·artifact SHA는 표본 전체에서 각각 유일해야 한다. confirmatory observation은
`deterministic primary + model primary = primary repair`, `review repair = 0`,
`new issue after repair = 0`을 모두 증명해야 한다.

replay는 전달된 hash만 신뢰하지 않고 manifest에서 canonical plan을 다시 구성해 plan 전체와 비교한다.
따라서 sequence를 바꾼 뒤 공개 hash 방식으로 다시 해시한 입력도 `plan_not_canonical`로 거부한다. P3 완료
당시에는 파일 loader나 runtime authority를 포함하지 않는 순수 shadow kernel이었고, replay 호출자의
run/quality/lifecycle 문자열을 실제 artifact bytes에서 독립 검증하는 Adapter가 없었다. 이후 단건 live
Adapter가 durable bytes와 issuance chain을 재검증하도록 보강됐지만, P3의 synthetic `GO` receipt 자체는
여전히 evaluator 검증일 뿐 실행·승격 권한으로 사용할 수 없다.

## 6. 실행 Implementation

범용 graph Interface를 공개하지 않는다. Module 내부의 고정 순서는 다음과 같다.

1. 코호트와 입력 provenance 봉인
2. 모델 호출 없는 입력·첨부 preflight
3. objective에 따른 lane 선택
4. runtime authority와 전역 4슬롯 획득
5. deep primary 또는 Kordoc stage 실행
6. validator issue ownership 분류
7. deterministic normalization
8. 실제 extractor defect에만 model repair
9. stage/notice receipt 원자 저장
10. 누적 sequential gate 판정

실제 대체 구현이 있는 seam에만 Adapter를 둔다.

| Seam | Adapter |
|---|---|
| Model execution | prepared deep-only Claude CLI / deterministic fake |
| Experiment artifact | local immutable filesystem의 actual bytes 재검증 / in-memory test |
| User approval | content-addressed approval + approval-key issuance CAS / in-memory test |
| Operational evidence | 고정 gcloud v1·Cloud Run v2·token identity 교차 검증 / deterministic test |
| Runtime authority | exact paused generation DB lease + renew abort / in-memory test |

현재 Gate R Adapter는 deep primary 한 lane만 실행하며 Kordoc·review·audit·confirmations·promotion을
소유하지 않는다. validator, graph compiler, outcome reducer, gate evaluator는 in-process Implementation으로
두며 불필요한 port를 만들지 않는다.

## 7. 통계 gate 제안

repair율 목표를 반복 관측에도 사용할 수 있도록 다음 값을 초기 정책 후보로 둔다.

- `p_good = 0.10`
- `p_bad = 0.20`
- `alpha = 0.05`
- `beta = 0.20`
- `Nmin = 15`
- `Nmax = 30`

이 후보 정책에서의 기대 판정:

| 누적 결과 | 판정 |
|---|---|
| 0/5 | CONTINUE |
| 0/15 | GO |
| 6/15 | NO_GO |

전체 sequence 예비 열거에서 `p=0.10`일 때 GO 52.82% / NO_GO 1.25% / INCONCLUSIVE 45.93%,
`p=0.20`일 때 GO 9.92% / NO_GO 27.45% / INCONCLUSIVE 62.62%였다. 오류율은 후보 한계 안이지만
Nmax 30에서 미결정 비율이 높다. 따라서 이를 범용 SPRT framework나 확정 운영 정책으로 만들지
않고, 상수가 고정된 `repair-sprt-v1` 순수 함수와 전체 sequence 열거 테스트로 먼저 검증한다.

hard gate가 하나라도
깨지면 통계값보다 먼저 `INVALID` 또는 `NO_GO`다.

- exact run/cohort/provenance 불일치
- confirmatory run에 예상 밖 review repair 혼입
- 필수 strata 미관측
- repair 후 신규 issue
- Kordoc field-level gold 없이 end-to-end GO 요청

## 8. 구현 단계와 커밋 경계

### P0. 상태 의미 복구

목표:

- held를 저장·batch·agent에서 품질 terminal로 보존
- held를 error count와 자동 retry에서 제외
- 구 artifact의 `primary_validation_held` sentinel 호환
- promotion/review 소비자는 publishable만 허용하는 기존 fail-closed 성질 보존

P0에서 held의 legacy `error` sentinel을 즉시 삭제하지 않는다. 그렇게 하면 `run.error === null`을
publishable 조건으로 사용하던 검수·감사·승격 소비자가 fail-open될 수 있다. 먼저 중앙 분류 함수로
의미를 한 곳에 모은다.

```ts
type LabRunOutcome = "publishable" | "held" | "failed";

classifyLabRunOutcome(run): LabRunOutcome
isPublishableLabRun(run): boolean
```

legacy truth table:

| 명시 outcome | error | 분류 |
|---|---|---|
| `publishable` | `null` | publishable |
| `held` | `null` | held, 신규 형식 |
| `held` | `primary_validation_held: ...` | held |
| 없음 | `primary_validation_held: ...` | held |
| 없음 | `null` | legacy publishable |
| `held` | 기타 error | failed, inconsistent |
| `publishable` | non-null | failed, inconsistent |
| 없음 | 기타 non-null | failed |
| `null`·알 수 없음 | 어떤 값 | failed, malformed |
| `publishable`·`held` | error 필드 누락 | failed, malformed |

batch는 `target-held` 이벤트와 `summary.held`를 별도로 기록한다. held는 표본 실행 완주에는 포함하지만
품질 GO 분자에는 포함하지 않는다. readiness는 이를 실행 BLOCKED로 오해하거나 GO로 통과시키지 않고,
품질 `ITERATE` 또는 experiment gate의 해당 scope 판정으로 보존한다. agent는 terminal held를 repair
대상으로 만들지 않고 명시적 held/blocking 결과로 보고한다.

검수·감사·확정 질문·dispatch·review selector·promotion candidate/release·관리 UI는 모두 최종적으로
중앙 outcome을 사용해야 한다. 단, ApplicationRoundtripRun과 문서의 `error`는 LabRun과 다른 계약이므로
`rg` 결과를 무차별 치환하지 않는다.

P0는 fail-open 창을 피하기 위해 세 수직 슬라이스로 나눈다.

1. **P0-A consumer hardening**: truth table, review/audit/confirmations/dispatch selector,
   `planGrantPromotion`과 source artifact 재검증을 `isPublishableLabRun`으로 방어한다. writer는 유지한다.
2. **P0-B writer와 batch 상태**: consumer가 GREEN인 뒤 신규 held를 `error:null`로 저장하고,
   `target-held`, `summary.held`, held telemetry, legacy sentinel scan을 도입한다.
3. **P0-C agent와 readiness**: terminal index를 publishable/held로 분리하고, primary held에는 review·repair를
   실행하지 않는다. 4 publishable + 1 held 표본은 batch/sample complete지만 deep quality 실패와
   최종 `ITERATE`로 판정한다.

필수 테스트:

- 실제 모양의 held LabRun이 `target-held`로 종결
- `summary.held=1`, `errorRuns=0`
- `retryErrors=true`여도 heldCurrent는 pending이 아님
- agent가 held를 “성공 런 없음”으로 throw하거나 재분석하지 않음
- provider error는 계속 retry/error 대상

구현 커밋: `f337380`(소비자 판정 단일화), `3b28530`(보류 저장·batch·agent 분리)

### P1. 구독 USD를 telemetry로 전환

상태(2026-08-14): 구현·회귀 검증 완료. batch/review/audit/confirmations와 subscription agent는
구독 명목 USD로 신규 착수를 멈추지 않고, API는 기존 완료 비용 기반 soft stop을 유지한다. 일반
aggregate와 promotion release aggregate도 API subset만 비용 gate로 사용하며 구독 값은 transport별
telemetry로 남긴다. transport 기록 이전 verified-local release는 검증된 source artifact에서 구독으로
복원하고, production deep·일반 legacy는 API로 해석한다. dual provenance와 transport/source 불일치는
manifest 검증에서 거부한다. source는 grant마다 하나만 허용하고 source run ID를 promotion plan run ID와
exact 결속해 같은 공고의 다른 구독 run으로 API 비용을 위장하지 못하게 한다. 과거 구독 `cost-cap`
batch도 전건 terminal·오류 0인 경우에만 readiness의
실행 완주로 좁게 복원하며 API `cost-cap`은 기존대로 차단한다.

목표:

- `claude-cli` 실행은 nominal USD cap으로 중단하지 않음
- API transport는 기존 완료 비용 기반 soft stop을 보존
- batch/review/audit/adjudication/confirmation이 같은 transport 정책 사용
- subscription agent가 batch뿐 아니라 review/audit/adjudication에 전달하던 USD progression 옵션 제거
- confirmations가 Max window exhaustion을 일반 target 실패로 재시도하지 않고 신규 착수를 중단
- 과거 CLI 인자는 즉시 깨뜨리지 않되 구독에서는 1회 deprecation warning을 내고 실행 정책과
  새 snapshot에는 넣지 않음

공통 정책은 다음 작은 union으로 고정한다.

```ts
type NominalCostPolicy =
  | { kind: "api-settled-usd-stop"; maxUsd: number }
  | { kind: "subscription-telemetry-only" };
```

`api-settled-usd-stop`도 동시 실행이 완료된 뒤 누적값을 확인하므로 hard cap이 아니다. P1은 이
기존 API semantics를 보존할 뿐 reservation ledger를 추가하거나 hard cap으로 이름을 바꾸지 않는다.
`ai-adjudication`은 현재 구독 전용이므로 telemetry-only만 적용한다.

필수 테스트:

- claude-cli에서 임의의 높은 nominal USD에도 전 target 실행
- api에서 기존 cost-cap 동작 유지
- plan/summary에는 nominal USD telemetry가 계속 남음
- 구독 agent가 USD를 progression capability로 전달하지 않음

구현 커밋: `4eca545`(실행 정책), `ebee820`(aggregate), `fea1ae5`(release 호환·결속)

### P2. lane 명시화

상태(2026-08-14): P2-a 구현·검증 완료. 일반 CLI·단건 route는 deep-only가 기본이고,
Kordoc은 `withApplicationRoundtrip=true`인 명시적 요청에서만 실행한다. deep repair는
기존 Kordoc run id가 있을 때만 검증·재결속하고 없으면 deep-only로 실행한다.
application retry는 현행 end-to-end를 유지한다. application-only executor는 이 단계에서
추가하지 않았으며 후속 seam으로 남긴다.

목표:

- CLI가 Kordoc을 암묵적으로 강제하지 않음
- deep-repair 실험은 신규 Kordoc 미실행(기존 불변 run만 검증·재결속)
- end-to-end 실험만 application lane 필수
- application retry가 deep primary를 다시 실행하지 않도록 후속 seam 준비

필수 테스트:

- flag/spec 없는 deep 실행에 Kordoc override 없음
- end-to-end policy에는 Kordoc 포함
- deep/application 결과가 독립 outcome으로 남음

구현 커밋: `eb0e6cf` `딥분석과 Kordoc 실행 레인을 명시적으로 분리하다`

### P3. 최소 Experiment kernel

상태(2026-08-14, P3 완료 시점): 구현·회귀 검증 완료. 당시 runtime export는 아래 두 함수뿐이며 CLI, executor, store,
runtime authority, DAG, UI는 추가하지 않았다. legacy v17 fixture는 실제 두 cohort와 15개 run artifact의
exact SHA를 봉인해 `0/5 CONTINUE`, `6/15 NO_GO`를 재현한다. pilot batch lifecycle은 artifact가 없어
`unknown`으로 유지하고, legacy가 통계 GO 경계에 닿더라도 최종 `INVALID`다.

목표:

- immutable plan/receipt 타입
- exact run/cohort binding
- 순수 sequential gate evaluator
- 기존 pilot5와 expand10을 모델 호출 없이 shadow replay

이번 단계의 외부 seam은 두 순수 동작뿐이다.

```ts
createDeepRepairExperimentPlan(manifest): DeepRepairExperimentPlan
replayDeepRepairExperiment(plan, observations): DeepRepairExperimentReceipt
```

P3 단계에서는 실제 batch executor를 교체하지 않았고 `execute/inspect`, runtime authority,
cancellation Adapter를 만들지 않았다. 새 kernel을 기존 artifact 위에 먼저 배치하고 판정의 정본을
옮겼다. 기존 `bulk-pilot5` artifact는 v17 pilot5와 다른 ID이므로 성공 fixture로
재사용하지 않고 cohort mismatch가 `INVALID`가 되는 음성 fixture로만 사용할 수 있다. pilot5의 batch
lifecycle artifact가 없으므로 `finished` 상태를 창작하지 않는다.

필수 테스트:

- 동일 spec과 cohort는 동일 plan SHA
- 한 필드 drift는 INVALID
- 0/5 CONTINUE, 0/15 GO, 6/15 NO_GO
- run ID가 graph와 다르면 INVALID
- lifecycle finished라도 quality NO_GO 가능
- prospective plan에 미래 run binding을 넣으면 생성 단계에서 실패
- 실제 execution provenance, run ID/path/SHA 중복·drift는 INVALID
- confirmatory review repair, repair 후 신규 issue, 불완전 repair provenance는 INVALID
- plan sequence를 바꾸고 자체 재해시해도 canonical plan 불일치로 INVALID

여기서 0/15 GO는 완전한 synthetic manifest에 대한 evaluator 계약이다. 기존 pilot/expand legacy
artifact는 0/5 CONTINUE와 누적 6/15 historical NO_GO만 재현하며 GO 근거로 사용하지 않는다.

검증된 legacy plan/receipt SHA는 다음과 같다.

| artifact | SHA-256 |
|---|---|
| series manifest | `9a08ee3ddff246c9abce1fa77aeaac20977937326ba07e8121646c4918b06cd3` |
| experiment plan | `2fef1b526e7c102121625f49f581c376b101be2ec28c6f948e0644439aa87161` |
| pilot5 receipt | `46e19786dc4d6ff5df0716ff05da4ceb5034170966887894224350329473c48d` |
| cumulative15 receipt | `5989129c9ceb9d2873d969e0178685d6df4837b0cbc53b5b4fa6ccead7b6ce7c` |

구현 커밋: `5fc7a62` `딥분석 실험 계획과 순차 게이트를 도입하다`

#### P3 이후 Gate R 단건 Adapter

P3의 두 순수 함수는 유지하면서, live capability는 legacy batch에 되돌려 주지 않고 고정된
`deep-v18` 실험 Module 안에 단계적으로 결속했다.

| 커밋 | 경계 | 결과 |
|---|---|---|
| `a9627ca` | 입력·repair provenance | attachment manifest와 deterministic/model repair·신규 issue 전이를 봉인 |
| `8bd4cfe` | 단건 실행 kernel | exact plan의 다음 target 한 건만 실행하고 durable run/observation/receipt를 검증 |
| `da039eb` | 사용자 승인 증거 | self-attested authority를 제거하고 content-addressed approval과 만료·binding을 검증 |
| `80c8002` | proposal preparation | 과거 표본을 제외한 exact 30·15/15 wave·입력/provenance를 모델 없이 불변 proposal로 준비 |
| `52663e6` | authority issuer | 현행 worker 증거·paused runtime·parent ancestry를 검증하고 approval-key CAS winner 하나만 발급 |
| `a263bc4` | 상태별 recovery | active lease 0을 authority에 봉인하고 no-start/ambiguous/terminal을 분리해 exact expired lease를 복구 |
| `8856e4f` | 실제 proposal 준비 | 임의 과거 run 전체 배제로 희소 층이 고갈되던 규칙을 v17 정식 비교 15건+committed experiment 제외로 축소 |

외부 실행 흐름은 `prepare proposal → 사용자 승인 artifact → issue authority → exact-next 1건 실행 →
receipt/replay`로 고정된다. `CONTINUE`여도 다음 target은 자동 실행하지 않고 새로운 사용자 approval과
authority가 필요하다. live replay의 wave lifecycle은 계획 wave 전체 소진 여부가 아니라 receipt에 포함된
exact observed prefix의 단건 실행이 모두 terminal인지 판정한다. 아직 실행하지 않은 suffix는 관측 집합
밖이며 `unknown` 실행으로 취급하지 않는다. 이 경로에는 Kordoc, review/audit/confirmations, promotion capability가 없으며,
운영자가 사용자 대신 승인 artifact를 만드는 CLI도 제공하지 않는다.

비정상 종료는 새 workflow나 자동 retry로 확장하지 않고 같은 Module 안에서 아래 세 상태로만 처리한다.

| durable attempt 상태 | recovery 결과 | 다음 실행 |
|---|---|---|
| claim 없음 | 만료 runtime lease만 exact CAS로 해제, attempt는 빈 상태 유지 | 새 사용자 approval·authority로 같은 exact slot 가능 |
| claim 있음 + terminal 없음 | 모델 착수 여부 불명이므로 immutable abandonment receipt로 series 중단 | 같은 slot 자동/수동 재실행 금지 |
| 유효 terminal 있음 | terminal/content receipt bytes를 보존하고 runtime lease만 해제 | 기존 통계·gate 결과 유지 |

`lab:experiment:recover -- --inspect=<authority-sha256>`는 읽기 전용 진단이다. 실제 복구는 exact attempt와
runtime snapshot, 로컬 프로세스 종료 확인을 묶은 별도 recovery approval이 있을 때만
`--approval=<recovery-approval-sha256>`로 수행한다. recovery는 모델·대상 선정·promotion capability를
갖지 않으며 `deep-v19` 같은 successor series를 자동 생성하지 않는다.

2026-08-14 실제 `lab:experiment:prepare -- --series=deep-v18`도 성공했다. 처음에는 과거 cohort/run
105건을 모두 제외해 현재 `kstartup/thick` 재고 7건 전부가 소진되면서 실패했다. 이 범위는 통계적
비중복에 필요하지 않은 탐색 런까지 영구 제외한 과도한 방어였다. 현재는 품질 판정 근거인 v17
pilot5+expand10 15건과 이미 commit된 experiment만 제외한다. 그 결과 다음 proposal이 봉인됐다.

- proposal: `bdc6147f6522240d44056c28ab8f9ef75b88a01d6e46f9a1df4775003acf6b17`
- plan: `1a381b0f8f227bf86e3bec61a581b3cdc00b0996f9f976fbbb316c0b71a9e0cc`
- 30건/15+15 wave, 6개 필수 층 모두 포함
- 첫 대상: `cae81ac1-6f71-4c44-9aa0-ad7e228c27fa`, `[충남] 2026년 6차 특허분쟁 대응전략 지원사업 공고(충남연계)`
- soft quota: rich criteria 6/6, 통합공고 3/4 경고. hard gate로 승격하지 않음
- proposal 자체의 `liveExecutionAuthorized=false`; authority·모델 실행·promotion은 0건

### P4. Kordoc preflight와 bounded stage

상태(2026-08-15): preflight와 ready 5건의 exact canary 실행을 종결했다. 범용 대량 실행은 미승인 상태다.

- 실제 파일 형식 probe
- 정책/지침/목록/참고문서 분류 보강
- 총 candidate/request 수 기록
- ambiguous 후보만 LLM 판정하는 triage 검증
- overflow는 silent truncation이 아니라 review_required

정책/법령 filename은 신청서 후보에서 제외하고, 구독 경로는 강한 구조 신호 505개를 결정적으로
종결해 실제 LLM 후보만 730개로 줄였다. proposal/source allowlist와 단건 receipt가 실행 범위를
봉인한다. 각 target은 별도 승인 범위에서 순차 실행했고 결과는 complete 2·partial 3이다. 이 결과는
새 공고나 다음 proposal의 자동 실행 권한이 아니다.

### P5. scoped evidence IR shadow

P0~P4의 issue ownership 계측 뒤 필요성이 재확인될 때만 진행한다. DB schema와 matcher를 바로
마이그레이션하지 않고 actor/track/phase scope를 가진 evidence IR을 현재 criterion으로 shadow
projection한다. 투영 불가능한 사실은 명시적으로 held/text-only로 남긴다.

## 9. Gate R: 새 모델 실행 재개 조건

다음 조건을 모두 만족해야 새로운 비중복 단건 canary의 live 실행 authority를 발급·제시할 수 있다.

- [x] P0 상태 회귀 테스트 GREEN
- [x] 구독 USD cap 미적용/API cap 보존 테스트 GREEN
- [x] held/error/publishable 소비자 전수 리뷰 완료
- [x] pilot5 shadow 판정 CONTINUE
- [x] 누적 15건 shadow 판정 NO_GO
- [x] exact run/graph/provenance drift 음성 테스트 GREEN
- [x] 관련 package typecheck, analysis-lab 테스트와 web/admin production build GREEN
- [x] 구독 분석 runtime pause 유지 확인 — generation 67, active lease 0
- [x] exact proposal·approval·plan·artifact·receipt에 결속된 최소 live Adapter와 issuer 구현·회귀 검증
- [x] 비정상 종료의 no-start/ambiguous/terminal 상태별 exact runtime recovery 구현·회귀 검증
- [x] v17 정식 15건과 비중복인 exact-30 `deep-v18` proposal 실제 준비
- [x] 운영 worker `observe_only` + `CLAIM_SCOPE=unconfigured` 유지의 현행 확인
- [x] `deep-v18` sequence 0~11의 exact 범위·모델·parent receipt 사용자 승인과 terminal receipt 획득
- [x] `daa7b51` 이후 새 matching-readiness provenance를 봉인한 `deep-v19` proposal/plan과 sequence 0~9 사용자 승인·terminal receipt

현재 추가 live 판정은 **NO-GO**다. `deep-v19` sequence 0~9와 Kordoc ready 5건이 모두 종결됐고,
Kordoc 결과는 sequence 7·1 complete, sequence 3·6·0 partial이다. 마지막 runtime은 `paused`,
generation 121, active deep/application lease 0이었다. sequence 1은 별도 exact 사용자 승인과 재인증 뒤
receipt `dd5db646...`로 complete 종결했으며, 미해결 후보와 구조 경고는 모두 0이다.
formal verdict는 deep-primary 10건에서 `CONTINUE`다. 승인 범위가 끝났고 이 결과 정본화 뒤 checkout
provenance도 달라지므로 같은 plan의 다음
target을 자동 실행하지 않는다. `cunote-codex-dev` 재인증 뒤 고정 operational capture도
성공했고, Cloud Run worker generation 97은 `observe_only`, 비종결 execution 0건, deployed SHA
`0b94576c...`는 safe worker baseline의 후손이었다. 배포 Job에는 claim scope literal이 없지만 이 exact
deployed worker가 미설정을 fail-closed `unconfigured`로 해석함을 해당 SHA의 코드와 ancestry로 검증하고
guard도 같은 의미로 정규화했다. 운영 Job 배포·설정 변경은 없었다.

다음 딥 매칭 실행은 legacy batch가 아니라 `새 proposal 준비 → 대상·모델·중단 조건 사용자 승인 →
authority 발급 시 현행 worker evidence 재캡처·결속 → exact 단건 실행`이다. 이번 Kordoc ready 5건의
종결 결과는 이 통계 plan을 연장하거나 새 공고를 자동 실행하는 권한이 아니다.

여기서 plan의 git/package/validator provenance는 로컬 구독 canary 실행 checkout을 봉인하고, operational
evidence의 deployed git SHA·image·UID·generation·etag는 운영 worker의 현행 중단 상태를 봉인한다. 두 출처는
각자의 실제 상태와 exact 검증하되 서로 같은 git SHA를 요구하지 않는다. 운영 evidence는 두 환경변수를
실제 fail-closed gate로 집행하는 최소 worker contract `ec8cec75566e9ba5d07aead3837ce48501b1b6a9`의
descendant인지도 로컬 git history에서 증명해야 한다. commit object 누락·shallow history·non-descendant는
자동 fetch 없이 NO-GO다. Gate R 증거를 맞추기 위한 운영 worker 재배포는 별도 운영 변경이며 이 재개
절차에 포함하지 않는다.

Gate R 통과 전에는 `lab:agent --execute`, smoke, 새 **live** batch/target selection, 검수·감사,
자동 repair, promotion approve/write를 실행하지 않는다. 정적 admission은 이 운용 규칙을 코드로
강제한다. 단, `lab:experiment:prepare -- --series=deep-v18`은 모델·runtime lease·authority·promotion
capability가 없는 proposal-only 예외다. 과거 표본과 겹치지 않는 exact 30건을 DB/R2 read-only로
봉인하고 고정 series marker를 남길 뿐이며, 그 proposal이나 marker만으로 canary 실행은 승인되지 않는다.

## 10. 과구현·오구현 방지선

이번 트랙에서 하지 않는다.

- 범용 DAG/workflow engine 또는 DSL
- graph DB와 전면 event-sourcing
- 별도 scheduler daemon
- 모든 helper의 port/interface화
- 구독 nominal USD hard cap의 다른 이름 재도입
- actor/track DB schema 즉시 전면 변경
- patch response schema 선도입
- 새로운 모델·검수 lane 추가
- confirmatory 실험 중 자동 review repair
- 자동 promotion
- 신규 experiment dashboard 선구현(기존 화면의 pause 안내는 safety backstop으로 유지)
- legacy batch 방식의 확대 10건 즉시 재실행
- 범용 successor series 계보, 자동 retry 또는 자동 recovery approval

판단 기준은 간단하다. 현재의 반복 원인을 제거하거나 Gate R을 검증하지 않는 코드는 이번 구현에
포함하지 않는다.

## 11. 검증 기록

초기 기준선에서는 batch/agent 단위 테스트만 GREEN이었고 실제 held seam과 실험 authority가 비어 있었다.
P0~P3, Gate R admission, proposal/approval/issuer-bound 단건 Adapter와 상태별 recovery를 합친
`8856e4f`에서 다음을 다시 확인했다.

- 상태·batch·agent: `lab:run-outcome:test`, `lab:batch-runner:test`, `lab:batch-job:test`,
  `lab:agent:test` GREEN
- 모델 레인: `lab:transport:test`, `lab:ai-review:test`, `lab:ai-audit:test`,
  `lab:confirmations:test` GREEN
- 품질·비용·승격: `lab:quality:test`, `lab:aggregate:test`, `lab:release:test` GREEN
- 실험·lane: `lab:experiment:prepare:test`, `lab:experiment:issue:test`, `lab:experiment:test`,
  `lab:experiment:recover:test`, `lab:roundtrip:test`, `lab:application-precompute:test` GREEN
- 운영 계약: `verify:deep-analysis-contract`, `verify:subscription-agent-ops` GREEN
- 타입·빌드: web/admin typecheck, packages build, web/admin Next production build GREEN
- web/admin build의 기존 dynamic filesystem/NFT tracing 경고는 남았지만 compile, TypeScript, page
  generation은 모두 성공했다.
- runtime 정본: `paused`, generation 67, local/production allowed false, active lease 0
- 단건 Adapter·issuer·상태별 recovery 회귀: terminal 결과 보존, no-start 슬롯 보존,
  ambiguous 재실행 차단, resolution-only 사전 차단, forged recovery 거부 GREEN
- 실제 proposal preparation: 첫 실행은 과도한 전체-history 제외로 `kstartup/thick` 고갈을 재현했고,
  제외 범위를 v17 정식 15건+committed experiment로 좁힌 뒤 exact 30건 봉인 성공
- 이 기준선 시점에는 실제 gcloud/token/Cloud Run 조회, approval/authority 발급, 모델 실행, 승격·배포를 수행하지 않음
- 기존 사용자 소유 미추적 문서 `docs/research/2026-08-10-루프와-그래프-엔지니어링-접근.md` 보존

이후 operational capture와 사용자 승인을 거쳐 `deep-v18` sequence 0~11을 실제 실행했고, 12건 모두
terminal receipt와 runtime release를 남겼다. 이 live 증거는 §1-4와 §9에 기록한다. 현재 추가 live가
NO-GO인 이유는 승인 부재의 역사 상태가 아니라 `daa7b51` 이후 새 코드 provenance를 봉인한 plan과
그 exact 범위의 새 승인이 아직 없기 때문이다.
