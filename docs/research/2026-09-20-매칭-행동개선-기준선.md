# 매칭 행동 개선 W0 기준선

작성일: 2026-09-20 KST
상태: 읽기 전용 기준선 완료. 모델 호출·DB 쓰기·배포·서버 기동 없음.

## 1. 결론

2026-09-20 15:46:55 KST의 동일한 공개 공고 스냅샷에서 고유 후보는 1,508건이었다.
이 중 현재 source revision과 matching serving evidence가 결속된 `verified`는 40건,
안전한 원문 탐색 후보인 `discovery`는 1,468건이다. 9월 19일 P5 기록의
42/1,466과 전체 합계는 같지만 검증 수준 2건이 바뀌었으므로 이전 숫자를 현행 수치로
복사하지 않는다.

기존 네 가상기업을 전체 1,508건에 대조하면 네 시나리오 모두 `openNow=0`이지만,
미확정 후보는 1,504~1,506건이었다. 따라서 현재 첫 화면의 “지금 정보로 확정된 공고가
없어요”는 후보가 없다는 뜻이 아니다. 기존 결과 UI는 확정 공고와 한 번의 답으로 확정되는
공고만 헤드라인의 `hasActionableMatches`에 포함하고, 대부분을 차지하는 discovery 및
`needs_core_review` 후보를 제외해서 0건 인상을 만든다.

이 문서는 기준선만 기록한다. W1 이후 구현이 같은 공유 작업공간에 들어오기 시작한 뒤의
HMR 화면과 소스 diff는 결과 수치와 원인 판정에서 제외했다.

## 2. 재현 경계

| 항목 | 기준 |
| --- | --- |
| 기준 소스 | `8668796681416ed92f8938c7e3b490c193bdc4c8` (`main`, `origin/main` 일치) |
| 데이터 관측 시각 | `2026-09-20T06:46:55.099Z` |
| 최신 수집 시각 | `2026-09-20T06:10:49.672Z` |
| 저장소 경로 | Drizzle `listActiveGrants` → current matching evidence projection → core dashboard/teaser |
| 유니버스 상한 | 20,000건, sentinel 초과 시 실패 |
| discovery 플래그 | 진단 프로세스 안에서만 `MATCH_DISCOVERY_CANDIDATES_ENABLED=true` |
| 회사 표본 | 저장소의 합성 가상기업 4개, 실제 사용자 표본 아님 |
| 쓰기 여부 | DB write 0, 모델 호출 0, 외부 배포 0 |

공개 후보의 코드상 범위는 `serving_state=visible`, 상태가 `open|upcoming|unknown`,
신청 종료일이 없거나 KST 기준일 이후인 공고다. 명백히 오래된 무기한 공고와 K-Startup
종료 payload를 추가로 제외하고, 확정 dedup member는 canonical 공고로 접는다.
구현 위치는 `apps/web/src/lib/server/repositories/drizzle.ts:288-440`과
`apps/web/src/lib/server/repositories/activeGrantFilter.ts`다.

재현은 저장소 환경을 로드한 뒤 다음 두 읽기 경로를 한 프로세스에서 실행했다.

1. `loadServiceGrantUniverse({ asOf, scanLimit: 20_000 })`
2. 반환된 동일 배열을 `buildDashboard({ limit: grants.length })`와
   `buildTeaser({ limit: 8 })`에 네 가상기업별로 전달

집계 시 ID는 `grant.id`, 없으면 `source:source_id`를 사용했다. 원인 집계는 card 단위로
중복 제거했고, 다중 라벨과 대표 원인을 모두 기록했다.

## 3. 공개 공고 퍼널

| 단계 | 건수 | 의미 |
| --- | ---: | --- |
| 공개·활성·dedup 고유 후보 | 1,508 | closed 제외. 접수 예정·일정 미확인 포함 |
| `open` | 542 | 저장 상태가 open이고 종료일 gate 통과 |
| `unknown` | 944 | 모집 상태를 확정하지 못함 |
| `upcoming` | 22 | 접수 예정 |
| `verified` | 40 | current source revision과 serving evidence 결속 |
| `discovery` | 1,468 | 개인화 자격을 만들지 않는 안전 projection |
| Bizinfo / K-Startup | 1,266 / 242 | source별 고유 후보 |
| 중복 ID | 0 | 1,508개 순회 기준 |

P5의 2026-09-19 11:57:06Z 기록은 같은 총 1,508건에서 `verified=42`,
`discovery=1,466`이었다. 현재 두 건의 수준 변화 원인은 이 W0에서 쓰기나 과거 revision
재실행 없이 확정하지 않았다. source drift, serving evidence 결속 변화 등을 후보로만 두고
현재 projection 결과를 정본으로 사용한다.

## 4. 회사별 0건 기준선

`후보`는 `recommendationTier !== not_recommended`인 고유 카드다. `다음 행동 없음`은
8개 teaser 카드에 원문/상세 이동 또는 구조화된 확인 행동이 하나도 없고 전역 다음 질문도
없는 경우로 정의했다.

| 합성 시나리오 | 확정 접수 중 | 후보 | `needs_core_review` | 화면 카드 | 전역 다음 질문 |
| --- | ---: | ---: | ---: | ---: | --- |
| 충남 장애인기업·조건 완성 | 0 | 1,505 | 1,505 | 8 | `target_type` |
| 서울 장애인기업·지역 불일치 | 0 | 1,506 | 1,506 | 8 | `target_type` |
| 충남 장애인기업·확인서 미확인 | 0 | 1,505 | 1,505 | 8 | `target_type` |
| 미래내일 참여기업·조건 완성 | 0 | 1,504 | 1,504 | 8 | `target_type` |

이 네 개 합성 시나리오 안에서만 계산한 비율은 다음과 같다.

- 확정 0건: 4/4, 100%
- 후보도 0건: 0/4, 0%
- 다음 행동 없음: 0/4, 0%

이는 실제 사용자 발생률이 아니다. 실제 사용자 company scope를 읽거나 식별자를 출력하지
않았고, 네 시나리오는 특정 공고의 회귀 검증을 위한 가상 프로필이다.

## 5. 0건 원인 분류

원인은 한 카드에 복수로 남겼다. 예를 들어 discovery 카드는 `criteria_extracted=false`이면서
표시 tier가 `needs_core_review`라 `미분석`과 `원문 검토`를 동시에 가진다. 일정 미확인도
자격 원인과 직교한다.

| 시나리오 | 확정 미충족 | 회사 입력 부족 | 원문/분석 검토 | 미분석 discovery | 일정 미확인 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 충남 완성 | 5 | 39 | 1,507 | 1,468 | 944 |
| 서울 지역 불일치 | 5 | 39 | 1,507 | 1,468 | 944 |
| 충남 확인서 미확인 | 5 | 39 | 1,507 | 1,468 | 944 |
| 미래내일 완성 | 4 | 35 | 1,505 | 1,468 | 944 |

요약 합계가 고유 공고 1,508건과 일치하도록 대표 원인은 다음 우선순위로 한 개만 골랐다.

1. 검증된 hard fail
2. 회사 입력으로 해소 가능한 hard unknown
3. 미분석 discovery
4. 그 밖의 원문·분석 검토
5. blocker 없음

| 시나리오 | hard fail | 입력 부족 | 미분석 | 잔여 원문 검토 | 합계 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 충남 완성 | 5 | 34 | 1,468 | 1 | 1,508 |
| 서울 지역 불일치 | 5 | 34 | 1,468 | 1 | 1,508 |
| 충남 확인서 미확인 | 5 | 34 | 1,468 | 1 | 1,508 |
| 미래내일 완성 | 4 | 34 | 1,468 | 2 | 1,508 |

이 분류는 제품의 최종 설명 계약이 아니다. W1에서 criterion별 owner/action을 DTO로 고정할 때
같은 정의를 테스트 fixture로 옮겨야 한다.

## 6. 변경 전 코드에서 확인한 실패 원인

아래 줄 번호는 기준 SHA `8668796`의 파일 기준이다.

1. **후보가 있어도 0건 헤드라인**
   `apps/web/src/features/match-results/ResultsHero.tsx:41-66`은 `openNow + oneAnswer`만 `hasActionableMatches`로 센다.
   `needs_core_review`, 혼합 상태, discovery 후보는 모두 빠져 1,500건 이상의 후보가 있어도
   “확정된 공고가 없어요”가 가장 큰 문장이 된다.

2. **8개 teaser와 전체 원인 수 혼용**
   `packages/core/src/use-cases/build-teaser.ts:136-171`은 기본 8개 중 review quota를 3개로 잡는다.
   `apps/web/src/features/match-results/Programs.tsx:174-186`의 “원문 확인 필요” 수는 서버 전체 `needsCoreReview`가 아니라
   `groups.checkSource.length`, 즉 현재 내려온 카드만 센다. 그래서 전체 검토 후보가 1,500건대여도
   화면에는 3건처럼 보일 수 있다.

3. **혼합 상태가 전역 질문 후보에서 탈락**
   `packages/core/src/use-cases/build-teaser.ts:66-73`은 전역 질문 planner 입력을 tier가 정확히
   `needs_profile_input`인 match로 제한한다. 회사 입력으로 일부 조건을 해소할 수 있어도 다른
   원문 검토 조건 때문에 card tier가 `needs_core_review`가 된 혼합 카드는 후보에서 빠진다.
   `packages/core/src/matching/question-planner.ts:96-131` 자체는 구조화·검수된 profile-resolvable criterion만 안전하게
   고를 수 있으므로 호출자 필터가 실제 병목이다.

4. **전체 프로필 완성도가 공고별 다음 행동보다 먼저 보임**
   `apps/web/src/features/match-results/ResultsHero.tsx:82-93`은 전체 profile row의 known/total 게이지를 헤드라인 바로 아래 둔다.
   `apps/web/src/features/match-results/logic.ts:767-797`도 전체 row 기준이다. 특정 후보를 해결하는 필드와 무관한 정보까지 섞여
   입력 후 결과 변화가 없는 것처럼 보일 수 있다.

5. **불완전한 조건 목록도 완전한 분모처럼 표시**
   `apps/web/src/features/match-results/Programs.tsx:457-473`은 현재 rule trace의 pass/fail/unknown 합을 `hardTotal`로 삼아
   `필수 자격 X/Y 충족`을 표시한다. extraction 전체성 gate나 원문에서 아직 추출하지 못한 조건은
   분모 밖에 있어도 Y가 완전한 자격 목록처럼 읽힌다.

6. **첫 원인만 보여 복합 blocker가 사라짐**
   `apps/web/src/features/match-results/Programs.tsx:525-550`은 `adminSourceReview[0]`과 `companyProfile[0]`만 별도 안내한다.
   전체 개수, 해소되는 조건, 입력 뒤에도 남는 원문 조건을 한 계약으로 설명하지 않는다.

7. **표시용 근거를 label 문자열에서 재파싱**
   `apps/web/src/features/match-results/logic.ts:237-269`은 matcher label을 정규식으로 분리하고 company value가 없으면
   “확인된 기업정보와 일치”를 넣는다. 저장 근거가 없을 때도 확인된 값처럼 보일 수 있다.

8. **같은 링크를 다른 행동처럼 중복 표시**
   `apps/web/src/features/match-results/Programs.tsx:577-585`의 “조건 전체 보기”와 “공고 상세”는 둘 다 동일한 `detailHref`다.
   원문 이동, 내부 상세, 특정 프로필 질문의 목적지가 분리되지 않았다.

## 7. P5·Preview·Production·로컬 서버 상태

| 표면 | 확인 결과 |
| --- | --- |
| P5 초기 preview | `8e3e273…`, discovery/verified core review 연결 결함 발견 |
| P5 최종 preview | `85bf28a…`, `dpl_5hb5BqGwCjPusQ9RzDWpbTePcbnq`, 가상기업 3사례 PASS |
| 현재 Production | `dpl_Gw1NEqomrUykFuVMxc5z4JvAgTzC`, READY, 2026-09-20 15:00:32 KST, source SHA `8668796…` |
| Production alias | `changupnote.com`, `www.changupnote.com`이 위 deployment를 가리킴 |
| 로컬 `:4010` | 이 저장소의 사용자 소유 `next dev`, 2026-09-20 14:34:30 KST 시작 |
| 로컬 `:3000` | `/Users/ffgg/glyph.works/gumiyum.landing`, 이 조사와 무관 |

P5 문서의 “Production 미적용”은 9월 19일 preview 인수 시점 기록이다. 현재 Production에는
P5 코드와 인수 기록을 포함한 exact HEAD가 배포돼 있다. 다만 Production의 sensitive flag 값은
읽기 전용 inspect에서 복호화되지 않았고, 인증 사용자 pagination과 개인화 결과를 호출하지
않았으므로 discovery 기능의 실제 Production 활성화까지 증명하지 않는다.

로컬 `:4010`의 변경 전 API를 가상기업으로 읽었을 때 discovery flag가 꺼진
promotion-filtered 유니버스 49건, `openNow=0`, `oneAnswer=0`, `needsCoreReview=41`, 표시 8건이었다.
이 49건은 current source projection의 `verified=40`과 정의가 다르므로 서로 대체하지 않는다.
기준선 집계 뒤 W1~W4 구현 diff가 공유 작업공간에 들어왔고 HMR이 이를 반영했기 때문에 이후
브라우저 화면은 이 문서의 변경 전 기준선으로 사용하지 않는다.

## 8. 한·독 세미나 회귀 fixture

공개 공고라 회사 정보 없이 semantic fixture로 보존한다.

```yaml
fixture: kstartup-179124-han-germany-seminar
observed_at: 2026-09-20T06:52:21Z
grant_id: 9b963f2a-bf9b-46e5-b684-773fc48ee930
source: kstartup
source_id: "179124"
status: open
apply_end: 2026-09-22
raw_collected_at: 2026-09-19T00:00:29.649Z
raw_hash: 211096fcf623051cdec3416f2843dc3a20c171818b3c6e5243d57b51034c4d46
matching_level: verified
source_revision_sha256: 53d821af8b3fe1b2eccb460f65b155c6596140e7a965e41b2c9e846b7ee101ca
expected_logic:
  - "바이오·헬스케어 분야"
  - "AND"
  - "(GBC 입주 OR 독일·유럽 시장 진출 희망)"
portal_target_summary:
  value: "연구기관, 일반기업, 1인 창조기업"
  role: "open-list target_type routing summary; 상세 자격의 AND 항목이 아님"
expected_product_state: "자동 적격 아님; 상세 원문 조건과 대안 경로를 함께 설명"
```

2026-09-20 15:52 KST에 K-Startup 공개 상세 페이지가 HTTP 200으로 응답했고,
“한국 바이오·헬스케어 분야 GBC 입주기업, 독일·유럽 시장 진출을 희망하는 한국
바이오·헬스케어 기업” 문구를 포함했다. 쉼표로 연결된 두 참가 경로이므로 현재 저장 분석처럼
`분야 AND (GBC 입주 OR 유럽 진출 희망)`으로 보존하고, `GBC 입주 AND 유럽 진출 희망`으로
바꾸지 않는다.

현재 저장 criterion은 다음 셋이다.

- `industry/text_only`: 바이오·헬스케어 분야와 세부 분야 원문
- `other/text_only`: GBC 입주 또는 유럽 진출 희망이라는 대안 경로 및 GBC 정의 미확정
- `target_type/in`: 포털 요약의 연구기관·일반기업·1인 창조기업, `list_semantics=open`

포털 요약은 신청 대상의 넓은 분류이며 상세 참가 경로를 대체하거나 세 값만 허용하는 닫힌
목록이 아니다. 첨부 PDF는 9월 16일 준비 기록에서 markdown 미생성으로 남았으므로,
이 fixture는 현재 공개 HTML과 current DB source revision까지만 증명한다.

## 9. 남은 증거 공백과 후속 검증

1. 실제 사용자 분포는 측정하지 않았다. 네 가상기업의 100% 확정 0건을 전체 사용자 통계로
   사용하지 않는다.
2. Production flag 값, 실제 로그인 회사의 cursor pagination, 모바일/키보드 행동은 아직
   운영 증거가 없다.
3. W5의 생산 퍼널 기준선은 없다. `/api/web/landing-events`는 이벤트 이름을 검증해 202 receipt만
   반환하고 저장하지 않는다. anonymous teaser snapshot은 별도 free-usage event로 저장되지만
   카드 열기·조건 보기·질문 완료·답변 후 해소·원문 이동까지 잇는 퍼널은 아니다.
4. 1,508은 시점 의존 snapshot이다. UI copy나 테스트 상수로 하드코딩하지 않는다.
5. 한·독 첨부 PDF의 current text/OCR 근거는 별도 read-only 추출 또는 원문 검수가 필요하다.

후속 단계는 동일 `asOf` fixture에서 다음을 검증해야 한다.

- 전체 1,508과 표시 8을 별도 필드로 유지하고, query 실패를 0으로 표시하지 않는다.
- 위 네 가상기업에서 확정 0/후보 있음, 후보 0, discovery만 있음, 오류/로딩, 다음 페이지를 각각
  독립 회귀 사례로 둔다.
- 혼합 카드가 회사 입력과 원문 검토를 동시에 보존하고, 질문 뒤 해소 수와 잔여 수를 다시 계산한다.
- 한·독 fixture에서 세 semantic condition과 OR 관계, portal summary의 open-list 역할을 보존한다.
- W5 지표를 주장하기 전에 실제 persistence와 가명 결속을 구현하고, 자동/가상기업 이벤트를
  운영 지표에서 제외한다.
