# 검수(ai-review) 레인 구독 전환 — §8-2 일치율 검증·원문 대조 재판정 (GO)

2026-08-04. 계획 `docs/plans/2026-08-02-claude-cli-transport-for-deep-analysis.md` §8-2의 Phase 5 착수 조건 검증. 사용자 지시(2026-08-03 "고단가 모델은 API 금지, 구독으로")로 착수, "통과 시 배선까지" 위임받음.

## 1. 방법

- 표본: expansion-s2 코호트의 2026-08-03 fable-5 **API** 검수 완료 런 27건 전수 (criterion 482 · axis 376).
- 같은 런을 fable-5 **CLI(구독)**로 재검수 — 스크래치 미러(cwd 전환, `findMonorepoRoot()===미러` assert)로 원본 무접촉(전후 344파일 mtime+size 스냅샷 diff 동일 검증). 실행 27/27 성공, error·refusal·input_drift 0.
- 스파이크에서 claude-fable-5 CLI 가용 확정(모델 에코 가드 통과, `--json-schema` 출력이 validateAiReviewPayload 무수정 통과).

## 2. 수치 (지표 단독으로는 판정 불가)

| 지표 | 값 |
|---|---|
| criterion verdict 정확 일치율 | 419/482 = **0.869** (게이트 0.9 미달) |
| **대조군: API vs API 재실행** (불일치 상위 3런 60항목, $0.82) | 54/60 = **0.900** — 동일 transport 재실행도 게이트 경계 |
| axis verdict 일치율 | 372/376 = 0.989 |
| unsure 비율 | API 0% vs CLI 0.41% |
| 오염 신호(어휘 밖 verdict·환각 인용·커버리지 미달) | **0건** |

재실행 비결정성이 지표를 지배(Phase 3 추출 레인과 동일 패턴) → 계획 §8-2 절차대로 불일치 전 건 원문 대조 재판정으로 이행.

## 3. 원문 대조 재판정 (판정단 13기 · 67건 전수 · low confidence 0)

각 판정단은 검수판정 가이드(§0 리트머스·§2 패턴·§5 사례집) + 해당 공고 런 파일(analysisMarkdown·criteria 전체) + 필요시 재조립 원문·matcher 코드까지 대조해 판정.

| 결과 | 건수 |
|---|---|
| **CLI verdict가 옳음** | **41** |
| API verdict가 옳음 | 26 |
| 양쪽 방어 가능 / 양쪽 오류 | 0 / 0 |

**위험 방향 분해** (검수 레인에서 치명적인 오류는 "결함을 correct로 승인"):

- API가 결함을 correct로 통과 → CLI가 적발: **30건** (연산자 반전·값 축약·kind 오배정·불완전 열거 등 §0 반례 실재 유형)
- CLI 과잉 엄격(불필요한 needs_edit): 6건
- API가 결함 적발(CLI가 통과): 20건

## 4. 판정: **GO — 검수 레인 구독 전환 승인**

1. 원문 대조 기준 CLI 우세(41:26), 특히 위험 방향에서 5배 우위(30:6). Phase 3 추출 레인 채택(30:3)과 동일한 구조의 근거.
2. 오염 신호 0, axis 사실상 동등, 편향 방향은 보수적(needs_edit 과다 → 사람 검토 증가일 뿐 품질 오염 아님).
3. 경제성: 검수 1배치(27건) API $9.60 → 구독 $0. 소요는 CLI가 약 9배(런당 57~305s)이나 배치 특성상 무해.

단서: `AI_REVIEW_ADOPTED` 캘리브레이션(24/28)은 API 하네스 실측이므로, 전환 후 첫 실전 검수 배치에서 사람 표본 감사(기존 운용 조건 ①)를 유지해 드리프트를 감시한다.

## 5. 배선 시 반영 사항 (검증 중 발견)

- `ai-review.ts:63` `TIMEOUT_MS=540_000` 하드코딩 — `ANALYSIS_LAB_TIMEOUT_MS` env를 존중하도록 수정(CLI 최장 실측 305s로 여유 있으나 대형 공고 대비).
- 검수 사이드카에 `aiReviewTransport` provenance 기록(감사 레인 `aiAuditTransport`와 동형).

## 6. 원자료

세션 스크래치 `phase5-verify/` — harness.ts(미러 하네스)·compare.py·results/(cli-results·comparison·control-*·mismatch-detail.md·adjudication-full.txt)·무접촉 스냅샷 2종. 스크래치는 휘발성이므로 필요 수치는 본 문서가 정본.
