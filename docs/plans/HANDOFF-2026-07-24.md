# HANDOFF 2026-07-24 — 딥분석 HWP 블라인드 결함 수정 + 재분석 준비

## 목표

ops 검수(`https://ops.changupnote.com/review/0684c2ad-...`)에서 리뷰어가 "이 공고, HWP 안 읽고 웹 공고만 읽은 것 아닌가?"라고 피드백 → **사실로 확인됨**. 원인 2중 결함을 수정하고, 딥분석이 안 된(또는 HWP 누락으로 부실한) 공고를 재분석할 준비를 한다.

## 배경 (검증된 사실)

- 해당 검수 건 = kstartup **178352** "해양·수산 [2026 국민 아이디어 제안대회]". LLM 입력(2,646자)에 웹 payload + 포스터.txt만 들어가고 **참가신청서.hwp는 변환 실패로 통누락, 고지도 없었음**.
- **결함 1**: HWP→markdown 변환은 인제스트 시점에 pyhwp(`hwp5html`)를 요구하는데 Vercel엔 없음 → 전부 failed. 전체 hwp/hwpx 첨부 2,186건 중 변환 완료 416건(19%)뿐 (미시도 1,520 / skipped 230 / failed 20).
- **결함 2**: `input.ts`가 markdown 없는 첨부를 **조용히 필터로 제거** → 모델이 `input_missing` 대신 `inspected_no_condition`으로 오판, ops 메타에도 흔적 없음.
- **검수 배분 21개 공고 중 HWP 통누락 4건**: kstartup 178329(3건), 178320(1), 178352(1), bizinfo PBLN_000000000121478(1 — 유일 첨부라 사실상 웹만 분석). 이 4건은 감사 판정 신뢰도에 주석 필요.
- **딥분석 미완 공고 실측** (2026-07-24, 프로덕션 DB): 모집중(open, KST 오늘 포함) 공고 **721건** 중 로컬 분석 런 보유 **10건**, **미분석 711건**. 기간 미상(apply_end null) 958건은 정책상 예외 큐. 분석 완료 목록(35건)은 `spike-out/analysis-lab/<source>__<sourceId>/` 디렉터리명 참조.

## 최종 완료 결과 (2026-07-24 23:05 KST)

- **배포 계정/프로젝트 확인**: 활성 계정 `sw@noten.im`, 프로젝트 `changupnote-com`, 리전 `asia-northeast3`. Cloud Run 서비스 creator/lastModifier도 `sw@noten.im`.
- **Cloud Run 배포 완료**:
  - 첫 배포 스모크에서 `pyhwp 0.1b15`가 선언하지 않은 런타임 의존성 `six` 누락을 발견했다. `apps/conversion/Dockerfile`에 `pyhwp six`를 함께 설치하도록 수정했다.
  - 최종 Cloud Build `b6734461-8331-44b4-8aca-5d5a4b9a623e` 성공.
  - 이미지 `asia-northeast3-docker.pkg.dev/changupnote-com/cunote/conversion:d87f5eb-six`, digest `sha256:2327224e543b0682cfe0c82dafde350a730a989154ad34f16af78fcaef71c795`.
  - Cloud Run 리비전 `cunote-conversion-00007-d95`가 100% 트래픽을 서비스한다. 기존 R2/공유 시크릿 연결과 300초 timeout을 보존했다.
  - 운영 URL: `https://cunote-conversion-644631753751.asia-northeast3.run.app`.
- **운영 스모크 통과**:
  - 무인증 `POST /v1/hwp-markdown` → 401.
  - 정상 인증 + 잘못된 body → 400.
  - kstartup 178352의 실제 HWP → `PATH hwp5html=/usr/local/bin/hwp5html`, markdown 2,212자, 예상 한국어 본문 확인.
- **감사 4공고 운영 복구 완료**:
  - kstartup 178320: HWP 1개 변환 완료.
  - kstartup 178329: HWP 3개 변환 완료.
  - kstartup 178352: 기존 failed HWP 1개 원격 백필 완료.
  - bizinfo PBLN_000000000121478: HWP 1개 변환 완료.
  - 총 HWP 6개 모두 `conversion_status=converted`, 원본/markdown R2 key 존재, `conversion_error=null`. 네 공고 모두 재백필 dry-run 후보 0.
- **감사 4공고 재분석 완료** (`claude-opus-4-8`, `lab-deep-v3`, 총 비용 `$0.7489`):
  - 178320: `run-2026-07-24T140254.003Z-024f68`, HWP 입력 5,336자, error 없음.
  - 178329: `run-2026-07-24T140254.003Z-15685c`, HWP 3개 입력 9,497자, error 없음.
  - 178352: `run-2026-07-24T140420.049Z-c99b93`, HWP 입력 2,212자, error 없음.
  - PBLN_000000000121478: `run-2026-07-24T140420.049Z-fa2ca7`, HWP 입력 656자, error 없음.
- 기존 `audit_dispatch_notices.input_text`는 검수 시점 불변 스냅샷이므로 덮어쓰지 않았다. 새 런은 재검수 후보로 보존됐고, 주간 배분은 AI review/audit sidecar와 별도 ops 배치 정책을 거쳐야 하므로 이번 복구 실행에서 임의 재배분하지 않았다.

## 후속 운영 반영 (2026-07-25)

- Vercel CLI 인증 정본을 루트 `.env.vercel.local`로 `AGENTS.md`에 고정했다. 토큰은
  `noten-dev`/`NOTEN(noten)` 팀의 `changupnote` 프로젝트로 확인했다.
- 재분석 4건에 채택 모델 AI 검수와 독립 AI 감사를 실행했다. 감사 14항목 중 7항목은
  일치해 자동 확정됐고 7항목은 불일치해 사람 판정 대상으로 남겼다.
- HWP 포함 원문 스냅샷으로 별도 ops 복구 배치 `2026-W30-hwp-repair-1`
  (`001a769c-9d63-4624-afaa-cc6af1cbcaec`)를 생성했다. 4공고·17논리 항목,
  reviewer assignment 18행(독립 중복 2행)이다.
- AI 검수·감사·dispatch CLI에 `--grant-ids`를 추가하고, 복구 배치가 기존 주간 배치와
  충돌하지 않도록 `--batch-key`를 추가했다.
- 웹 프로덕션을 `changupnote.com`에 배포했다. 최초 시도에서 동적 파일 추적 때문에
  `ingest-kstartup` 함수가 294MB로 250MB 제한을 넘은 것을 확인했고, Vercel/Next 추적 제외
  규칙으로 57.1MB까지 줄인 뒤 배포에 성공했다.
- 신규 수집 경로의 남은 하드코딩도 수정했다. K-Startup·BizInfo 크론은
  `CONVERSION_SERVER_URL`과 `CONVERSION_SHARED_SECRET`이 모두 있을 때 원격 HWP markdown
  변환을 켠다. BizInfo는 생성된 첨부 markdown을 같은 Anthropic criteria 추출 입력에
  포함한다. K-Startup은 변환·보관까지 자동화되지만 criteria 승격은 기존 검수 게이트를
  유지한다.

## 이어서 진행 결과 (2026-07-24 22:30 KST)

- **백필 CLI 완료**: `backfill-attachment-markdown.ts`
  - dry-run 기본, `--write`, `--limit`, `--source`, `--source-id`, 성공/실패/동시 변환 skip 집계.
  - HWP 정규식은 SQL 파라미터로 바인딩하고, UPDATE 시 `markdown_storage_key IS NULL`을 다시 확인해 동시 실행 결과를 덮어쓰지 않는다.
  - `objectKey`/`renderArchivedMarkdown`/`stripExtension`은 `grantAttachmentArchive.ts` 정본을 export해 재사용.
- **원격 클라이언트·폴백 테스트 완료**: `remoteHwpMarkdown.test.ts` 7개 시나리오
  - env gating, 200, 401, 422, timeout, 로컬 불가→원격 converted, 원격 미주입→기존 failed.
- **감사 대상 복구 경로 보강**:
  - 명시 `--sourceIds`를 준 K-Startup 백필은 활성 목록에서 먼저 탈락시키지 않고 마감 공고도 직접 로드한다.
  - `grant_raw.attachments=[]`로 덮인 공고는 `grant_attachment_archives`의 원본 URL/상태를 명시 복구 실행에서만 되살린다.
  - dry-run 결과: 178320은 3개(PDF/TXT/HWP), 178329는 4개(HWP 3/TXT 1) 아카이브 후보. bizinfo PBLN_000000000121478은 HWP 1개 후보.
- **현재 DB 분모 정정**:
  - HWP/HWPX archive 행 2,679건 = 원본 R2 보관 852 / 미보관 1,827.
  - markdown 변환 완료 528 / 이번 원격 백필 가능(`storage_key` 있음 + markdown 없음) **324건**.
  - 따라서 아래의 과거 예상치 `~1,770건`은 이번 CLI 대상 수가 아니다. 1,827건은 먼저 원본 아카이브가 필요하다.
- **로컬 검증 완료**:
  - conversion endpoint 11/11, input 5/5, 기존 attachment 23/23, 신규 remote/fallback 7/7.
  - web/conversion typecheck, `git diff --check` 통과.
- **배포 블로커(해결됨)**:
  - 현재 Cloud Run의 인증된 `POST /v1/hwp-markdown`은 404 → 새 엔드포인트 미배포 확인.
  - 로컬 `gcloud` 활성 계정 `sw@ba-ton.kr`, 프로젝트 `changupnote-com`이지만 refresh token 만료. 비대화형 실행에서 재인증 불가.
  - 이후 사용자가 `sw@noten.im`으로 인증했고, 위 최종 완료 결과대로 배포·운영 백필·재분석까지 완료했다.

## 완료된 것 (이 문서와 같은 로컬 커밋에 포함)

1. **변환 서버 동기 엔드포인트** `POST /v1/hwp-markdown` — `apps/conversion/src/server.ts`
   - 계약: 요청 `{filename(.hwp/.hwpx), sourceObjectUrl(presigned GET), sha256?}` → 200 `{markdown, converter}`. 에러 400/401/409(sha256_mismatch)/413(30MB 가드)/422(conversion_failed)/502(source_fetch_failed)/503(converter_unavailable). `bootstrapFromEnv`가 기존 `hwpToMarkdown`(pyhwp) 어댑터를 주입.
   - 검증: `pnpm --filter @cunote/conversion build && node apps/conversion/scripts/hwp-markdown-endpoint-test.mjs` → **11항목 전부 통과**. typecheck 통과.
2. **input.ts 조용한 제외 제거** — `apps/web/src/lib/server/analysis-lab/input.ts`
   - markdown 없는 첨부를 `unavailable("변환 안 됨")`로 수집 → blocks 메타 `첨부 미투입(변환 안 됨): X` + 모델 고지 `[입력 한계 고지] ... input_missing으로 보고하라`에 등장. 테스트용 storage 주입(`LabAttachmentTextStorage`, `assembleLabInput` 2번째 인자) 추가.
   - 검증: `TSX_TSCONFIG_PATH=apps/web/tsconfig.json node --import tsx apps/web/src/lib/server/analysis-lab/input.test.ts` → **5 시나리오 통과** (신규 테스트 파일).
3. **인제스트 원격 폴백** — `apps/web/src/lib/server/ingestion/remoteHwpMarkdown.ts`(신규 클라이언트) + `grantAttachmentArchive.ts`의 `convertHwpMarkdownWithFallback`
   - 로컬 hwp5html 가용성 캐시(`detectHwpMarkdownConverter`) → 가용 시 로컬, 불가·실패 시 원격(방금 업로드한 R2 객체 `presignGetUrl` + sha256 동봉). 원격 클라이언트는 `CONVERSION_SERVER_URL`+`CONVERSION_SHARED_SECRET` 둘 다 있어야 생성(없으면 null=기존 동작). options에 `remoteHwpMarkdown` 필드 — **undefined면 env 자동 해석이라 기존 호출부 무수정 배선 완료**, 테스트는 null/fake 주입.
   - 검증: `pnpm --filter @cunote/web typecheck` 통과, 기존 `grantAttachmentArchive.magic-bytes.test.ts` **23 passed**.

## 남은 작업 (순서대로)

1. **[완료] 백필 CLI 작성** — `apps/web/src/lib/server/ingestion/backfill-attachment-markdown.ts` (신규)
   - 대상: `grant_attachment_archives`에서 `markdown_storage_key IS NULL AND storage_key IS NOT NULL AND filename ~* '\.(hwp|hwpx)$'`
   - 동작: `presignGetUrl` → `createRemoteHwpMarkdownFromEnv().convert()` → 기존 규약대로 R2 업로드 → 행 UPDATE(conversion_status='converted' 등). **`renderArchivedMarkdown`/`objectKey`/`stripExtension`이 grantAttachmentArchive.ts에 비export 상태(544/560/617행) — export 추가해 재사용, 복제 금지.**
   - 옵션: dry-run 기본, `--write`, `--limit N`(기본 20), `--source`, `--source-id`. 멱등, 실패 건 로그 후 계속. 스타일은 `backfill-bizinfo-attachments.ts` 참조.
2. **[완료] 원격 클라이언트·폴백 단위 테스트** — fake fetch로 성공/401/422/타임아웃 + 폴백(로컬 불가 시 remote 주입→converted / 미주입→기존 failed). `setCachedLocalHwpConverterAvailableForTest()`로 테스트 간 캐시 격리.
3. **[완료] package.json 스크립트 등재**: conversion `test:hwp-markdown`, web `test:analysis-input`/`test:hwp-markdown`.
4. **[완료] 커밋** — 관련 코드·테스트·이 핸드오프만 명시 스테이징했다. `docs/plans/2026-07-24-deep-analysis-production-rollout.md`는 **다른 세션 소유라 건드리지 않았다**.
5. **[완료] Cloud Run 재배포** — 새 엔드포인트 반영, 401/400 인증 경계와 실 HWP 변환 스모크 통과. 최종 리비전·이미지는 위 완료 결과 참조.
6. **[완료] 감사 4공고 백필 실행** — env는 루트 `.env.local`에 있음(CONVERSION_SERVER_URL/SECRET).
   ```bash
   pnpm exec tsx --tsconfig apps/web/tsconfig.json apps/web/src/lib/server/ingestion/backfill-attachment-markdown.ts --source kstartup --source-id 178352          # dry-run
   # ... --write 로 4건(178329·178320·178352·PBLN_000000000121478) 처리 완료
   ```
7. **[감사 4건 완료 / 전체 확대는 별도 범위] 재분석** — 감사 4공고는 markdown 확보 후 새 런 생성을 완료했고 HWP 입력 블록을 확인했다. 기존 검수 스냅샷은 불변 보존한다. 미분석 open 711건과 전체 324건 HWP markdown 백필 확대는 비용·운영 범위를 사용자와 별도 합의한 뒤 `pnpm lab:batch`로 실행한다(기간 정책·코호트 규약은 `cohort.ts` 상단 주석).

## 검증 체크리스트

- [x] conversion build + `node apps/conversion/scripts/hwp-markdown-endpoint-test.mjs` 11항목 통과 유지
- [x] `input.test.ts` 5 시나리오 + `grantAttachmentArchive.magic-bytes.test.ts` 23건 통과 유지
- [x] 원격 클라이언트·폴백 테스트 7건 + K-Startup 복구 planner 테스트 통과
- [x] web/conversion typecheck 통과
- [x] 백필 dry-run 대상 324건 확인(원본 R2 미보관 1,827건은 별도 아카이브 선행)
- [x] Cloud Run 재배포 후 실 HWP 1건 원격 변환 성공(markdown 2,212자와 한국어 본문 확인)
- [x] 감사 4공고 재분석 런에서 HWP 6개 블록이 입력에 포함되고 4런 모두 error 없음

## 주의 / 함정

- **서브에이전트 3개가 Fable 5 사용량 한도로 두 번 연속 사망**한 이력 있음 — 대량 위임 전 잔여 한도 확인, 큰 작업은 직접 수행이 안전.
- psql 접속: `.env`의 `DATABASE_URL`에 psql이 못 읽는 쿼리 파라미터가 붙어 있음 → `sed 's/?.*$//'`로 제거 후 사용 (읽기 전용 준수).
- SQL 조인 시 `grant_attachment_archives.source`는 enum이라 `a.source::text = n.source` 캐스트 필요.
- `audit_dispatch_notices.input_text`는 발송 시점 스냅샷 — 재분석해도 기존 검수 화면 원문은 안 바뀜(새 런 발행/재배분 필요 여부는 ops 정책 판단).
- 신규 런부터 `[입력 한계 고지]`가 붙어 inputSha256이 달라짐 — 의도된 변화.
- 딥분석 재실행은 실 LLM 비용. Opus 4.8·Fable 5에 temperature 지정 금지(기존 규칙).

**새 세션 프롬프트**: `docs/plans/HANDOFF-2026-07-24.md 읽고 남은 작업 이어서 진행해줘`
