# HANDOFF — 공모 딥분석 확대 실험: 30건 분석·AI 검수 완료, 사람 감사만 잔여 (2026-07-23)

> **2026-07-25 후속:** 운영 승격 제어면은
> `docs/plans/2026-07-24-deep-analysis-production-rollout.md`와
> `docs/runbooks/deep-analysis-promotion.md` 기준으로 구현됐다. 기존
> `lab:promote --write --confirm-go` 실발행은 폐기됐고, immutable manifest → aggregate GO
> → shadow PASS → dry-run PASS → 분리 승인 → 카나리만 허용한다. W30은 현재
> `decided 42 / pending 12 / conflict 12 / collected 0`이므로 실제 criteria 승격은 아직
> 차단 상태다.
>
> 🟢 **상태(2026-07-23 오후 갱신)**: AI 블라인드 감사(`lab:ai-audit`, §10)가 118항목 중
> **70건 자동 확정** — 사람 감사는 **불일치 48항목(19공고)만** 남음(기존 1.5~2시간 →
> ~30~50분). 게이트 표본에 8공고 선편입(wrong 0%). 같은 날 지시로 ①모집기간 정책
> (오늘 포함만 분석 대상, 기간 미상은 예외 큐+기간 입력 UI) ②아카이브 기능 일시 숨김도
> 구현 완료. **아래 "2026-07-23 오후 추가 작업" 절 참조. 미커밋 상태.**
> 섀도 측정 결과가 **메인 매칭 교체 여부를 결정하는 증거**라는 골격은 불변.

## 목표

공고문 전문(구조화 필드+첨부 markdown)을 Opus 4.8로 딥분석해 22축 자격요건을 기계판정
가능한 criteria로 채우는 파이프라인을 층화 30건(→100건)으로 확증하고, 긍정이면 메인 매칭의
**공고 측 criteria 생산 파이프라인**을 이것으로 교체한다(매칭 엔진·UI는 무변경).

- 실행 계획 단일 원천: `docs/plans/2026-07-21-analysis-lab-expansion-experiment.md`
  (§3 판정 규칙 동결 `1adfeec` · §9 검수 자동화 프로토콜 개정 + 캘리브레이션 결과)
- 섀도 측정 계획: `docs/plans/2026-07-22-analysis-lab-shadow-harness.md` (§8 해석 주의 필독)
- 과정 로그: `docs/plans/HANDOFF-2026-07-22.md` (이 문서로 대체됨 — 커밋 해시·경위 참조용)

## 완료된 것 (전부 main 커밋·push. spike-out 산출물은 gitignore — 디스크 단본)

| 무엇 | 어디에 | 검증 증거 |
|---|---|---|
| 동결 선언(프롬프트 lab-deep-v2·검수 가이드·판정 규칙) | 계획 §3, 커밋 `1adfeec`·712e0fa | 동결 해시가 계획에 기입됨 |
| 층화 30건 선정(seed 42, expansion-s1) | `spike-out/analysis-lab/cohort.json` v2 | 쿼터 통합공고 4/4·A≥3 12/5 충족, 파일럿 3 보존 |
| **30건 분석 완주** — 파일럿 3(기존) + 확대 27 신규, $5.41 | `spike-out/analysis-lab/<source>__<id>/run-*.json` | 27/27 성공·실패 0, 공고당 $0.11~0.45 |
| **검수 자동화(§9)** — AI 검수기 + 캘리브레이션 채택 | `pnpm lab:ai-review`, 커밋 55a22cd·88f14b2·d0c41f7 | claude-fable-5가 사전 등록 3기준 충족(criterion 24/28·correct→wrong 오검출 0·빈 축 45/46). 프롬프트 개정 카드(1회) 소진 — 재개정은 STOP/ITERATE 후에만 |
| **확대 27건 AI 검수 완료**, $7.36 | `<runId>.ai-review.claude-fable-5.json` ×27 | 실패·드리프트 0. ARPA-H 초회 bio-refusal은 재실행에서 미재현·정상 완료(사람 전수 불필요) |
| **감사 루프** — UI 감사 탭 → 게이트 표본 자동 편입 | 커밋 2a396f1 (AuditSheet·audit-store·audited-reviews·aggregate/shadow 통합) | typecheck 0·lab 테스트 3종 통과·드리프트 신규 0. 감사=사람 판정, 미감사=AI 판정 병합(§9 표본 설계) |
| **감사 파일 27건·118항목 동결** | `<runId>.audit.claude-fable-5.json` ×27 | 대상은 풀 결정론(시드 42)으로 고정 — 이후 어떤 작업에도 불변 |
| 섀도 하네스 | `pnpm lab:shadow`, 커밋 eb0b934 | 파일럿 3건 실스모크(27/27 변환, DB 쓰기 0) |
| 집계 순환 차단 | 커밋 5c443ca | 기본 집계가 파일럿 층을 게이트 표본에서 제외(`--all`=민감도 포함치) |
| 운영 이탈 기록(분석 선실행) | 커밋 fa2e911 | 사유·매몰 상한 ~$12·게이트 판정 불변을 계획에 명시 |

실험 누적 비용 ~$17 (분석 $5.41 + AI 검수 $7.36 + 캘리브레이션 $4.22).

## 2026-07-23 오후 추가 작업 (사용자 지시 3건 — 전부 구현·검증 완료, 미커밋)

1. **AI 블라인드 감사 자동화 (§10 프로토콜 개정 2)** — 원칙 변경 사용자 승인:
   - 새 러너 `pnpm lab:ai-audit` (`ai-audit.ts`·`ai-audit-cli.ts`): 검수 판정을 가린 채
     제3 모델이 감사 항목을 독립 재판정 → 정확 일치만 자동 확정, 불일치는 사람 큐 잔류.
   - 순환성 가드 3중: 감사 모델 ≠ 추출(Opus 4.8) ≠ 검수(fable-5), 블라인드 강제.
     기본 claude-sonnet-5, env `ANALYSIS_LAB_AUDIT_MODEL`. humanVerdict·review.json 불가침.
   - **실행 결과**: 26공고 118항목, 일치 70(자동 확정) · 불일치 48 · unsure 0 · 실패 0 ·
     드리프트 0, 비용 $1.08. 게이트 표본 8공고 선편입(`lab:aggregate` 방법론 병기 확인).
   - `isLabAuditComplete` 신규 규칙: humanVerdict 또는 (aiAuditVerdict 정확 일치 ∧ 非unsure).
   - 불일치 공고당 ~1.8건 — §10 신뢰 조항 기준 초과이나, 이는 감사기(sonnet-5, 캘리브레이션
     13/28로 이탈 경향 기지)의 엄격성 포함치. **사람의 불일치 48건 판정 결과가 신뢰 재평가의
     실데이터** — 사람이 fable-5 쪽을 대부분 지지하면 검수 신뢰 유지로 해석.
2. **모집기간 정책 (§10)**: 분석 대상 = `applyEnd ≥ 오늘(KST 일 단위) AND (applyStart 없음
   OR 시작됨)`. 코호트 후보 쿼리·배치 가드에 적용(동결 코호트 30건 로드는 무변경).
   기간 미상(`applyEnd NULL`)은 예외 큐 — NoticeCard "기간 미상" 배지 + 분석 차단 + 날짜
   입력 폼(`PATCH /api/dev/analysis-lab/notice-period`, production 404). AI 기간 추정 금지.
3. **아카이브 일시 숨김**: 사이드바·브레드크럼 메뉴 제거, `/api/web/archive` 3종 404
   (`ARCHIVE_HIDDEN` 플래그 — 복원 시 플래그만 제거). `/archive` 페이지는 기존부터
   /dashboard 리다이렉트.

검증: web typecheck 통과 · lab 테스트 5종(ai-audit/audit/ai-review/shadow/notice-period)
통과 · dev 범위 드리프트 스캔 0 · `lab:ai-audit --dry-run`/실행/`lab:aggregate` 실측 정합.

## 남은 작업 (순서대로)

1. **[사용자] UI 감사 — AI 감사 불일치 48항목·19공고만** (~30~50분, 기존 118항목에서 축소.
   부분 저장 가능 — LLM·외부 인증 불필요, dev 서버는 사용자 기동):
   ```bash
   pnpm dev:web   # → http://127.0.0.1:4010/dev/analysis-lab
   ```
   감사 미완 배지 공고 → 런 상세 **"감사" 탭** → **"AI 감사 불일치 — 사람 판정 필요"**
   표시 항목만 판정(일치 항목은 "자동 확정" 배지, 덮어쓰기 가능). AI 감사(2차 판정)의
   사유가 항목에 병기되어 참고 가능. 뒤집힘(사람이 fable-5 검수 판정을 부정)이 공고당
   >1건 누적되면 §9 신뢰 재평가.
2. **배치 1 감사 완료 시 조기중단 게이트 점검** (LLM·DB 불필요):
   ```bash
   pnpm lab:aggregate
   ```
   wrong률 >10% 또는 공고당 누락 >1건이면 중단·STOP/ITERATE 절차(계획 §3). 헤더에
   방법론(사람 N + AI+감사 M)·캘리브레이션 일치(24/28·45/46)가 병기된다.
3. **[사용자·비차단] 식품 공고 export_performance 1축 판정** — 검수 시트(축 목록 맨 끝 행),
   기존 판정 26건이 채워진 채 열리는지 먼저 확인. 재집계는 `pnpm lab:aggregate -- --all`
   (파일럿은 기본 집계에서 제외되므로). 어느 판정이든 게이트 결론 불변.
4. **감사 118/118 완주 후 — 세션에 요청하면 대행**:
   ```bash
   pnpm lab:aggregate          # 30건 게이트 6종 판정
   pnpm lab:shadow             # 매칭 임팩트 섀도 측정 (DB read-only·LLM 0, 루트 .env DATABASE_URL)
   ```
   → STOP/ITERATE/GO 판정 문서(docs/research/, 한글 파일명) + 섀도 결과 문서화.
   섀도 해석 주의: **빈 프로필 회사에서 후(after) 지표 0 붕괴는 프로덕션 실동작** —
   실사용자·팝빌 캐시 보유 사업자 기준으로 해석(shadow-harness 계획 §8). `--bizNo=CSV`로
   팝빌 캐시 사업자 추가 가능. 부정이면 병목=기업 측 소싱으로 판정(사전 등록 분기).
5. **GO + 섀도 긍정 시 — 메인 매칭 교체 착수**: ①golden_set 승격 어댑터(shadow-convert +
   기존 발행 경로 `publish-reviewed-grant-annotations.ts` 재사용, 1~2세션) ②운영화 결정
   (전 open 공고 ~1,500건 초기 분석 ~$300~600, Batches API 50% 절감 옵션 — calibration 기등재)
6. **병행 트랙**: 변환 서버 재배포 → kstartup 첨부 일괄 변환(2단계 +70건 층 재고) /
   Sonnet 5 A/B(인트로 $2/$10, **2026-08-31 종료**) / UI 시각 검수(감사 탭·배지 신규 포함)

## 검증 체크리스트

- [x] AI 블라인드 감사 70/118 자동 확정 (`lab:ai-audit`, 2026-07-23 오후, $1.08)
- [ ] [사용자] 감사 — AI 감사 불일치 48항목(19공고) 판정
- [ ] `pnpm lab:aggregate` 게이트 6종 판정 출력(방법론 병기) 확보
- [ ] STOP/ITERATE/GO 판정 문서가 docs/research/에 커밋
- [ ] `pnpm lab:shadow` 결과(지표 4종 전/후)가 판정 문서에 병기·커밋
- [ ] (GO 시) 승격 어댑터·운영화 계획 착수
- [ ] [사용자·비차단] export_performance 판정 → `--all` 재집계

## 주의 / 함정

- **검수·감사·AI 검수 파일은 디스크 단본**(spike-out, gitignore) — 주 1회 백업 권장.
- **감사 시트가 빈 채 열리면 로드 실패** — 저장은 서버가 차단하지만 새로고침으로 재시도.
  감사 저장은 판정 병합 방식이라 빈 덮어쓰기 사고는 구조적으로 불가.
- **코호트 재선정(refresh) 버튼 금지**(운영 가드). 확대 중 프롬프트·가이드·판정 규칙
  동결 — 수정은 STOP/ITERATE 후에만(§3·§9).
- fable-5 refusal은 확률적 — 재현 시 해당 런만 재실행하면 잔여분에 자연 포함된다.
- aggregate 기본=파일럿 제외(게이트 표본), `--all`=포함(민감도·파일럿 확인용).
  **섀도는 파일럿 포함이 정상**(30건 검수 확정분 정의).
- Opus 4.8·Fable 5에 temperature/top_p/thinking 전송 시 400 — extractor·ai-review 모두
  의도적 미전송. 모델 오버라이드: env `ANALYSIS_LAB_MODEL`(추출)·`ANALYSIS_LAB_REVIEW_MODEL`(판정,
  추출 모델과 동일 지정 시 하드 가드로 거부).
- dev 서버는 사용자 소유 — 세션이 백그라운드로 띄우지 말 것.
- batch·aggregate·ai-review·shadow 전부 명시적 exit 구현(미종료 전례는 verify:service-data 계열).

## 백그라운드 작업

없음 — 전부 완료. 워킹트리 클린, 미push 커밋 0.
