# 매칭 공급 확대 — 실행과 평가

기준일: 2026-09-25 KST. 담당: 현재 세션 단일 실행자.

## 목표와 범위

사용자 승인한 순서: 현행 누락 집계 → 업종·조건 보완 → 원문 복구·분석·발행 → 독립 평가·실제 계정 인수 → 다음 수집 반복 검증.

현재 모집 중 고유 공고를 조사하고, 수집원과 다음 작업 상태를 층으로 나눈 100건을 평가 대상으로 고정한다. 합성 초기기업 10개는 평가 전용이며 실제 고객 정보에 쓰지 않는다. 신청서 자동작성은 매칭의 선행 조건으로 삼지 않는다.

새 live 모델 cohort는 입력 복구와 코드 검증 후 exact manifest로 제시한다. AGENTS.md의 manifest 승인 경계는 유지한다. 과거 BTS 실행 권한을 다른 공고의 launch grant로 재사용하지 않는다. 기존 raw·원문·분석 receipt는 보존한다.

## 1. 현재 상태 — 읽기 전용 실측

2026-09-25 10:22 KST 조회. 운영 후보 1,514건과 현재 모집 중 541건을 구별했다. 541건은 기업마당 311·K-Startup 230건이다.

| 공급 판정 | 공고 수 |
| --- | ---: |
| 조건 검수 | 275 |
| 신규 조건 분석 | 196 |
| 원문 변경 검토 | 41 |
| 질문 준비 | 25 |
| 재사용 준비 | 4 |

보관 raw가 없는 공고는 0건, DB criteria가 존재하는 공고는 345건이다. criteria 존재가 검수·서비스 사용 가능성을 뜻하지 않는다.

표본은 source + nextWork 그룹을 고정하고 그룹 안에서 ID의 SHA-256 순서로 선택했다. 예측·회사 업종으로 선택하지 않았다. 기업마당 54·K-Startup 46건, 다음 작업별 29/29/20/18/4건이다. 진단용 층화 표본이므로 비율을 모집단 유병률처럼 일반화하지 않는다. development 80·holdout 20 구분은 후속 독립 평가를 위한 것이며 현재 검수된 정답은 아니다.

## 2. 발견한 공통 결함과 수정

### 원문 목록과 공급 판정

기존 Lab 입력은 archive 행만 읽었다. 기업마당 `printFileNm/printFlpthNm`의 별도 공고문이 raw.attachments에도 archive에도 없으면 입력 누락 경고 자체에서 빠질 수 있었다. 원래 입력으로 51/100건이 첨부 unavailable/truncated였고, 선언 목록까지 포함한 입력에서는 70/100건이었다. 차이 19건은 새 수집 실패가 아니라 기존 누락의 추가 발견이다.

- 기업마당 원문 파일과 K-Startup 상세 첨부 선언을 공통으로 합친다.
- 선언됐지만 보관되지 않은 첨부는 Lab 입력·첨부 manifest에 unavailable로 남긴다.
- 운영 입력 준비와 source revision 계산도 같은 선언 목록을 사용한다.
- 공급 판정의 ‘첨부 행 하나 이상이면 complete’를 제거하고 선언별 원본 SHA·storage key·URL을 확인한다.
- 같은 이름의 파일이라도 URL이 바뀌면 이전 보관본으로 채우지 않는다.
- 원본 보관 완료와 텍스트/이미지 의미 커버리지는 별개다. 입력 준비와 독립 원문 검수가 후자를 확인한다.

### 업종 의미

기존 ICT 정책 포함관계에 저장소의 KSIC 대분류·중분류 계층을 연결했다. 음식·의류·가구 제조, 도소매, 창고, 연구개발, 영상 등에서 정확한 업종 라벨 또는 회사가 이미 보유한 KSIC로 상위 업종을 판정한다.

- 하위→상위만 허용하고 부모·형제 업종으로 세부 활동을 추정하지 않는다.
- ‘정보통신업’ 통계 분류와 ‘ICT’ 정책 개념을 동일시하지 않는다.
- 6자리 세무 업종코드나 대분류 접두와 중분류가 모순인 코드는 새 경로의 근거로 사용하지 않는다.
- AI·바이오·친환경 등 제품·기술 요건은 일반 업종에서 자동 추론하지 않는다. 원문/실제 사업 사실과 추가 확인이 필요하다.
- 매칭과 관련성 점수가 동일한 방향성 관계를 소비한다. ruleset은 v16이다.

분류 관계는 [기존 KSIC 모듈](../../packages/core/src/industry/ksic.ts)을 재사용한다. 개념의 동의어·포함·연관을 구별하는 설계 참고: [W3C SKOS](https://www.w3.org/TR/skos-reference/). 콘텐츠 정책범위와 산업분류가 일대일이 아님을 확인하는 공식 자료: [한국콘텐츠진흥원 연계표](https://welcon.kocca.kr/cmm/fms/CrawlingFileDown.do?atchFileId=FILE_b297e8ac-4fa5-4526-a31e-d2185eb8fd74&fileSn=3). 이 자료만으로 조건부 활동을 무조건 포함관계에 넣지 않았다.

### PDF 이미지

텍스트가 200자 이상이면 OCR을 건너뛰던 복구 경로를 수정했다. 이미지 목록을 읽고 이미지가 있거나 목록을 확인하지 못하면 시각 OCR을 요구한다. native text와 OCR은 출처를 구별해 함께 보존한다. 20페이지 상한·신뢰도 기준을 낮추지 않으며 실패는 해당 원문에 남긴다. 원본 PDF가 보관됐으면 사용자가 미리보기를 열어 pdf artifact를 만들기 전에도 복구할 수 있다.

## 3. 평가

`tools/matching-coverage.ts`가 고정 표본과 serving snapshot을 읽어 10개 최소 프로필 × 100건을 실제 matcher로 실행한다. 회사 규모·인증·제품 보유를 추정하지 않는다.

- prediction과 독립 review packet을 분리하고 정답 칸은 null로 둔다.
- review는 source input SHA·회사 프로필·평가 시각에 결속한다.
- `conditional`이어도 `needs_core_review`인 결과는 서비스 후보 회수 성공으로 세지 않는다.
- 독립 정답이 없으면 precision/recall은 null이다.
- 공급 확대 전 snapshot의 1,000쌍: 후보 2쌍, 내부 검토 945쌍, 비추천 53쌍. 이는 정확도가 아니라 현재 노출 분포다.

실행 예:

```sh
pnpm exec tsx --tsconfig apps/web/tsconfig.json tools/matching-coverage.ts \
  --snapshot=spike-out/coverage-20260925-normalized.json \
  --sample=spike-out/coverage-20260925-evaluation-sample.json \
  --output=spike-out/coverage-evaluation-new
```

새 출력 디렉터리를 사용하며 기존 영수증을 덮어쓰지 않는다. 독립 검수 결과 배열은 `--reviews=<path>`로 전달한다. source가 바뀌면 이전 검수 hash를 재사용하지 않는다.

## 4. 운영 처리와 남은 단계

원문 복구 사전 계획: 고정 표본 안에서 기업마당 34건·첨부 48개, K-Startup 27건·첨부 48개. local HWP 변환·macOS OCR을 사용하며 유료 LLM API 호출은 없다. 공고별 결과·실패를 보존하고 기존 publisher의 승격 분석 보호를 유지한다.

실행 결과·검증 및 새 모델 exact manifest는 후속 절에 기록한다. 원문 복구 성공을 분석·발행·추천 성공으로 집계하지 않는다. 검증 중인 코드가 곧 운영 배포를 뜻하지 않는다.

### 종료 기준

1. 표본 100건의 원문·현재 분석·검수·발행 상태와 최소 다음 작업이 설명된다.
2. 필요한 대상만 exact manifest로 분석하고 publishable 결과를 독립 검수·승격한다.
3. 독립 정답 평가 및 실제 글리프 계정에서 조건·질문·조회 갱신을 확인한다.
4. 이후 새 수집 이벤트를 같은 판정으로 처리하고 성공 자산의 모델 호출 0회 재사용을 확인한다.
5. 관찰되지 않은 신규 유입·처리시간 분포·산업 범위는 완료로 선언하지 않는다.

## 5. 2026-09-25 실행 결과

- exact 표본의 61개 공고 작업, 첨부 96개를 원문 복구 대상으로 처리했다. 작업 수준 실패 0이지만 첨부 실패는 기업마당 5개·K-Startup 5개다. 이 수치를 첨부 96개 성공으로 해석하지 않는다.
- 원본 PDF 49개를 추가 복구해 47개 성공·2개 보류. native+OCR 22개, 로컬 렌더 OCR 9개, native text 16개다. 보류 2개는 페이지 OCR 신뢰도 0.565/0.530으로 기준 0.600을 충족하지 못했다.
- DB/R2의 실제 복구 결과를 다시 읽은 100건 입력에서 누락·잘림 없는 공고는 30→91건이다. ZIP의 검증된 자식 파일로 내용을 확인한 경우를 포함한다. 나머지 9건에는 읽지 못한 첨부 15개가 남았다. 원문 의미를 전부 사람 검수했다는 뜻은 아니다.
- 복합 PDF 1건(홍릉벤처밸리)의 원본 SHA 확인·1페이지 렌더 대조에서 업력 7년, 주소 이전 3개월, 부담금 표를 입력 전문에서 확인했다. 전체 47개에 대한 시각 검수는 아니다.
- 이전 실행 파일의 실제 바이트·입력 SHA·첨부 SHA를 결속하는 `legacy_material`을 추가했다. 실제 입력이 바뀐 경우만 재준비하며, 입력이 같은 역사 실행은 기존 결과 검수로 남긴다. 계약 버전 변화만으로 재실행하지 않는다.
- 100건 분류: 현재 원문 변경 55건·신규 5건은 matching-only 준비 대상, 기존 결과 검수 24건·보류 16건. 보류에는 원문 누락 9건이 포함된다. 새로운 모델 호출은 0회다.
- 분류 SHA: `002beb69ea282adc5e5469a4a7b06061094b5d6081567a95d20f1230bff5046d`. 입력 구조·복구 상태 검토이며 독립 자격 심사 정답은 아니다.
- 복구 후 로컬 코드로 조회한 운영 데이터에서 검증된 분석 노출은 64→29건이다. 새 원문과 옛 분석의 결속이 달라져 발생한 보류를 포함한다. 즉시 추천 증가로 보고하지 않으며 새 분석·검수·발행 후 다시 측정한다. 글리프의 조건부 후보 1건은 유지됐다.

### 검증 증거

패키지 빌드, 웹 typecheck·production build, 딥분석 계약 집계 gate, 공고 readiness 집계 suite, 격리 PostgreSQL 공급 통합 테스트를 통과했다. 업종 의미·매칭·관련성·입력·평가·campaign 및 역사 파일 변조/변경 여부 회귀를 통과했다. 새 테스트는 관련 package script에 연결했다.

영수증은 gitignored `spike-out/coverage-20260925-*`에 원본 유지한다. source/input baseline, 복구별 결과, v3 재조회, 분류·평가 행렬이 각각 별도 파일이며 덮어쓰지 않는다. 메인 dirty checkout은 수정하지 않았다. 신규 코드의 배포·새 LLM 실행·독립 정답 평가·새 유입 반복 검증은 아직 수행하지 않았다.

## 6. 다음 실행 범위 — 준비 완료, 승인 대기

- 코드: `b131e6f0227af984dff2d5a4397c43f3bd36ba8a`.
- exact manifest: `fc0d0dffe48a05096dcb3e12c39c7d08d795d71f6037be8b23583c36ef00b746`.
- inventory: `f8bb7848097c65dc0733058ace3b1c5e4b91efee169d2f5ae5b00d10b4ae92c6`.
- 대상 60건: 원문 변경 55건 + 신규 5건. 고정 100건 밖의 대체 공고는 없다.
- 설정: 로컬 `claude-cli` / `claude-opus-4-8`, `matching_only`, 동시 2건, 신청서 실행 없음. 이전 성공 BTS 실행과 같은 모델이다.
- 모든 대상의 현재 모집 상태·입력·첨부 SHA를 준비 전후와 봉인 직전 다시 확인했다. 결과의 `liveExecutionAuthorized=false`, 모델 호출·grant 발급 0회다.
- AGENTS.md의 새 exact manifest 승인 규칙에 따라 60건 실행 승인을 요청했다. 응답 전 live 실행하지 않는다.
- 1,000쌍의 복구 후 로컬 조회: 후보 1쌍, 내부 검토 999쌍. 원문 변경 보류가 반영된 단계이며 독립 정답 0건이므로 정확도·회수율은 미측정이다.
- 웹 빌드는 성공했으며 NFT가 프로젝트 전체를 추적할 수 있다는 비차단 경고 1건이 있다.

### 재개 지점

`spike-out/coverage-20260925-launch-prepared.json`과 `coverage-20260925-target-review.md`에서 exact 대상·설정을 확인한다. 승인 후 정식 `lab:launch:grant → lab:launch`로 진행하고, 성공 receipt를 독립 검수·release·승격하는 범위에서 실제 계정 결과를 다시 확인한다. 승인 응답은 이 문서만으로 추정하지 않는다. 미변경 분석 24건의 검수와 보류 16건은 별도 후속 작업이며 현재 60건 실행 권한에 임의 추가하지 않는다.

### 최종 매칭 집계 검증

`pnpm test:matching-unit`도 통과했다. 집계 중 발견한 오래된 ruleset v15 기대값을 v16으로 갱신하고, 기존 main의 정책 업종 판정과 충돌하던 프로필 영향 테스트를 바로잡았다. ‘바이오’라는 명칭만으로 소프트웨어 활동 부재를 확정하지 않으며, 소진적인 KSIC 코드 목록의 명시적 비일치는 탈락으로 확정하는 두 경로를 모두 검증했다. 이 보완은 테스트만 바꾸므로 봉인한 실행 material은 그대로다.

초안 PR: https://github.com/coolwithyou/noten.changupnote/pull/17. 최초 push의 Vercel Preview build는 성공했으며 프로덕션 배포·실제 계정 인수 증거로 사용하지 않는다.

## 7. 승인된 60건 launch와 독립 검수

사용자의 exact manifest 승인 뒤 grant `480eccd09064a25d10bc8db1b3cb86d24898960b206fe084d23fc2386ec033d5`를 발급하고 로컬 Max 구독으로 실행했다. terminal receipt `3d2fd5390142403f9a7470282428c77055988bed033d0cd3dc2458984837eabd`는 publishable 53·held 1·failed 1·skipped 5다. 실패 1건은 Max 사용량 창 소진이며 공유 guard가 잔여 착수를 멈췄다. skipped 중 sequence 10은 기존 성공 실행의 정책상 재사용이고, sequence 56~59는 미착수다. 같은 grant의 오류 재시도 범위는 sequence 55~59이며, 구독 창 소진 중에는 재호출하지 않았다. held sequence 24는 primary 보류라 재시도 범위에 넣지 않는다.

publishable 53건의 독립 Codex 구독 검수는 `gpt-6-sol`로 완료했다. manifest `d2ef51d25c866a1457e30fdad2db403b177b024bf9cdbc0e6b0dabffaed4410b`, aggregate `14d73d1fc4ed623922c3cf165daaabf6dd6efadd8c6daa6622b3dabb21bf60ff`이다. 보호 검증에 거부된 sequence 19의 첫 두 응답은 같은 packet을 다시 실행해 정상 검증된 결과로 종결했다. aggregate는 결함 123개·미해결 5개·영향 target 40건과 비발행 target 2건을 기록했고 admission은 `HOLD`다. 단일 독립 검수의 지적이며 사실별 재분석·재검수가 필요하다. 결함이 있고 미해결은 없는 35건만 repair manifest `af0b4595c383752dae81b89e58601b08729f29c64c6a168e6c5a7ed6eaa91a84`로 오프라인 준비했다. 변경된 원문 0건, `matching_only`, 동시 2건, 준비된 모델은 `claude-opus-5`이며 새 manifest의 live 승인 없음이다. 미해결 5건은 자동 repair 대상에서 제외했다.

결함 없는 13건의 읽기 전용 release inspect 중 10건은 기존 승인·활성 승격 기록과 중복됐다. 나머지 원본 sequence 18·42·46 세 건만 현행 결속 점검을 통과했다(ready 2·conditional 1, 차단 사유 0). 정확한 grant/run/source SHA는 gitignored `spike-out/coverage-20260925-clean3-release-binding.json`에 보존했다. 이 점검은 release 준비·승인이나 서비스 발행이 아니다. 신규 exact release 처리 상한은 별도 확인이 필요하다.

승인된 `claude-opus-4-8` matching-only 완료 receipt를 offline release reader가 현재 신청서 모델과 동일한 모델이어야 한다며 거부하던 계약 결함을 수정했다(`c3781c8`). 현재 prompt/validator와 current-inventory/matching-only 결속에서만 이 모델을 오프라인 소비한다. 신규 live 모델 권한은 넓히지 않았다. 관련 launch 단위 테스트 23/23, release suite, 웹 typecheck가 통과했다. `lab:launch:test`의 역사 fixture 2건은 변경 전 HEAD에서도 같은 오류로 실패함을 별도 재현했다. 신규 코드의 프로덕션 배포, release DB 준비·승인·승격, 실제 계정 검증, 후속 신규 수집 주기 검증은 아직 수행하지 않았다.

## 8. 임시 작업 디렉터리 소실과 신규 3건 준비

이전 실행 checkout `/private/tmp/cunote-session-review`가 사라져 60건 launch의 원본 manifest/grant/terminal receipt, 53건 LabRun, 독립 검수 결과가 로컬 불변 저장소에 없다. 위 §7의 SHA와 집계는 당시 세션 로그의 관측 기록이며 원본 바이트를 대체하지 않는다. 이 결과를 release에 재사용하거나 이전 grant의 오류 target을 재시도하지 않는다. Claude CLI는 `--no-session-persistence`, 독립 검수 Codex CLI는 `--ephemeral`이어서 모델 세션에서도 원본을 복구할 수 없었다. 로컬 복구 snapshot도 없었다.

2026-09-25 18:06 KST DB runtime은 `paused`, owner/expiry 없음, active deep/application lease 0이었다. 복구 mutation은 하지 않았다. 영속 checkout `/Users/ffgg/noten.works/cunote-coverage-review`의 `spike-out`은 메인 checkout의 기존 불변 저장소를 가리킨다. 새 guard는 실제 산출물 경로가 `/tmp`, `/private/tmp` 또는 시스템 임시 디렉터리이면 launch와 독립 검수 모델 착수 전에 거부한다.

세션 로그에서 독립 검수 결함이 없고 당시 release inspect가 통과했던 세 건의 grant/run/source/input/attachment 결속을 회수했다. [관측 attestation](../evidence/2026-09-25-clean3-artifact-loss-attestation.json)은 `session-transcript-only`이며 소실된 원본 receipt의 검증이나 재사용 권한을 뜻하지 않는다. 현재 DB와 준비 입력을 다시 읽어 세 건 모두 `open`, `visible`, 신청기간 내이며 이전 관측한 source/input/attachment SHA와 일치함을 확인했다.

| 신규 sequence | 공고 | grant ID |
| ---: | --- | --- |
| 0 | 경기 스타트업 지원센터 일반상담 모집 | `76069674-e762-456e-9c69-9c94e76eba1f` |
| 1 | [무료 AI 역량강화 교육]생성형 AI 기반 글로벌 무역·물류 데이터 분석 및 공급망 최적화 과정 (대전) | `13892d8b-a06b-4717-aeda-da44772a786c` |
| 2 | [인천] 2026년 FTA 커스터마이징 컨설팅 참가업체 모집 공고 | `0189ee53-a0b4-48c2-a753-cdb2860dec3b` |

새 `artifact-loss-reanalysis` inventory 정책은 역사 대상 여부, 현재 신청 가능 상태, 원문 material 일치, 현재 material의 기존 성공 LabRun 부재를 확인한다. `matching_only` 신규 실행만 허용하고 신청서 분석 및 기존 receipt 재사용은 허용하지 않는다. 신규 inventory `d2a56ae14e61c658a5d9a7f25902a286d7a4e4da16a744bc9b57de000daa4421`과 신규 manifest `ec3a42f6497cba7f79a6831d2bcdcb24870826aca6b50b94569fdcd5747607ca`를 메인 checkout의 `spike-out/analysis-lab/launch`에 물리 저장하고 각 SHA를 검증했다. 모델은 로컬 Max `claude-opus-4-8`, 동시 2건, 신청서 실행 없음이다. `liveExecutionAuthorized=false`, 신규 grant 0, 모델 호출 0, 서비스 쓰기 0이다. 신규 manifest 전체에 대한 사용자 승인이 필요하다.

코드 commit `e74de4102ebb5626ab8d50f3fe6c70bf3cc934de`: guard 테스트 2/2, recovery 결속 테스트 1/1, launch 단위 테스트 23/23, 웹 typecheck와 package runtime freshness 통과. 넓은 current-inventory suite의 역사 fixture 두 건은 변경 이전에도 같은 실패가 기록돼 있으며 이번 검증에서도 실패했다. 소실된 53건 결과의 원본 복구, 신규 모델 실행, release, 실제 계정 검증은 `UNEXECUTED`다. 메인 dirty 61개 파일은 수정하지 않았다.

## 9. exact19 감시 사건의 별도 준비물 소유 인수

사용자 전달 감시 사건 `350f3af245cde94a9455e22c6d7c7d57a2f5892921053951d05d99556765f8bc`를 인수했다. 사건 종류는 `prepared_manifest_changed` 하나다. 해당 준비물의 후속 소유자는 현재 inventory 구현 세션이다.

- 별도 manifest: `ec3a42f6497cba7f79a6831d2bcdcb24870826aca6b50b94569fdcd5747607ca`. 수신 세션에서도 파일 바이트 SHA 일치를 확인했다.
- 범위: `current-artifact-loss-20260925`, `matching_only`, 3건, `claude-cli`, `claude-opus-4-8`, 동시 2건, 신청서 실행 없음. 봉인된 manifest의 설정과 일치한다.
- 전달받은 감시 snapshot에는 grant/receipt/status가 없다. 이 인계는 신규 launch 승인이나 실행 지시가 아니며 §8의 승인 대기는 유지한다.
- exact19는 전달받은 핸드오프 §50·65 기준의 19 terminal(18 publishable/1 held), seq0 신청서 `review_required`, field-ready 0, 신청기간 종료 상태를 유지한다. 별도 3건과 대상·성과를 합산하지 않는다. 이 수신 작업에서 exact19 상태를 새로 측정하거나 변경하지 않았다.
- 감시 세션은 사건 분류·핸드오프 기록·ack를 담당한다. 수신 세션은 동일 사건을 중복 ack하지 않으며, 이 알림으로 구현·모델 호출·grant 발급·release·배포를 착수하지 않는다.

수신 검증은 manifest SHA·실행 설정 대조와 문서 diff 검증이다. 감시 상태 파일·DB·불변 실행 artifact는 수정하지 않았다.

## 10. 승인된 exact3 재분석과 독립 검수

사용자의 §8 exact manifest 진행 승인에 따라 grant `2e07dad1e7cabdad79977d8ba8d84fd92e325fca376ac97be63cd8beb16f2722`를 발급하고 로컬 Max `claude-opus-4-8`로 3건을 실행했다. terminal receipt `fe7decda8ecaffd2c1fa36672933c0e871a5bd17cee1544f3428dd435f4cd928`는 완료 3·publishable 3·held 0·failed 0·systemic failure 없음으로 봉인됐고, 각 LabRun SHA도 원본 바이트로 검증했다. 총 461.09초, 공고별 wall time은 seq0 130.906초·seq1 153.365초·seq2 328.865초였다. 로컬 비봉인 request 관측에서 seq2는 primary 225.896초와 validator repair 101.484초를 각각 완료했으며 timeout은 없었다. 이 세 건의 지연 관찰은 운영 p95나 SLA가 아니다.

독립 `gpt-6-sol` 검수 manifest `6409c04f3b94a52d33837de44536b1eb8e37ef3a43ee538bd31f670b10899208`의 3개 packet을 모두 처리했다. aggregate `2b5e65d7af1c6ff7414647b22a4a618ed91c039169b64837afaf21d7f8f6b4f7`는 합의 결함 2·미해결 1, 영향 대상 seq0·seq2, admission `HOLD`다. seq0의 `예비 · 초기 (재)창업자`를 재창업자로만 좁히고 업종 축을 붙인 것은 실제 의미 오류다. seq2의 `수출 영세업체`를 매출 규모로 바꾼 것도 근거 없는 추정이다. 같은 seq2는 공식 신청대상 필드의 중소기업과 첨부의 중소·중견기업이 충돌하므로 규모 축은 unresolved로 보류한다. 둘 다 발행 대상에서 제외했다.

결함 없는 seq1의 read-only release inspect는 current source/input/attachment 결속과 conditional readiness, 차단 사유 0을 확인했다. release `deep-matching-coverage-clean3-seq1-20260925-r1-20260925T115806Z-63c03454`를 대상 1건으로 prepare했고 manifest `5a4ba5100e8fb7beccb53d68f7ef1061b9acee25ab780137d4ae74e39edaa971`, plan `4a1af09f10c5ddb8449cbb3fb08971c20d7233a42588132f5e2082ea729a7997`이다. Aggregate는 GO, blocking 1/1, source drift 0이었으나 shadow는 145/145 회사에서 `needs_profile_input→needs_core_review`라는 unexplained transition으로 FAIL했다. 결정 가능 수는 0→0, eligibility는 conditional→conditional, 새 recommendable/eligible은 0이다. 기존 DB의 region·founder_age·biz_age 세 criterion은 각각 포털 `supt_regin`, `biz_trgt_age`, `biz_enyy` 검색 메타데이터에서 만들어진 것으로 현행 원문이 자격으로 뒷받침하지 않는다. 새 분석은 이 3개를 제거하고 재직 실무자·중간관리자라는 `other/text_only` 조건 1개를 남겼다. 기존 질문 레코드는 0개이며 회사 profile로 신청 직원의 역할·재직증명서 사실을 확인할 수 없다. 무근거 기존 축을 유지하거나 profile 사실을 추정하지 않는다. 현재 gate에는 원문으로 입증된 옛 criterion 제거에 대한 별도 explained-transition 결속이 없으므로 r1은 FAIL로 유지하고 dry-run, release approve, `lab:promote --write`를 진행하지 않았다. 후속 설계가 필요하면 옛 criterion별 출처 판정과 회사별 전환 사유를 봉인하는 좁은 계약으로 검토해야 한다.

코드 commit `b796f131a2aa00e8d3a2f5866018b23ae9381891`부터 prompt `lab-deep-v30`과 validator의 원문 기반 seq0·seq2 의미 축소·업종/매출 추정 방어를 추가했다. 최종 validator 버전은 좁은 업종 guard 보정 후 `deep-analysis-validator-v25`다. 재창업 전용 문구, 독립 업종 근거, `소프트웨어 개발 (재)창업자`, 명시 매출 기준은 통과하는 음성 사례도 검증했다. 직접 industry criterion의 모든 업종명 표현을 키워드 규칙으로 판단하지 않으며 독립 검수가 계속 필요하다. `verify:deep-analysis-contract`, launch 23건, normalizer provenance 테스트와 전체 typecheck·package freshness가 통과했다. 과거 exact3 v29/v23 receipt는 offline 검증만 계속 가능하다.

seq0만 독립 검수 결함에 결속한 repair manifest `499b10069afcfcff3be92ba707299269008ae63b19fe20e4e321afe8fbf3c224`로 한 차례 오프라인 준비했다. 원본과 동일한 `claude-opus-4-8`, `matching_only`, 동시 1건, 신청서 분석 없음, aggregate `2b5e65d7...`, 현재 입력·첨부 drift 0, package runtime `81fd6e7dded332cefa42057b9fcbc089aecae1093b30011355df56c1f771c4da`를 결속한다. 그 전 manifest `7067108d...`는 기존 기본 정책의 `claude-opus-5`였다. 둘 다 실행하지 않은 역사 준비물이다. 최종 의미 guard 소스는 아래 후속 commit에서 다시 봉인한다. 새 manifest의 grant·terminal receipt는 0이며 live 모델 호출에는 새 exact 승인 범위가 필요하다. seq2는 원문 충돌이 해결되기 전 자동 repair 대상에 넣지 않는다.

별도 감시 인계 fingerprint `692ea62b296395831bbf544a02aa8934ac39c7191f6c80669084d70066d15e0f`를 수신했다. 전달된 exact3 grant·terminal 결속과 완료 상태 및 봉인 SHA·LabRun SHA 검증은 위 기록과 같고, 새 repair 준비물은 grant/receipt/status 없음이다. 후속 소유는 이 세션이며 감시쪽은 fingerprint만 ack한다. 이는 새 실행·서비스 쓰기·품질 인수 승인이 아니고 exact19와 합산하지 않는다. 소실된 기존 53건 원본 receipt도 복원되지 않았다. 실제 글리프 계정 추천 인수 및 신규 공고 반복 처리 검증은 아직 수행하지 않았다.

### seq0 최종 재분석 준비물

- 최종 code commit: `6efb13cc115877f0deaf72e5e1be2a46ea3aa11e`. 마지막 업종 guard는 원문이 `예비·초기 (재)창업자`인 좁은 형태만 검사한다. `소프트웨어 개발 (재)창업자` 같은 명시 분야는 거부하지 않는다. 모든 자연어 의미 오류를 해결하는 규칙은 아니므로 독립 검수는 유지한다.
- 최종 manifest SHA: `5e9a4c8ebee10f47b6a81b2d66b659f51a032f131440735cd28cf3d174167120`. 물리 경로는 `/Users/ffgg/noten.works/cunote/spike-out/analysis-lab/launch/manifests/5e9a4c8ebee10f47b6a81b2d66b659f51a032f131440735cd28cf3d174167120.json`이며 파일 SHA가 이름과 일치한다. 이전 `499b1006...` 준비물도 실행하지 않았다.
- 정확한 대상은 `76069674-e762-456e-9c69-9c94e76eba1f` 한 건, input `a99c478cf832b18008b39010535553c39ac0e0983dc0389c06ef078e3fd1f2cb`, attachment manifest `c424acbcbc328ee5a7e92657d642421fc51470c8ad4af1110cbe37184ac6e8e5`. 독립 검수 aggregate `2b5e65d7...`의 원본 seq0 결함 1개와 원본 run ID를 결속한다. 모델은 원본과 같은 `claude-opus-4-8`, 로컬 `claude-cli`, `matching_only`, 동시 1건, 신청서 분석 없음이다. prompt `lab-deep-v30`, validator `deep-analysis-validator-v25`, package runtime `81fd6e7d...`를 결속한다. 원본 exact3 v29/v23과 달라진 material은 이 prompt/validator 및 단건 repair instruction이고, 모델·입력·첨부는 같다.
- 신규 manifest의 grant와 receipt는 0개다. 실행 승인이 오면 `pnpm lab:launch:grant -- --manifest=5e9a4c8ebee10f47b6a81b2d66b659f51a032f131440735cd28cf3d174167120 --approved-by=ffgg-user`로 단일 grant를 만들고, 그 출력 SHA로 `pnpm lab:launch -- --grant=<새-grant-sha256>`를 실행한다. 현재는 두 명령을 실행하지 않았다.
- 최종 `verify:deep-analysis-contract` 및 전체 typecheck, launch 단위 23건, `verify:package-runtime-freshness` 통과. 계약 gate 출력은 물리 경로 `/Users/ffgg/noten.works/cunote/spike-out/coverage-20260925-deep-contract-v25.log`에 보존했다. 마지막 runtime 관측(2026-09-25T12:09:43.946Z)은 `paused`, generation 457, owner/expiry 없음, active deep/application lease 0이다. 서비스 criteria 쓰기 0, 실제 계정 검증 0이다.

## 11. seq0 보정 실행과 독립 재검수

사용자가 §10 최종 manifest `5e9a4c8ebee10f47b6a81b2d66b659f51a032f131440735cd28cf3d174167120`의 단건 보정과 Sol 독립 재검수를 승인했다. 실행 전 manifest 파일 SHA, 현재 input `a99c478cf832b18008b39010535553c39ac0e0983dc0389c06ef078e3fd1f2cb` 및 첨부 manifest `c424acbcbc328ee5a7e92657d642421fc51470c8ad4af1110cbe37184ac6e8e5`, package runtime freshness, paused·무 lease DB snapshot, `claude.ai/firstParty/max` 인증을 확인했다. 이전 동일 manifest grant·receipt는 0개였다.

정식 grant `0f0b0c6216b2aa15d22efab2888789f382078029a06d684e079b7cec0cdb08af`와 terminal receipt `f8e1ccec3614191f9ee6889800b93feb4f6618f8664edc1d6af197198f747266`를 생성했다. 모델은 승인된 `claude-opus-4-8`, 로컬 구독, matching-only였고 결과는 publishable 1·held 0·failed 0·skipped 0, `stopReason=completed`, systemic failure 없음이다. 새 LabRun `run-2026-09-25T123852.193Z-499d27`의 SHA는 `a98a1e354935effe5472c5dabec63c57450baf8e332669e1307875f271973e03`이다. grant·receipt·LabRun 각각의 원본 파일 SHA를 확인했다. Cohort 시작 2026-09-25T12:38:50.935Z, 종료 12:41:09.308Z로 총 138.373초, LabRun 137.081초다. 비봉인 실행 관측의 primary 요청은 136.146초·queue wait 0.148ms, 완료 1회이며 validator repair 요청과 timeout은 없었다. 이는 단건 지연 관찰이다.

보정 전 criterion[1]은 `other/text_only`의 value.note에서 일반 창업자를 빠뜨리고 covered_dimensions를 `biz_age,industry`로 두었다. 새 결과는 예비·초기 일반 창업자와 재창업자 모두를 적고 `biz_age,target_type`으로 바꾸며 industry를 `inspected_no_condition`으로 정리했다. 경기도 region criterion은 유지됐다. 그러나 새 value.note가 `① 예비창업자, ② 초기(신규)창업자 및 재창업자, ③ 기술창업기업이 OR`라고 써 재창업자를 초기 여부와 분리해 읽히게 했고, 포털 검색용 `biz_enyy`의 예비·1년·2년·3년 미만 열거를 업력 범위의 보조 근거로 사용했다. axis biz_age 설명에도 원문에 없는 `초기(3년 미만)`을 썼다. 봉인된 본문·상세·첨부의 대상 문구는 `경기도 내 예비·초기 (재)창업자 및 기술창업기업`이며 숫자 업력 상한은 없다.

Sol 독립 검수 manifest `7269cdde320530a607d66073f15f624a034377bb8dc76720973f6a3707629440`는 packet 1건을 처리했고 aggregate `36ff06d0f022b6e7a5b7db0c53c87da6ef76b13a990458d3a1c3f0e09a6f50a5`는 결함 1·미해결 0, `HOLD`다. 원본 LabRun의 note와 axis에 존재하는 오류라 단순 검수 과잉이나 projection 재실행만으로 해결되지 않는다. 현행 review carryforward는 text_only 값의 무손실 복원·runtime 차이만 허용하고, matching reprojection은 진단용이다. 원본 receipt를 수정하거나 gate를 완화하지 않았다. 독립 검수 HOLD이므로 release inspect/prepare, dry-run, approve, 서비스 promotion을 하지 않았다.

후속 단건 repair manifest `413f0b86afd80e9b1a9f4257b9e8b50bdc49d0a8240ad3090cbe176d25a926bf`는 모델 호출 없이 준비만 했다. 원본 seq0 grant `76069674-e762-456e-9c69-9c94e76eba1f`, 동일 input·첨부 SHA, `claude-opus-4-8`, `matching_only`, 동시 1건, prompt v30·validator v25·package runtime `81fd6e7d...`를 유지한다. 다만 상위 검수 aggregate가 `36ff06d0...`으로 바뀌고 이전 결함과 이번 결함 2개를 누적 지시로 봉인한 새 manifest다. 파일 SHA를 검증했고 이 manifest의 grant·receipt는 0개다. 새 exact 승인 없이 실행하지 않는다. 같은 모델 호출을 반복하는 것만으로 근본 해결된다고 가정하지 않는다. 후속 구조개선에서는 text_only note와 axis의 해석도 현재 source 필드의 자격 근거인지 포털 검색 메타데이터인지 분리해 결속해야 한다.

2026-09-25T12:45:05.476Z DB runtime은 `paused`, generation 459, owner/expiry 없음, active deep/application lease 0이었다. seq1 legacy shadow FAIL, seq2 공식 대상·첨부 규모 충돌, exact19는 그대로 별도 보류다. 독립 정답 평가, 글리프 실계정 추천 인수, 신규 수집 주기 검증은 아직 수행하지 않았다.

## 12. 작은 사용자 목표로 전환

사용자의 단계별 진행 지시에 따라 [글리프 소규모 매칭 인수](./2026-09-25-글리프-소규모-매칭-인수.md)로 현재 작업을 좁혔다.
8건 원문을 비교하고 실제 글리프 계정에서 BTS 조건부 후보→ICT 업종 근거→기업규모 질문 이동을 확인했다.
회사 답변은 입력하지 않았고 추천 수 증가·신규 배포는 없다. 성수 공고는 검토 여지가 있으나 운영 상세에서
분석 미확정으로 표시되는 것을 재현했다. 이 표본을 전체 정확도나 독립 사람 검수 정답으로 해석하지 않는다.

`f73a845`는 K-Startup 검색 필터 3개를 자격 입력과 후단 추출 결과 합성에서 제외한다.
`b0ef774`는 발행 증거 없이 안정키 없는 v3 검색 필터 파서 조건만 있는 경우를 분석 누락으로 구별한다.
기존 DB·원문·불변 분석을 보존하며, 후자의 교정으로 실제 성수 1건이 정상 분석 준비 경로에 도달했다.
검증 범위와 남은 pilot fixture 실패는 새 실행계획에 기록했다.

새 성수 단건 manifest는 `67732d914aa6947780553e087dd521d592c1f0c38e11b259d9003ccc7d42c615`이다.
로컬 Max Opus 4.8, matching-only, 동시 1건, 신청서 없음이며 준비 시 grant·모델 호출 0이다.
기존 seq0 repair와 exact19를 실행하거나 합산하지 않았다. 신규 모델 실행은 제시한 이 명세의 승인을 기다린다.
