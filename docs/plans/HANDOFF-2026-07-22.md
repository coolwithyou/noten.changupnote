# HANDOFF — 공모 딥분석 확대 실험 (2026-07-22 — **대체됨**)

> ⚠️ **이 문서는 `HANDOFF-2026-07-23.md`로 대체되었다** — 재개 시 그 문서만 읽으면 된다.
> 아래 내용은 07-22~23 경위·커밋 해시 참조용 로그로만 유지한다.

> 🟢 **진행 상황(07-23)**: **확대 30건 전체 분석·AI 검수·감사 동결 완료** — 남은 것은
> [사용자] UI 감사뿐. 경로: 동결 `1adfeec` → 층화 30건 → 검수 자동화 채택(§9 사전 등록,
> fable-5 캘리브레이션 3기준 충족 24/28·오검출 0·45/46, 개정 카드 소진) → 감사 루프
> 구현(2a396f1, UI 감사 탭→게이트 표본 자동 편입) → 배치 1~3 분석 27/27($5.41)·AI 검수
> 27/27($7.36 — ARPA-H 거부는 재실행에서 미재현·정상 완료, 사람 전수 불필요) → **감사
> 파일 27건·118항목 동결**. [사용자] 감사 ~1.5~2시간(세션 분할 가능) → aggregate(조기
> 중단+게이트) → STOP/ITERATE/GO 문서 → `pnpm lab:shadow`. 비차단: export_performance 축.

## 목표

공고문 전문(구조화 필드+첨부 markdown)을 Opus 4.8로 딥분석해 22축 자격요건을 **기계판정
가능한 형태로** 채우는 파이프라인을, 층화 30→100건 + 사람 검수로 확증한다. 상위 목적은
매칭 확정 판정률 개선(공고당 기계판정 가능 criteria 0.67→5.67건 실측이 근거).
실행 계획의 단일 원천: `docs/plans/2026-07-21-analysis-lab-expansion-experiment.md`.
파일럿 판정: `docs/research/2026-07-21-공모딥분석-검수집계-판정.md`.

## 완료된 것 (전부 main 커밋·push 완료)

| 커밋 | 내용 · 검증 증거 |
|---|---|
| 128bee2 | **파일럿 판정 문서** — 게이트 통과, 구조화 ≥50%의 6번째 게이트 승격 결정. 3렌즈 원천 대조 검증 |
| f7ac7cb·27befca | **확대 실험 계획** — 층화·STOP/ITERATE/GO 사전 등록·검수자 편향 완화 4종. 재고 실측(kstartup markdown 재고 6건뿐 → bizinfo 중심 1단계, applyEnd 필터 폐기)·매칭 임팩트 섀도 측정 단계 반영 |
| 7b26092 | **선행 구현 #1~6** — 집계 코호트 필터(--all 탈출구)·게이트 6종·층화 코호트(strata.ts, cohort.json v2, 검수 보존 가드, 실DB 30건 선정이 계획 배분과 일치 검증)·배치 러너 `pnpm lab:batch`(dry-run 6시나리오 검증)·UI 동적화·startedAt 계측. tsc 0·드리프트 0 |
| 868d4f9 | **CALIBRATION 외부 대조** — 전제 재고 0건(P1~P4 유지·P5·P6 보강). 파일럿 게이트 판정 표본 제외(순환 제거)·이의 플래그 사후 노출 3조건·축 승격 규칙(독립 공고 ≥3) 사전 등록. `docs/research/2026-07-22-공모딥분석-확대실험-calibration.md` |
| 3b896e0 | **선행 구현 #7** — span 검증 시 위치 기록(spanOffsetRatio, 요청 본문 무변경)·집계 위치 진단 블록(전/중/후반). 픽스처 E2E·실데이터 스모크 검증 |
| 6c040b7 | **검수 저장 가드** — 기존 검수 로드 실패 시 저장 차단(07-22 실사고 재발 방지) |
| 9640380 | 이전 핸드오프(07-21) 현행화 |
| eb0b934 | **매칭 임팩트 섀도 하네스 선구현** — `pnpm lab:shadow`(검수 correct criteria → GrantCriterion 변환 → 섀도 match_state 재계산, 사전 등록 지표 4종, DB 쓰기 0). 변환기는 golden_set 승격 트랙 발행 어댑터로 재사용 예정. typecheck 0·픽스처 테스트·aggregate byte-identical·파일럿 3건 실스모크(27/27 변환). 계획·스모크 관찰: `docs/plans/2026-07-22-analysis-lab-shadow-harness.md` §8 |
| 1adfeec·712e0fa | **사례집 등재 + 동결 선언** — 가이드 §5에 "우대 대상 목록의 불완전 열거" 등재, 동결 해시 `1adfeec`를 확대 계획 §3에 기입(프롬프트 v2·가이드·판정 규칙 동결 — 수정은 STOP/ITERATE 후에만) |
| 5c443ca | **집계 게이트 표본 파일럿 제외** — 검수 보존 가드가 파일럿을 확대 코호트에 남겨 기본 집계에 파일럿 28판정이 혼입되던 순환 발견·수정. 기본=제외(안내 출력), `--all`=포함치(민감도). 섀도는 명시적 포함(30건 검수 확정분 = 파일럿 포함) |
| a342d7e | **프로토콜 개정 §9 사전 등록** — 검수 주체 "사람 전수"→"AI 전수(추출과 다른 모델) + 사람 표본 감사"(창업자 결정·시간 제약). 캘리브레이션 채택 기준을 결과 확인 전 등재. 동결 3종 무변경, 순환성 가드 3중 유지 |
| 55a22cd·88f14b2 | **AI 검수기(`pnpm lab:ai-review`) + 캘리브레이션** — 입력 재조립 sha 검증·블라인드 프롬프트·가이드 rubric 런타임 이식·판정≠추출 모델 하드 가드·불변 저장(`<runId>.ai-review.<model>.json`). v1 미달 → §9 1회 개정(v2: §0 우위·통합공고·빈 축 중복 배제) → **fable-5 기준 3종 충족(24/28·오검출 0·45/46)**, sonnet-5 미달. 4회 실측 전부 치명 오검출 0. 캘리브레이션 총 $4.22 |
| d0c41f7 | §9 캘리브레이션 결과 기록(계획 문서) — fable-5 채택, 개정 카드 소진, 운영 절차 명시 |
| 191819f | 감사 목록에서 사람 검수 보유 런(파일럿) 제외 |
| 2a396f1 | **감사 기록 + 집계·섀도 통합** — audit-store(`<runId>.audit.<model>.json`, 대상 풀 결정론 동결·판정 병합 저장·삼중 사고 가드)·audited-reviews(감사 완료 AI 검수 → LabReview 호환 병합)·aggregate 게이트 표본 편입(방법론·캘리브레이션 병기, 감사 대기 무은폐)·shadow 포함·UI 감사 탭(AuditSheet, 동의/수정+사유 필수·부분 저장)·카드/보드 감사 배지. 감사 파일 9건 생성(49항목 동결). typecheck 0·테스트 3종·드리프트 신규 0 |

**커밋 아님(디스크 산출물, spike-out은 gitignore)**:

- **층화 30건 선정 완료(07-22)** — cohort.json v2: seed 42·expansion-s1, 층 분포 pilot 3 보존
  + bizinfo(thick 7·medium 7·thin 4) + kstartup(thick 1·medium 3·thin 5), 쿼터 통합공고
  4/4·A≥3 12/5 충족, 경고는 보존 안내뿐.
- **배치 1 분석 10건 완료(07-22)** — `pnpm lab:batch` dry-run→2건($0.45)→8건($1.87), 10/10
  성공·실패 0, 총 $2.32, 공고당 51~90초·$0.15~0.34(파일럿 추정 $0.395보다 저렴). 대상은
  bizinfo medium 7·thick 3. 배치 2·3 잔여 17건.
- **배치 1 AI 검수 9/10 완료(07-23)** — `pnpm lab:ai-review -- --model=claude-fable-5`,
  $2.54. **ARPA-H 공고 1건은 fable-5 bio 안전 분류기 판정 거부(refusal)** — 추출 모델
  (Opus 4.8) 폴백은 자기 채점 순환 가드 위반이라 미사용, **사람 전수 검수 경로로 이관**.
  AI 판정 분포: needs_edit 다수(예외 누락·서식 문구 오인 지적 등 실질 사유), wrong·unsure 0.
- **감사 목록 생성(07-23)** — `pnpm lab:ai-review -- --audit-list --model=claude-fable-5`
  → 9공고 49항목(비-correct 31 + missed 플래그 3 전수 + correct 75중 표본 15, 시드 42).
  `spike-out/analysis-lab/audit/2026-07-22T154744.009Z.json`.

- **07-22 검수 사고·복원(커밋 아님 — spike-out은 gitignore)**: 기존 검수가 로드되지 않은
  빈 시트에서 재판정·저장돼 needs_edit 1건이 소실됐었음 → 07-20 원판정으로 복원 완료,
  `pnpm lab:aggregate` 재집계가 판정 문서 수치를 정확히 재현함을 확인(🟢 6/6).

## 남은 작업 (순서대로)

1. **[사용자만 가능·비차단] 식품 공고 export_performance 축 판정** — `pnpm dev:web` →
   http://127.0.0.1:4010/dev/analysis-lab → 식품 해외인증 "최신 런 검수하기".
   **기존 판정 26건이 채워진 채 열리는지 먼저 확인**(빈 시트면 로드 실패 — 저장이 차단되고
   새로고침 안내가 뜬다). 축 목록 맨 마지막 행 판정 → 하단 바 **진행률 27/27 확인** → 저장
   → `pnpm lab:aggregate` 재집계. 어느 판정이든 게이트 결론 불변.
2. **[사용자] 감사 27공고·118항목 — UI에서** (~1.5~2시간, 종전 전수 검수 ~7.5시간 대체.
   세션 분할 가능 — 부분 저장 지원): `pnpm dev:web` → /dev/analysis-lab → "감사 0/N" 배지
   공고 → 런 상세 **"감사" 탭** → 항목별 **동의** 원클릭 또는 **수정**(판정 변경+사유
   필수). **권장 순서: 배치 1(9공고 49항목) 먼저** → `pnpm lab:aggregate`로 조기중단 게이트
   (wrong률 >10%·공고당 누락 >1건) 점검 → 이상 없으면 배치 2·3(18공고 69항목) 계속.
   전 항목 완료 공고는 게이트 표본에 `[AI+감사]` 자동 편입. **뒤집힘 공고당 >1건 누적 시
   자동 검수 신뢰 재평가(§9)**. 감사 파일 27건은 대상 동결 상태(추가 배치에도 불변).
3. **감사 완주 후 `pnpm lab:aggregate`** — 30건 게이트 6종 판정(방법론·캘리브레이션 병기
   출력). 이어서 STOP/ITERATE/GO 판정 문서(docs/research/, 한글 파일명) + `pnpm lab:shadow`
   섀도 측정 — 이 둘은 세션에 요청하면 집계·문서화·실행을 대행한다.
5. **배치 2·3: 분석 + AI 검수 선실행 완료 예정(07-23 진행)** — **운영 이탈 기록**: 계획의
   "분석 선실행 금지"(배치당 매몰 ~$4 상한 가드)를 사용자 최고 속도 지시에 따라 이탈,
   배치 1 감사 완료 전에 배치 2·3 분석(17건)+AI 검수+감사 동결을 선실행. 근거: 조기중단
   게이트 선행 신호(배치 1 AI 검수 wrong 0건·누락 플래그 0.33/공고 — 기준 10%·1건/공고
   대비 큰 여유)로 STOP 확률 낮음, 매몰 위험 상한 ~$12. **게이트 공식 판정은 변함없이
   감사 후 `pnpm lab:aggregate`로 한다**(사전 등록 판정 규칙 무변경). 배치 경계 사례집·
   unsure 재판정 의례는 감사 흐름의 배치 경계에서 수행.
6. **30건 완주 시** — STOP/ITERATE/GO 판정 문서화(한글 파일명, docs/research/) +
   **매칭 임팩트 섀도 측정**(계획 §4 — 검수 확정 criteria로 match_state 섀도 재계산,
   절대량 전/후). **측정 코드는 선구현 완료(eb0b934)**: `pnpm lab:shadow` 실행이면 됨
   (기본 코호트 필터, 회사는 DB 전 행 + `--bizNo=CSV` 팝빌 캐시). 해석 주의는
   `docs/plans/2026-07-22-analysis-lab-shadow-harness.md` §8(빈 프로필 붕괴는 프로덕션
   실동작 — 실사용자·팝빌 캐시 기준으로 해석). 이 결과가 golden_set 승격(메인 매칭 편입)
   착수 근거.
7. **병행 트랙**: 변환 서버 재배포 → kstartup open 첨부 일괄 변환(2단계 층 재고 + 제품
   recommendable 지렛대) / UI 시각 검수(검수 대기 토글·보드 점프·게이트 배지 6개 줄바꿈).

## 검증 체크리스트

- [ ] export_performance 판정 저장(식품 review.json axisReviews 11건) 후 재집계(`--all`)
- [x] 사례집 등재·동결 커밋 해시가 계획 §3에 기입됨 (`1adfeec`, 712e0fa)
- [x] cohort.json v2에 30건·층·시드 기록, 쿼터 충족 (seed 42·expansion-s1, 통합공고 4/4·A≥3 12/5)
- [x] 배치 1: 런 10건 생성, 총비용 $2.32 ≤ $5 (10/10 성공, 실패 0)
- [x] `pnpm lab:aggregate`가 확대 코호트만 집계 — 헤더 expansion-s1 표시 + 파일럿 층 게이트
      표본 제외(5c443ca, 기본 동작. 파일럿 포함치는 `--all`)
- [x] 배치 1 AI 검수 9/10($2.54) + 감사 목록 49항목 생성 — fable-5(§9 캘리브레이션 충족)
- [x] 배치 2·3 분석 17/17($3.09) + 확대 27건 AI 검수 27/27(누적 $7.36, ARPA-H 재실행 정상 완료) + 감사 27건·118항목 동결
- [ ] [사용자] 감사 118항목(배치 1 먼저 → 조기중단 게이트 → 배치 2·3)
- [x] 감사 판정 기록·집계 통합 구현 (2a396f1 — 감사 파일 9건·49항목 동결)
- [ ] [사용자] 감사 완료 → `pnpm lab:aggregate` 조기중단 게이트 공식 점검
- [ ] 30건 완주 → 판정 문서 + 섀도 측정 결과가 docs/research/에 커밋

## 주의 / 함정

- **검수 시트 사고 교훈(07-22 실사고)**: 시트가 빈 채 열리면 기존 검수 로드 실패다 —
  이제 저장이 차단되지만, 판정 전 항상 기존 판정이 채워져 있는지·저장 전 진행률 N/N을
  확인할 것. 검수 파일은 디스크 단본(주 1회 백업 루틴 권장).
- **Opus 4.8 파라미터**: temperature/top_p/thinking 전송 시 400 — extractor가 의도적
  미전송. 모델 오버라이드는 env ANALYSIS_LAB_MODEL.
- **dev 서버는 사용자 소유** — 세션이 백그라운드로 띄우지 말 것.
- **파일럿 3건은 게이트 판정 표본에서 제외** — 검수 보존 가드가 파일럿을 확대 코호트에
  남기므로 코호트 필터만으론 부족했고, 5c443ca가 stratum=pilot 층 제외를 기본화(집계 시
  안내 출력). `--all`은 민감도 참고용 포함치. 단 **섀도 측정(lab:shadow)은 파일럿 포함**이
  맞다(30건 검수 확정분, 계획 §4). 확대 중 프롬프트 v2 동결(수정은 STOP/ITERATE 후에만).
- **코호트 재선정(refresh)**: 검수 보유 공고 보존 가드가 있으나, 확대 실험 중 UI의
  재선정 버튼은 여전히 누르지 말 것(운영 가드).
- batch·aggregate·smoke는 명시적 exit 구현됨(미종료 전례는 verify:service-data 계열).
- **Sonnet 5 A/B 후보는 인트로 가격($2/$10)이 2026-08-31 종료** — 30건 종료 직후 실행하면
  ~$5로 검증 가능(env만 변경, calibration §5).
- 층화 재고는 bizinfo 중심(kstartup markdown 보유 open 6건뿐) — kstartup 층은 변환 서버
  가동 후 2단계에서. grants 행은 인제스트에서 삭제되지 않음(마감은 status UPDATE) —
  코호트 grantId 고정 안전. 단 프로덕션 DB에서 dev 시드/verify 스크립트 수동 실행 금지.

## 백그라운드 작업

없음 — 에이전트·워크플로우 전부 완료, 워킹트리 클린, 미push 커밋 0.
