# HANDOFF — 공모 딥분석 실험실: 검수 UX·지표·가이드 완비 (2026-07-21)

> 🟢 **진행 상황 (2026-07-21 갱신)**: 창업자 검수 3건 완료 → `pnpm lab:aggregate` **게이트
> 5/5 전부 통과**(정밀도 96.4%·오류 0%·누락 0건·커버리지 13.5x·$0.395/공고). 판정 문서
> (`docs/research/2026-07-21-공모딥분석-검수집계-판정.md` — 구조화 ≥50%의 6번째 게이트 승격
> 결정 포함)와 층화 확대 실험 계획(`docs/plans/2026-07-21-analysis-lab-expansion-experiment.md`)
> 작성·커밋. 잔여: ① [사용자] 식품 공고 빈 축 export_performance 1건이 미판정으로 남음
> (검수 UI에서 이 축만 판정 후 재집계 — 어느 판정이든 게이트 결론 불변) ② 확대 실험 착수
> (선행 구현 5건 + CALIBRATION 대조, 계획 문서 참조) ③ push 여부 사용자 확인.

## 목표

공고문 전문(구조화 필드 + 첨부 markdown)을 Opus 딥분석해 **22축 자격요건을 "실제 사업체와
매칭 가능한(기계판정 가능한) 형태로" 채울 수 있는지** 검증한다. 사람 검수(창업자)가 골든셋의
1차 원천이며, 통과 기준 5종 + 보조 지표로 판정한다. 배경·이전 이력은
`docs/plans/HANDOFF-2026-07-18-notice-deep-analysis-lab.md`, 근거 진단은
`docs/research/2026-07-13-매칭시스템-현황평가.md`(공고 측 기계판정 가능률 ~40% 병목).

## 완료된 것 (전부 main 커밋, push 안 됨)

| 커밋 | 내용 · 검증 증거 |
|---|---|
| 83de765 | **검수 UX 전면 개선** — 사용 순서 가이드·검수 진행 보드(n/3)·원클릭 "최신 런 검수하기"(검수된 성공 런 우선)·검수 탭 안내(판정 기준 6종+GATES)·고정 저장 바(진행률·다음 미판정 점프)·"누락 있음" 사유 필수 클라 검증·keepMounted(탭 왕복 초안 보존)·beforeunload. 멀티에이전트 적대 리뷰(3렌즈 12에이전트) 확정 결함 6종 수정: ①분석 완료 시 화면 탈취로 미저장 초안 파괴 차단(dirty 리프트) ②aggregate 공고당 최신 검수 1건 dedupe ③실패 런 검수 3중 차단(UI·서버 400·집계) ④runLoading 잔류 무한 스피너 ⑤note maxLength 선차단(2,000/4,000자) ⑥Card overflow-hidden이 sticky 저장 바 막던 것. 실브라우저에서 전 흐름 실측 확인 |
| d362280·6883cde·1b4dd56 | 핸드오프(07-18) 갱신 3회 — UX 개선·가이드 링크·보조 지표 등재 |
| 9643b7f | **검수 판정 가이드** `docs/research/2026-07-18-공모딥분석-검수판정-가이드.md` — 리트머스 질문("이대로 DB에 넣고 매칭하면 원문과 다른 결론이 나오는 기업이 있는가")·경계 규칙 §2-A~G·실런 3건 공고별 최우선 확인 항목(★)·§5 사례집 누적 루프 |
| e3123a1 | **기계판정 가능 보조 지표**(게이트 아님) — `pnpm lab:aggregate`에 "정확 확정 B 중 구조화(≠text_only) 비율"과 "공고당 기계판정 가능 criteria A→B 배수" 추가. 가짜 모노레포 루트 픽스처로 E2E 검증(실 spike-out 미접촉, 기대값 전부 일치). 게이트 승격은 첫 집계 후 결정 |
| f34c452 | **혜택 배지** — 공고 카드·런 상세 헤더에 "이 공고가 주는 것"(자금지원/융자/역량강화/공간/판로·마케팅/인증·IP/네트워크) 표시. 제품 공용 `deriveGrantBenefits` 7 family 재사용(archive와 동일 라벨), 미분류는 ghost 배지. 실측: 코호트 3건 = 자금지원 / 인증·IP / 역량강화·공간 |
| d9cea2d | (병렬 세션 산출물 커밋 대행) TrialGPT·조건 정규화·Rules as Code 연구 `docs/research/2026-07-20-trialgpt-...md` |

통과 기준·보조 지표의 단일 원천: `apps/web/src/features/dev/analysis-lab/contract.ts`의
`ANALYSIS_LAB_GATES` — 정확≥80% · 오류≤10% · 공고당 누락≤1건 · 커버리지≥1.5x · 공고당 ≤$1.

## 남은 작업 (순서대로)

1. ~~검수 3건 수행~~ **완료** (2026-07-20~21, sw@noten.im). ~~집계·판정~~ **완료 — 🟢 5/5**.
   ~~판정 문서화~~ **완료**: `docs/research/2026-07-21-공모딥분석-검수집계-판정.md`
   (보조 지표 "구조화 비율 ≥50%"의 6번째 게이트 승격 결정 포함).
2. **[사용자만 가능] 식품 공고 빈 축 1건 추가 판정** — 검수 파일 대조 검증에서 발견:
   식품 해외인증의 빈 축은 11개인데 10개만 판정 저장됨(UI는 미응답 축을 저장에서 제외).
   빠진 **export_performance**가 하필 가이드 ★★★ 누락 후보(원문에 수출실적 가점 실재
   가능성, 딥분석 축 검사는 condition_found). dev 서버에서 해당 런 검수 탭 → 이 축만 판정
   → 저장 → `pnpm lab:aggregate` 재집계. 어느 판정이든 게이트 5/5는 불변(누락이어도
   공고당 0.33건 ≤ 1건) — 기록 완결성 목적.
3. **확대 실험 착수** — `docs/plans/2026-07-21-analysis-lab-expansion-experiment.md` 순서대로:
   층별 재고 카운트 쿼리 → 선행 구현 5건(집계 코호트 필터가 최우선 — 파일럿 3건 혼입 차단)
   → CALIBRATION 외부 대조 → 배치 1(10건). 판정 규칙(STOP/ITERATE/GO)은 계획에 사전 등록됨.
4. **병행 가능 후속** (각각 별도 계획 필요): 검수 → golden_set 승격 트랙(DB 쓰기) /
   통합공고 분해 처리 / 신규 축 제안 반복 집계(현재 공고 간 독립 반복 0건 — 판정 문서 §7).
   미검증 아이디어: DB 파생 vs 딥분석 혜택 분류 A/B 비교(화면 구조는 이미 지원).
5. (선택) **push** — main에 미push 커밋 다수(이번 판정·계획 문서 포함). 사용자 확인 후.

## 검증 체크리스트

- [x] `ls spike-out/analysis-lab/*/*.review.json` ≥ 3건 (성공 런 기준 3공고)
- [x] `pnpm lab:aggregate` 종합 판정(🟢/🟡/🔴) + 보조 지표 출력 — 🟢 5/5
- [x] 판정 연구 문서가 docs/research/에 한글 파일명으로 커밋됨
- [x] 확대 실험 계획이 docs/plans/에 생성됨
- [ ] 식품 export_performance 빈 축 1건 판정 후 재집계 (기록 완결성)

## 주의 / 함정

- **07-18 핸드오프의 주의 사항 전부 유효** (Opus 4.8 파라미터 400, dev 서버 사용자 소유,
  ANTHROPIC_API_KEY는 루트 .env, 코호트 재선정 금지, spike-out 불변, 분석 API 동기 수 분).
  이번 세션 추가분:
- **aggregate 의미 변경**: 같은 공고에 검수가 여러 개면 최신(updatedAt) 1건만 집계(이중 계상
  방지), 실패 런 검수는 제외 — 제외분은 경고로 출력된다. "공고당" 지표의 분모는 이제 공고 수.
- **실패 런은 검수 불가** — UI가 시트를 막고 서버 PUT도 400. 성공 런만 검수 대상.
- **분석 완료 시 미저장 검수가 있으면 화면을 전환하지 않고** 해당 공고 카드에 안내만 남는다
  (초안 보호). "저장된 런"에서 새 런을 수동 선택.
- **검수 저장 차단 사유는 고정 바에 표시** — 이메일 미입력 / 판정 0건 / "누락 있음" 사유 공백.
- **Chrome 자동화로 검증 시**: 창이 가려지면(visibilityState hidden) smooth 스크롤이 전부
  무동작 — 코드 버그 아님. instant로 폴백해 검증할 것.
- **`toGrant` 매퍼**(`apps/web/src/lib/server/archive/grantArchiveData.ts`)는 실험실 cohort가
  재사용하려고 export로 열었다 — archive 리팩토링 시 서명 유지 주의.
- **드리프트 스캔**: analysis-lab은 0건. archive·match-results에 기존 위반 잔존(이번 범위 아님).

## 백그라운드 작업

없음 — 워크플로우·리뷰 전부 완료 상태.
