# HANDOFF 2026-07-11 — 공고매칭 차원 확장 (검토·계획 완료, 구현 미착수)

## 목표

공고 자격조건을 기업 프로필과 대조하는 매칭 14차원 분류 체계가 정확한 매칭에 충분한지 심층 검토하고, 부족분(결격/배제 조건 축)의 확장 구현계획을 수립·리뷰하는 것. **사용자 지시로 구현은 착수하지 않음** — 계획 승인 후 별도 세션에서 구현.

- 검토 결과: `docs/research/2026-07-11-공고매칭-14차원-확장-검토.md`
- 구현계획(단일 원천): `docs/plans/2026-07-11-matching-dimension-expansion.md`

## 완료된 것 (전부 워킹트리에만 있음 — **커밋 안 됨**)

1. **연구 문서** `docs/research/2026-07-11-공고매칭-14차원-확장-검토.md` (신규)
   - 결론: 기존 13개 긍정요건 축은 타당, 결격(체납·신용·제재·재무·고용보험) 축이 통째로 `other` text_only로 새서 영구 unknown. DB 백업(`backups/cunote_full_20260627_142340.sql`)의 실제 배제 조건 27종 분해·귀속 표 + 외부 SOTA 9개 서비스 대조 포함.
   - 증거 수집은 Explore(opus)·리서치(opus) 에이전트 2개 병렬로 수행, 코드 인용은 리뷰 에이전트가 재검증함.
2. **구현계획** `docs/plans/2026-07-11-matching-dimension-expansion.md` (신규)
   - 신규 8차원(tax_compliance, credit_status, sanction, financial_health, insured_workforce, investment + 예약 premises, export_performance), P0~P7 Phase, 파일 단위 작업 목록.
   - **Fable 설계 리뷰 완료 → 전건 반영됨** (문서 §6에 기록). Critical 2건이 범위를 바꿈: ① 플래그 단위 `known_flags` 게이트 신설(미질의 플래그 false pass 차단), ② prior_award 4종(중복수혜·수료 이력) 범위 제외 — 완료 기준 20→**16/27**.
3. **CLAUDE.md** "작업 체계"에 규칙 추가: 연구 문서(plan 제외)는 한글 파일명 `YYYY-MM-DD-한글제목.md`.

## 남은 작업 (순서대로)

1. **이 세션 산출물 커밋** — 병렬 세션 변경이 섞여 있으므로 명시 스테이징만:
   ```bash
   cd /Users/ffgg/noten.works/cunote
   mkdir -p .git/stale-locks && mv .git/*.lock .git/stale-locks/ 2>/dev/null || true
   git add "docs/research/2026-07-11-공고매칭-14차원-확장-검토.md" docs/plans/2026-07-11-matching-dimension-expansion.md docs/plans/HANDOFF-2026-07-11.md CLAUDE.md && git -c user.name="coolwithyou" -c user.email="sw@ba-ton.kr" commit -m "공고매칭 14차원 확장 검토·구현계획 + 연구문서 한글 파일명 규칙"
   ```
   ⚠️ CLAUDE.md에 병렬 세션 변경이 겹쳤을 수 있음 — add 전에 `git diff CLAUDE.md`로 이 세션 변경(한글 파일명 규칙 1줄)만인지 확인.
2. **구현 착수 (사용자 승인 후)** — 계획 문서의 P0(계약·canonical 사전)부터. Phase별 Opus 서브에이전트 위임, 메인 검수(CLAUDE.md 작업 체계). LLM 사용 Phase는 P4(bizinfo 프롬프트 갱신)·P5(재추출, `ANTHROPIC_API_KEY` 필요, 비용 산정 후 실행 승인).
3. **후속 트랙(별도 계획 필요)**: prior_award 구조화(수혜 이력 구조화 + grant 자기참조 매칭), audience 게이트(P6, 독립 실행 가능).

## 검증 체크리스트

- [ ] 문서 3건 커밋됨 (병렬 세션 파일 미포함 확인)
- [ ] 구현 착수 시: 계획 문서 §5 완료 기준 6건이 판정 기준 (16/27 구조화, golden 회귀 100%, tier 층화 측정, e2e, 계약 복제 지점 일치, 문항→플래그 커버 테스트)

## 주의/함정

- **초안이 아니라 리뷰 반영본 기준으로 구현할 것.** 특히: 분해기가 중복수혜류(#8/#10/#13/#20)를 prior_award로 구조화하면 안 됨(현행 exact-match evaluator가 false pass 양산). 결격 evaluator는 `known_flags` 게이트 필수.
- **PG enum 값은 제거 불가** — P1 전에 8개 이름 최종 확정. drizzle generate가 `BEFORE 'other'`를 안 만들면 수동 편집이 기본 경로(전례 0004).
- 계약 복제 지점은 3곳이 아니라 **openapi.ts 하드코딩 5곳 포함 6~7곳** (계획 P0 표 참조).
- `packages/core` 수정 후 build 없이는 dev 서버 미반영 (메모리: core dist export).
- 워킹트리에 **병렬 세션 산출물 다수**(apps/admin/*, ServiceDataMonitor, `docs/plans/2026-07-11-matching-data-sourcing.md`, codef 문서) — `git add -A` 금지, 이 세션 파일만 명시 스테이징.
- 백그라운드 작업 없음. 리뷰 에이전트(ab713c8df826f53f4)는 종료 상태 — 후속 질의 시 새로 띄울 것.
