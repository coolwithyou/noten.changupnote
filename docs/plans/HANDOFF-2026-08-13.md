# HANDOFF — 딥분석 처리속도 트랙: CP2 최종 테스트 (2026-08-13)

> 원 계획(정본): `docs/plans/2026-08-11-deep-analysis-throughput-plan.md`
> 운용 지침: `docs/explainers/구독모델로-딥분석-돌리는-법.md` §3
> 상태 정정(2026-08-13): CP2는 repair 7/10(70%)로 실패했다. CP2-b 정적 품질 회복 구현·회귀 검증은 완료했고, 실제 v15 품질 판정은 125015 live canary 실행 승인 대기다. 현황·판정의 정본은 `docs/research/2026-08-13-딥분석-처리속도-트랙-리뷰-정리.md` §6~9다.

## 목표

lab 딥분석+Kordoc의 건당 처리 속도 개선 트랙에서 **CP2 — 프롬프트 lab-deep-v14의 repair 발생률 20% 미만**을 검증했으나 실패했다. 성공 10건 중 7건이 repair를 사용했고 첫 패스에 `unresolved_axis` 관측 최소 29건·`semantic_misattribution` 2건·`canonical_contract_invalid` 1건이 남았다. 원인 정적 재현과 v15 수정·회귀 테스트는 완료했으며, 다음 목표는 승인된 125015 단건 canary로 실제 repair/hold/의미 보존을 검증하는 것이다.

## CP2 전 완료된 것 (당시 관련 변경 커밋 완료)

| 항목 | 커밋 | 증거 |
|---|---|---|
| T0 운용값(배치 인플라이트 4 권장) runbook 반영 | `2ee696e` | 10건 검증 배치에서 건당 wall 57분→24.5분(−57%) 실측 |
| T1 Kordoc 2단 effort(round 0 medium + 거절 임계 0.85, env `APPLICATION_ROUNDTRIP_EFFORT`) | `9c80d18` | 재canary 3공고 입력 필드 손실 0건, 판정 요청당 −35% |
| T4 계측(`LabRun.primaryPasses` — 패스별 시간·validator issue 코드) | `01f40ea` | 검증 배치에서 repair 주범 = `unresolved_axis`(성공 9런 전원) 확정 |
| T4 2단계: 축 종결 규율을 첫 패스 프롬프트로 승격, **프롬프트 v13→v14** | `85c210d` | canary 2건 중 178559 무repair(v11 이후 최초). 규모 검증은 이번 CP2 |
| 품질 게이트: Fable 검수 correct 124/130 · Sonnet 감사 일치 25/26 | 산출물만 | 사이드카: `spike-out/analysis-lab/*/run-2026-08-11T1*.ai-review.*` · `*.audit.*` |
| codex 세션 별도 작업(공정 스케줄러·validator 확장·실행 스코프 타임아웃) | `9f66186` | 이 트랙과 합류된 상태 |
| 첫 패스 진단 exact count·bounded detail | `ef3a984` | 22축 unresolved와 criterion semantic fixture로 회귀 고정 |
| lab accept/repair/hold + held sentinel | `af65bf2` | unresolved-only 1회 hold, mixed issue repair, 성공 소비처 fail-closed |
| 운영 blocked 종결 + 안전한 축 교정 | `9e33f6a` | repair/worker policy/contract/runtime/typecheck GREEN |
| 프롬프트 v15/v21 보류 계약 | `3c16cf3` | 실제 충돌·누락 hold, canonical 불안 text_only 계약 테스트 GREEN |
| held terminal 상태 회귀 | `29f0491` | attempt 여유·소진 모두 `input_blocked → blocked`, 자동 retry 없음 |

미커밋: `docs/research/2026-08-10-루프와-그래프-엔지니어링-접근.md`(타 트랙 소유 — 건드리지 말 것).

## CP2 실행 기록과 남은 작업

### 1. CP2 검증 배치 — 실행 완료, 같은 명령 재실행 금지

코호트 60건은 전부 구버전(v12/v13) ok 런만 보유 → `--reanalyze-outdated` 필수.
125015(0aa42679…, 936후보·v13 딥 실패건)는 이 플래그로 **재분석 자격만 획득**하며, 코호트 순서 + `pending.slice(0, limit)` 선정에서는 32번째라 limit=10에 포함되지 않았다. 향후 실행은 `--grant-ids=0aa42679-…`로 명시해야 하며, v15 수정·회귀 검증과 별도 승인 전에 시작하지 않는다.

```bash
cd ~/noten.works/cunote
ANALYSIS_LAB_TIMEOUT_MS=900000 ANALYSIS_LAB_TRANSPORT=claude-cli ANALYSIS_LAB_MODEL=claude-opus-5 \
  APPLICATION_ROUNDTRIP_EFFORT=medium \
  pnpm lab:batch -- --reanalyze-outdated --retry-errors --with-application-roundtrip \
  --roundtrip-model=claude-opus-5 --limit=10 --concurrency=4 --max-cost-usd=65
```

실측: 10/10 성공, 건당 평균 12.23분·최대 25.63분, 배치 wall 38.03분. 명목 비용 $23.8054(구독이라 실지출 0).

### 2. repair율·primaryPasses 재현 집계 (로컬, 무비용)

```bash
cd ~/noten.works/cunote/spike-out/analysis-lab && node -e '
const fs=require("fs"),path=require("path");let n=0,r0=0,codes={};
for(const d of fs.readdirSync(".")){if(!d.includes("__"))continue;const dp=path.join(".",d);if(!fs.statSync(dp).isDirectory())continue;
for(const f of fs.readdirSync(dp)){if(!f.startsWith("run-2026-08-12T15")||!f.endsWith(".json")||f.includes(".ai-")||f.includes(".audit")||f.includes(".review")||f.includes(".confirmations"))continue;
const r=JSON.parse(fs.readFileSync(path.join(dp,f),"utf8"));if(r.promptVersion!=="lab-deep-v14"||r.error!==null)continue;
n++;if((r.primaryRepairCount??0)===0)r0++;
for(const p of r.primaryPasses??[])if(p.kind==="primary")for(const c of p.issueCodes)codes[c]=(codes[c]||0)+1;}}
console.log(JSON.stringify({성공런:n,무repair:r0,"repair율%":n?Math.round((n-r0)/n*100):null,issue코드:codes}));'
```

런 파일은 UTC 타임스탬프를 쓰므로 KST 08-13 배치의 정확한 프리픽스는 `run-2026-08-12T15`다. 위 집계는 첫 패스의 앞 20개 issue code만 세므로 최종 10건·repair 7건과 **관측 최소 issue 32건**을 재현한다. 정확한 과거 총량으로 해석하면 안 된다.

### 3. 검수·감사 레인 — CP2-b live canary 품질 게이트 전 실행 보류

```bash
ANALYSIS_LAB_TIMEOUT_MS=900000 ANALYSIS_LAB_TRANSPORT=claude-cli \
  pnpm lab:ai-review -- --model=claude-fable-5 --allow-empty --limit=10 --max-cost-usd=1000
ANALYSIS_LAB_TIMEOUT_MS=900000 ANALYSIS_LAB_TRANSPORT=claude-cli \
  pnpm lab:ai-audit -- --model=claude-sonnet-5 --create-missing --limit=10 --max-cost-usd=1000
```

전회(08-12) 감사에서 용인시(c8757731…) 1건이 일시 DB 오류로 실패했다 — CP2-b live canary 품질 게이트 통과 후 감사를 실행하면 자동 재시도 대상이 된다.
감사 불일치 1항목(전회분)은 `/dev/analysis-lab` 감사 UI에서 사람 판정 필요(dev 서버는 사용자가 직접 기동).

### 4. 결과 기록

- [x] 계획 문서 §5 CP2에 repair율·건당 wall 실측을 기입하고 §4 표를 실측으로 갱신했다.
- 실측 repair율은 7/10(70%)로 CP2 실패가 확정됐다. 첫 패스 `unresolved_axis` 관측 최소 29건의 진단 손실과 보류/교정 계약 충돌을 정적 재현했고 v15/v21 및 회귀 테스트까지 구현했다. 실제 개선을 확인하기 전까지 추가 배치·2차 교정을 자동으로 실행하지 않는다.
- 커밋: 한국어 제목, 명시 스테이징(`git add <파일들> && git commit`), **Co-Authored-By 금지**, `git add -A` 금지.

## 검증 체크리스트

- [x] 배치 성공 ≥9/10, window-exhausted·timeout 실패 0 — **10/10 성공·오류 0**
- [x] **repair율 <20% 게이트 실패 확정** — 7/10=70%
- [ ] 125015(0aa42679…) 딥분석 성공 — CP2 미편입, 00:58 명시 타깃 시도는 런 미저장으로 종료
- [x] 건당 wall 평균 ≤25분 유지 — 평균 12.23분·최대 25.63분·배치 wall 38.03분
- [x] Kordoc 새 roundtrip 산출물 9/9의 `requestedEffort: "medium"`·`transport: "claude-cli"` 확인
- [ ] 감사 실패 1건(용인시) 재시도·신규 검수·감사 — repair 게이트 실패로 보류
- [x] CP2-b 정적 구현·contract/quality/runtime/typecheck 검증
- [x] production build·promotion protection·최종 Claude 상태 전이 리뷰(APPROVE, P0/P1 0)
- [ ] 승인 후 125015 v15 단건 canary에서 publishable/held/repair/new-issue 지표 확인

## 주의 / 함정 (이 세션에서 실측된 것)

1. **`APPLICATION_ROUNDTRIP_EFFORT=medium`을 절대 빼먹지 말 것** — lab:batch는 Kordoc을 플래그와 무관하게 **항상** 딥분석과 함께 실행한다(형제 실행 불변식). 누락 시 Kordoc이 기본 effort로 돌아 배치가 2~3배 느려진다.
2. **`--concurrency=4` 유지** — 전역 `claude -p` 상한이 4라 인플라이트를 늘려도 처리량은 같고 건당 완료만 3배 느려진다(08-11 실측).
3. **장시간 배치는 로컬 중단에 취약** — 08-12 canary가 머신 절전/세션 종료로 killed됐다. 터미널에서 직접 실행하거나 재개를 전제할 것. 배치는 체크포인트-재개 가능: **같은 명령 재실행이면 완료분은 자동 스킵되고 이어서 돈다.**
4. 웹 배치(`/dev/analysis-lab` 배치 운영 탭)와 CLI 배치를 **동시에 실행 금지**. 다른 codex/claude 세션의 lab 명령과도 겹치지 말 것.
5. `ANALYSIS_LAB_TIMEOUT_MS=900000` 필수 — 기본 540s는 큐 대기 포함이라 대량 request_timeout 위험.
6. **P1 publisher 보호는 완료** — `c79e2c0`에서 publisher 가드·수동 CLI 락·재판별을 구현했고 `verify:promotion-protection`이 test 체인에 배선돼 있다. P1 부재를 이유로 한 전면 금지는 해제됐지만 release·write·confirm·품질 게이트는 유지한다.
7. 배치 요약의 비용은 명목값(API 환산)이다 — 구독 경로 실지출 0, `--max-cost-usd`는 폭주 방지 상한일 뿐.
