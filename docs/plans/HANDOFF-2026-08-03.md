# HANDOFF 2026-08-03 — 구독모델 딥분석 트랙 (완료·운용 가능)

## 목표

로컬 대량 딥분석·블라인드 감사를 API 토큰 대신 **Claude Max 구독(claude CLI)**으로 실행한다. 운영(API) 경로는 불변. 원 계획: `docs/plans/2026-08-02-claude-cli-transport-for-deep-analysis.md` (🟢), 채택 판정: `docs/research/2026-08-02-구독전환-AB-섀도-비교.md`, 운용 안내: `docs/explainers/구독모델로-딥분석-돌리는-법.md`.

## 완료된 것 (전부 커밋됨, main)

| 커밋 | 내용 | 검증 증거 |
|---|---|---|
| 39bff47 | 타당성 검토 + 구현 계획 문서 | 적대 검증 14건 반영 |
| 463a7ac | Phase 1: `analysis-lab/claude-cli-transport.ts` + 테스트 + `lab:transport:test` | 유닛 8/8, tsc 0, 실측 5종(opus-5 가용·api_error_status 보존·stdin·`--safe-mode` 격리·감사 스키마 강제) |
| e1621e0 | Phase 2: 배선 8파일 + usage 입력 보정(cache_creation 합산) | 회귀 7종 PASS·격리 rg 0건·통합 스모크 3종(API 무영향 65s / 구독 92k자 240s / 감사 체인 E2E `aiAuditTransport` 기록) |
| 8bcf046 | Phase 3: A/B 섀도 비교 리포트 | 원문 대조 59건: 추출 CLI 30:3, 감사 CLI 19:1, 환각 0, spanVerified 100%. 동일 모델 대조군(sonnet 양측 16.7%)으로 수치 게이트 불성립 증명 |
| 29b830a | Phase 4: 채택 확정 — .env.example ANALYSIS_LAB_* 9종 문서화, 검수 가이드 §5에 결함 패턴 2종 등재 | DoD 전 항목 체크 |
| 9d2db03 | 운용 안내서(explainer) + README 등재 | 문서 내 명령 전수 코드 대조(--limit 등) |

백그라운드 작업: **없음** (전부 종료). 세션 스크래치의 A/B 판정 원본은 휘발성 — 필요 수치는 리포트에 박제됨.

## 운용 명령 (즉시 사용 가능)

```bash
# 대량 배치 (구독·opus-5) — 끊겨도 같은 명령 재실행이 곧 재개
ANALYSIS_LAB_TIMEOUT_MS=900000 ANALYSIS_LAB_TRANSPORT=claude-cli ANALYSIS_LAB_MODEL=claude-opus-5 \
  pnpm lab:batch -- --limit=30

# 블라인드 감사 (구독·sonnet-5)
ANALYSIS_LAB_TRANSPORT=claude-cli pnpm lab:ai-audit

# API 복귀 = env 제거 후 실행 (코드 롤백 불필요)
```

인증: claude CLI가 Keychain OAuth(Max)로 로그인돼 있어야 함(`sw@noten.im`). DB·R2는 루트 `.env`/`.env.local`(lab CLI가 자동 로드).

## 남은 작업 (2026-08-03 전부 종결 — 아래 "마감 실측" 참조)

1. ~~**[사용자 동반, 1회] dev 웹 라우트 CLI 스폰 확인**~~ → **완료(2026-08-03)**. Next dev 프로세스에서 claude 스폰·Keychain OAuth 정상(4/4 spanVerified·130s). env 전달은 셸 프리픽스가 아니라 **`apps/web/.env.development.local`로 고정**(프리픽스 방식은 재기동 2회 모두 미전달 실측 — 사용자 기동 경로에서 유실). 이 파일은 Next dev만 읽음(lab CLI는 루트 .env/.env.local만 로드, 프로덕션 무관). 웹 레인 API 복귀 = 파일 삭제 후 재기동.
2. **[선택·보류 확정(2026-08-03 사용자)] Phase 5: ai-review·confirmations 레인 구독 확대** — 결정 ① 유보 취지 유지. 착수 조건(§8-2 소규모 일치율 검증)과 배선 방법(계획 §5 #2·#4)은 그대로 유효.
3. ~~**[운용] 첫 실전 배치**~~ → **완료(2026-08-03)**. `--reanalyze-outdated`로 실행 — 구버전 20건 중 18건 기간 정책 스킵(마감), 실 대상 1건 성공(124.8s·transport claude-cli·opus-5·명목 $0.31·실지출 $0). 마감 공고가 대부분이라 대량 배치는 **신규 코호트 편입 시**가 실사용처.

## 2026-08-03 후속 세션 재확인 (실측)

- **무비용 체크리스트 3종 통과**: `lab:transport:test` 전부 통과 · 격리 rg 2종(deep-analysis 0건, import처 = 계획 §5 진입점 목록과 정확히 일치) · claude CLI **2.1.219**(shim 계약 버전 일치, 드리프트 없음). 스모크 2종(비용 발생)은 드리프트 징후가 없어 생략.
- **잔여 3번 "첫 실전 배치"는 현재 no-op**: `lab:batch -- --dry-run` 실측 — 코호트(expansion-s1, 30건) 전부 ok 런 보유(현행 v5 10 · 구버전만 20), 이번 실행 대상 **0건**. 구버전 20건의 v5 재분석은 `--reanalyze-outdated`를 명시해야 하며(우발 재분석 가드), 이는 구독이라 $0이지만 **하류 fable-5 검수 풀이 +20건** 되는 별도 결정 사안.
- **A/B 부산물 검수 대기 정량화**: `lab:ai-review -- --dry-run` — 대기 8건(전부 2026-08-02 A/B 신규 런), 예상 비용 fable-5 ≈ **$3.93**(sonnet-5 기준 $0.79). 제외(사람 검수 보유) 3 · 보류(현행 ok 런 없음) 19.
- **잔여 1번 절차 확정**: dev 서버는 사용자가 `ANALYSIS_LAB_TRANSPORT=claude-cli ANALYSIS_LAB_MODEL=claude-opus-5 ANALYSIS_LAB_TIMEOUT_MS=900000 pnpm dev:web`로 기동(기본 포트 4010, dev-web.mjs가 env를 next 프로세스로 상속) → `http://127.0.0.1:4010/dev/analysis-lab`에서 분석 1회 → `spike-out/analysis-lab/<source>__<sourceId>/<runId>.json`의 `transport: "claude-cli"` 확인.

## 2026-08-03 마감 실측 (같은 날 후속 세션 — 남은 작업 전부 종결)

- **잔여 ① dev 웹 라우트 CLI 스폰 = 성공**: `POST /api/dev/analysis-lab/analyze` 실측 3회 — 프리픽스 기동 2회는 next-server 프로세스 env에 ANALYSIS_LAB_* 부재로 `transport:"api"`(ps eww로 확정, 코드 문제 아님·기동 경로에서 env 유실), **`apps/web/.env.development.local` 생성 후 재기동 없이 핫 리로드로 `transport:"claude-cli"`·`model:"claude-opus-5"`·4/4 spanVerified·130.2s** (run-2026-08-03T050535.702Z-ce68e4). Next 프로세스의 claude PATH·Keychain 접근 정상 — §5 "라우트 api 강제" 축소 불필요.
- **잔여 ③ 첫 실전 배치 = 완료**: `--reanalyze-outdated` 20건 중 **기간 정책 스킵 18(전부 마감)** · 현행 보유 스킵 11 · **실행 1건 성공**(소담스퀘어, 124.8s, claude-cli·opus-5, 명목 $0.31·실지출 $0, 런 파일 transport 확인). 코호트가 7월 공고라 대부분 마감 — 대량 배치의 실사용처는 신규 코호트 편입 시.
- **A/B 부산물 검수 8건 처리(사용자 확정 "지금 실행")**: fable-5 API — **완료 7 · 판정 거부(refusal) 1(ARPA-H, 설계된 처리) · 원문 드리프트 0 · 총 $2.02**(추정 $3.93의 51%).
- **Phase 5 = 보류(사용자 확정)**, 구버전 재분석 = 실행(사용자 확정, 위 결과).
- **검수 풀 잔여 주의 (fable-5 기준 3건 ≈ $1.11)**: ARPA-H(refusal은 사이드카가 있어도 풀에 남음 — 재실행 시 자동 재시도, `--force` 불필요) + 오늘 신규 v5 런 2건(웹 확인용 4a7aa057 · 배치 c418c645). **함정**: 검수 사이드카는 모델별(`.ai-review.<model>.json`)이라 기본 모드(sonnet-5)로 돌리면 fable-5 완료분 7건을 못 보고 10건 전부 재검수한다 — 반드시 `--model=claude-fable-5`로 실행할 것.

## 검증 체크리스트 (재확인용)

- [ ] `pnpm lab:transport:test` 통과 (CLI 버전 드리프트 의심 시 1순위 진단)
- [ ] env 미설정 `pnpm lab:smoke` → `transport: api` (운영 무영향)
- [ ] 구독 스모크 → `transport: claude-cli`·`model: claude-opus-5`·error null
- [ ] `rg -l "claude-cli-transport|ANALYSIS_LAB_TRANSPORT" apps/web/src/lib/server/deep-analysis/` → 0건 (운영 격리)

## 주의/함정 (이번 세션 실측)

- **`claude --bare` 금지** — OAuth를 안 읽고 API 키 과금으로 전환됨. shim 고정 플래그는 `--safe-mode`(사용자 스코프 CLAUDE.md 차단 + OAuth 유지, 실측 확정).
- **윈도 소진 시**: batch가 `[CLAUDE_CLI_WINDOW_EXHAUSTED]` 마커를 감지해 신규 착수를 스스로 중단. 리셋 후 같은 명령 재실행 — 단 소진 시점에 이미 착수됐던 소수 error 런만 `--retry-errors` 필요(이 플래그는 무관한 실패 런도 재실행하니 남발 금지).
- **A/B 부산물 런 주의**: spike-out에 v5 신규 런 23건(API 10·CLI 9·sonnet 4)이 쌓임. `lab:ai-review` 기본 모드는 "사람 검수 없는 ok 런"을 대상으로 잡으므로 **다음 검수 배치에서 이들이 대상 풀에 들어가 fable-5 API 비용 발생** — 의도 확인 후 `--grant-ids`로 좁히거나 수용할 것.
- **CLI 명목 비용은 근사치**: 입력은 cache_creation 합산 보정으로 회복했지만 출력에 추론 토큰이 포함돼 API 대비 과대(보수적 방향). costUsd는 게이트 잣대일 뿐 실지출 0.
- **딥분석 재실행은 비결정적** — essence 집합 완전 일치 기준의 축 일치율은 동일 모델 재실행 쌍도 16.7%(실측). 레인 품질 비교는 반드시 원문 대조 판정으로 할 것(수치 일치율로 판정하면 오판).
- **병렬 세션 변경 불가침**: 워킹트리의 apply-workspace·application-roundtrip·routePolicy·applyReconciledFields·package.json(`test:apply-workspace` hunk)은 다른 세션 소유. `git add -A` 금지, 명시 스테이징만.
- 검수 가이드(`docs/research/2026-07-18-공모딥분석-검수판정-가이드.md`)를 수정하면 이후 ai-review/audit 프롬프트가 바뀜(§0·1·2·5가 rubric으로 주입됨) — guideSha256은 기록용일 뿐 차단 없음.

## 새 세션 시작 프롬프트

> `docs/plans/HANDOFF-2026-08-03.md 읽고 남은 작업 이어서 진행해줘`
