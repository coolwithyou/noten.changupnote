# 잔여 개선 실행 원장

2026-09-09. 시작 소스 `d935dcc`. 사용자 요청: 직전 잔여 작업 목록 전체 진행.
이 문서는 [순차 개선 계획](2026-09-07-분석-매칭-정합성-순차개선.md)의 후속 실행 상태를 기록한다.
기존 사용자 변경 `apps/web/next-env.d.ts`는 작업·커밋 범위에서 제외한다.
구현은 지정 sol/xhigh, 주 에이전트는 설계 피드백·독립 감사·인수·출고를 맡는다.

## 작업 목록

| ID | 범위 | 현재 상태 | 완료 증거 |
| --- | --- | --- | --- |
| R1 | 매칭/작성 준비도 분리, 검수된 required text-only 질문 진입 | R1-A/B 커밋·집계 통과, 질문 자연 UI 통과; 운영 작성 인수 별도 | 6c69142, 3992961 및 아래 검증 |
| R2 | release별 manifest 조회/검증 재사용, snapshot 무결성, 성능 계측 | 구현·집계·실제 PG 동시성 통과, 실측 결과 동일, production 반영 | 아래 최종 gate/실측 및 R9 production DB 읽기; 운영 부하 실측 별도 |
| R3 | 기존 검수 자산 현행 조사·분류·공급 회복 | 승인 47건 분류·서비스 85건 kind 대조·21건 projection 재검증; 교정/공급 미실시 | 10d46fd 및 아래 진단; 독립 검수와 구분 |
| R4 | 대량 분석 계측·오류 감소·작성 재사용·lease 장애 회귀 | lease·실행 관측·primary-only exact 재사용 구현·최종 집계 통과 | 12c218e, 28627c3 및 아래 오프라인 회귀; 새 live 효율 실측 별도 |
| R5 | 회사 공통/개인 정보 의미와 프로필 패널 재진입 UX | 코드·회귀·격리 실제 인수 통과 | 28e028d + R8 editor 저장/재조회 뒤 패널 닫힘 유지 |
| R6 | 원천 정정 사용자→관리자→공식 갱신→재평가 실제 UI | 격리 자연 화면 단일 실행·독립 DB 인수 통과 | 아래 WBZqSh 최종 증거; 외부 기관 갱신과 구분 |
| R7 | 실제 기관 양식 저장·재열기·다운로드, R2/OAuth 인수 | 원본 2양식의 실제 R2 저장·격리 DB head 재조회·재열기 통과 | OAuth·제품 다운로드 클릭/시각적 보존 인수 별도 |
| R8 | 자연 회사 전환 지연 응답 인수, 재사용 가능한 브라우저 회귀 | 격리 실제 브라우저·DB 대조 통과, 운영 인수 별도 | 아래 R1-B 소스 실제 인수 및 분리된 component 7-case |
| R9 | migration 0078~0083·구버전 writer·기능 플래그·exact SHA 배포 | 실제 6개 migration·web/ops b6e1c5f production 반영; 캐시 68쌍 저장·독립 DB 대조 통과 | 아래 적용 receipt·배포·스모크·캐시 사후 검증 |
| R10 | 실제 release 승인/승격/노출/rollback·운영 측정 | R3/R9 이후 | 미확보 |
| R11 | v2 확인질문 자동 초안 생성·관리자 편집 GUI | 코드·집계·격리 실제 화면 인수 통과, ops production 반영 | 0c5c2ad 및 아래 다운로드/재검증/재열기·R9 배포; 운영 인증 인수 별도 |
| R12 | 반복 지역·사업장·기준일 조건 구조화 | 좁은 premises-v1 구현·최종 집계·실제 격리 브라우저/DB 통과, web/ops production 반영 | b6e1c5f 및 아래 최종 R12 증거; 시군구·이전 약속 등 확장 계약은 미구현 |

## 권한과 검증 경계

- 전체 목록의 구현·테스트·환경 구성·배포 작업을 진행하되, 새 live 모델 실행은 현행
  AGENTS의 exact manifest 승인 후 grant 계약을 유지한다. 미승인 모델 호출은 하지 않는다.
- 분석 자산의 실제 승격은 조사로 exact 대상과 현재 material binding을 확정한 뒤 승인 범위를
  대조한다. 과거 승인이나 이번 일반 개선 요청만으로 임의 cohort를 추가하지 않는다.
- 운영 worker `observe_only`를 유지한다. Cloudflare 접근 정책을 임의로 완화하지 않는다.
- 장기 개발 서버는 시작하지 않는다. 기존 실행 포트를 먼저 확인하고, 허용된 격리 인수 도구의
  유한 production 인수와 개발 서버를 구분한다.
- 코드/자동 테스트/격리 실제 인수/외부 계정 인수/DB 적용/배포를 각각 기록한다.
- 테스트용 로그인 성공을 실제 이메일 소유권이나 외부 OAuth 성공으로 보고하지 않는다.

## 기록

- 시작점에서 Git 변경은 사용자 `apps/web/next-env.d.ts` 한 개뿐임을 확인했다.
- R1 구현을 기존 구현자에게 배정했다. 주 에이전트는 후속 자료·원격 접근·회귀 기준을 조사한다.

### 원격 읽기 전용 재확인 — 2026-09-09 05시 KST

- Vercel 정본 토큰으로 `whoami=noten-dev`, web/ops 모두 `NOTEN` 소유와 지정 project ID,
  Root Directory `apps/web`/`apps/admin`을 확인했다. 배포는 아직 하지 않았다.
- web production target은 `dpl_6wpyJ8fapFd9KvcYQJG8RXkbnfbY`, 소스 메타데이터
  `44fda4d66765ef46affed70cc2bccf8ebaca511a`, alias `changupnote.com`이다.
  ops target은 `dpl_86WeBcgWLTRGuzbE3C3iRmG5cHXW`, alias `ops.changupnote.com`이며
  조회 응답의 git SHA는 없었다. READY는 실제 인증 화면 성공 증거가 아니다.
- 서비스 DB는 단일 연결·read-only transaction·15초 statement timeout으로 조회했다.
  runtime은 `paused`, generation 383, local owner/expiry 없음. worker 설정이나 active job 전체를
  확인한 증거로 확대하지 않는다.
- approved deep-v31 release의 47개 item은 전부 미적용이며, 현행 KST 모집기간의 open/visible은
  39개다. source/attachment/review 재검증 전이므로 39개가 즉시 승격 가능한 것은 아니다.
- 전체 open/visible은 2,030개, 그중 null 기간을 허용한 현재 날짜 조건은 486개다.
  분석용 비중복 재고가 아니라 원천 상태 집계이며 모집단을 서로 혼용하지 않는다.
- migration 원장은 79행, 현행 로컬 journal은 83개다. 개수만 보고 0078까지 적용되었다고
  판단할 수 없다. SQL 파일 hash 대조에서 0078~0082가 미일치이며 0005/0024의 역사 hash도
  다르다. 원장에 현행 SQL과 매핑되지 않는 hash 3개가 있어 출고 전 역사/실제 schema 대조가
  필요하다. 이 상태에서 `db:push`나 과거 SQL의 일괄 재실행은 하지 않는다.
- Supabase CLI 조회에서는 창업노트 프로젝트가 나타나지 않았다. 타 고객 프로젝트는 사용하지
  않는다. `.env.uat.local`은 없으며, R2 전용 관리 토큰의 bucket list는 HTTP 403/code 10000이다.
  운영 객체 읽기 자격이나 일반 DNS 토큰을 새 격리 버킷 생성 권한으로 해석하지 않는다.

확인에 사용한 공식 문서: [Vercel 프로젝트 조회](https://vercel.com/docs/cli/project),
[PostgreSQL 읽기 전용·격리](https://www.postgresql.org/docs/18/sql-set-transaction.html),
[Cloudflare R2 버킷 목록](https://developers.cloudflare.com/api/resources/r2/subresources/buckets/methods/list/).

### R3 사전 조사 — 승인 후 미적용 47건

정확 대상은 `deep-deep-v31-50-20260902-r1-20260902T143920Z-4787a6b6`의 source artifact
47개다. 현재 DB 승인 상태와 개수를 확인하고, 원본 launch receipt/run 파일 SHA를 대조한 뒤
운영 read-only 연결과 기존 R2 객체 GET으로 현재 input/attachment/source revision을 재조립했다.
현행 변환의 오류·누락 여부도 오프라인으로 확인했다.

| 분류 | 건수 | 후속 처리 |
| --- | --- | --- |
| 마감 또는 비노출 | 8 | 현행 공급에서 제외; 역사 자산은 보존 |
| 열린 공고지만 원천/입력/첨부 결속 변경 | 18 | 변경 원천을 대조하고 재검수/재분석 필요성을 결정 |
| 결속 유지, 현행 재검증 증거 필요 | 21 | 모델 재호출 없이 현행 projection·독립 검수 결속을 확인할 우선 후보 |

47건 모두 신규 primary matching projection 증거가 원 receipt에 없었다. 원 receipt를 고치거나
21건을 곧바로 ready로 기록하지 않는다. 이번 진단은 독립 검수 전수 재감사나 release 승인,
서비스 승격을 대체하지 않는다. 조회 중 apply_start를 다시 검증하는 최종 release gate도 남아 있다.

- 진단 종료: 2026-09-09 05:22:42 KST, exit 0, 모델 호출 0, 서비스 쓰기 0.
- 코드 `/tmp/cunote-remaining-supply-audit.mts`, SHA
  `af78b41566e6cf6002918bbae2148a298821443f07946e38b215441bb8669c27`.
- 47행 결과 `/tmp/cunote-remaining-supply-audit.log`, SHA
  `f58967210b22d3e1078a89f1003b157e0d437a0d7cfba268a1449a5b505d24dc`.
- `/tmp`는 로컬 진단 영수증이며 지속 CI 산출물이 아니다. 원천 변화가 있으면 다시 조사한다.

### 역사 migration 조사

원장 미매핑 3개 중 두 개는 Git 역사에서 정확 SQL hash를 찾았다.
`0005_certain_korg.sql`(940f664)의 password_hash 추가와
`0047_outgoing_layla_miller.sql`(3a06cba)의 관리자 pipeline SQL이다.
현행 0005 insight table 및 users.password_hash는 실제 DB에 모두 존재한다.
0024 benefits 컬럼도 있지만 현행 0024에서 선언한 인덱스 6개의 이름은 DB에 없다.
남은 역사 hash와 인덱스의 동등 정의를 확인한 뒤 필요한 전진 migration을 판단한다.
원장 hash를 새 값으로 덮거나 역사 SQL을 재실행해 일치시켜서는 안 된다.

후속 대조에서 마지막 `188f5263…`는 현행 0024 첫 문장과 LF만 있는
`ALTER TABLE "grants" ADD COLUMN "benefits" jsonb;`의 SHA와 정확히 일치했다.
DB `grants`/`grant_criteria` 인덱스 전수 정의에도 현행 0024의 인덱스 6개와 동등한 정의는
없었다. 세 역사 hash의 출처는 모두 식별했으며, 0078~0082의 새 migration 적용과 인덱스
성능 보완은 별도 판단한다. 적용 전후 검증은 migration 행 개수가 아니라 exact hash와 실제
schema/정책을 비교해야 한다.

### R1 독립 감사 — 원본 13건과 새 상태 계약

- 원 deep-v34 두 receipt `53a7b8db…`/`c2b245df…`와 참조 held run 13개의 실제 파일 SHA를
  검증했다. 원본 primary는 publishable 13, matching ready 6/conditional 7,
  application review_required 13이며 보류 사유는 모두 `field_analysis_held`였다.
- 해당 두 receipt의 independent-review manifest packet에서는 exact 원 run 13개 모두
  미포함이었다. 다른 repair run의 검수와 섞지 않으며, 기능 분리만으로 원본 13건을 승격하지 않는다.
- root가 신규 상태 normalizer의 `matching ready + deferred/unverified`,
  `authoring ready + held/unverified` 조합 허용 문제를 발견했다. 구현자가 보완한 뒤 독립
  `/tmp/cunote-root-feature-audit.mts`의 모순 4조합 거부 검사를 통과했다.
  이 중간 검사는 R1 전체 계약·제품 흐름 완료를 뜻하지 않는다.
- 현행 `promotion-release-cli.ts`는 `--require-kordoc`을 RHWP 전환으로 이미 거부한다.
  계획의 오래된 문구와 다르므로 폐기 옵션을 부활시키지 않는다. 과거 Kordoc-bearing release의
  엄격한 evidence 검증은 유지하고, 새로운 matching-only release와 분리한다.

### R2 현행 읽기 기준선

`/tmp/cunote-root-serving-baseline.mts`에서 단일 연결의 read-only 설정으로 실제
`listActiveGrants`를 같은 asOf에서 3회 호출했다. 측정 대상 소스 3개의 전후 SHA는 동일했다.

- 공고 85개·조건 502개, 3회 결과 SHA 동일.
- 첫 프로세스 조회 5,400ms, 후속 조회 4,469ms/4,401ms. DB buffer가 cold였다는 의미나 p95는 아니다.
- applied item 120개·고유 release 9개. item 조인의 manifest JSON 부분 40,573,725 bytes,
  고유 release별 같은 부분 1,893,100 bytes. 이는 SQL JSON의 텍스트 크기이며 전체 HTTP 크기나
  네트워크 압축 후 실전송량이 아니다.
- 로그 `/tmp/cunote-root-serving-baseline.log`. 변경 후 동일 조건의 비교가 남아 있다.

### R4 사전 안전 감사 — 확정 lease 상실

실제 launch가 사용하는 `createDeepRepairLiveRuntimeAuthority`에 오프라인 client와 수동
갱신 timer를 넣어, 갱신 응답이 `paused/generation 변경/owner 없음`으로 반환되는 경우를
재현했다. 현행은 일반 일시 오류와 같이 2회 유예하여 `abortedAfterKnownLoss=false`,
`returned=true`였다. root assertion은 exit 1로 실패했다. 모델·DB 호출은 0이다.
재현 도구: `/tmp/cunote-root-lease-audit.mts`.

확정 권한 상실의 즉시 abort와 종료 중 pending renewal 경합 검사를 R1 다음에 앞당긴다.
이 결함을 닫기 전 live 모델 실행이나 대량 확대는 하지 않는다.

### R3 추가 감사 — 현행 서비스 조건 종류

실제 repository가 반환한 85개 공고의 원본 run 파일 SHA를 release source artifact와 대조했다.
DB 조건은 UUID이므로 ID를 run index로 해석하지 않았다. sealed promotion plan의
dimension/operator/value/sourceSpan으로 유일하게 결속한 뒤 plan의 원본 index로 비교했다.
502개 조건 모두 매핑되었고 누락·다중 매핑은 0이다.

- 30개 공고·36개 조건에서 원본 run과 DB의 kind가 다름: required→exclusion 32,
  preferred→exclusion 4. 모두 other 축이다.
- 이 결과는 현재 원문의 정답 전수 검수와 다르지만, 코드의 kind 보존 수정만으로 이미 적용된
  DB 조건이 교정되지는 않는다는 직접 증거다. 공급 회복에서 해당 exact 30개를 우선 조사한다.
- 원본/질문/답변을 보존하는 검증된 상위 release 경로가 필요하다. DB kind 직접 덮어쓰기나
  현재 원장과 불변 receipt를 수정하는 방식은 사용하지 않는다.
- 첫 도구는 UUID에서 run index를 찾으려 해 비교 0개였다. 이를 통과 증거로 세지 않았다.
  위 502개 비교는 수정한 v2 도구 실행의 결과다.
- 코드 `/tmp/cunote-root-serving-kind-audit.mts`, SHA
  `1d1b3c1146b08bb02de0de8681ecb166c7f62b764c7333c37f8a34761dc15751`.
- 결과 `/tmp/cunote-root-serving-kind-audit-v2.log`, SHA
  `33bc079be2679eabef0ba7ae5efc326662226e3cf3bfd6353ab341bcfc43743b`.
  모델 호출 0, 서비스 쓰기 0. 현재 입력 변화 시 다시 대조해야 한다.

### R4 lease 안전 수정과 독립 감사

지정 sol/xhigh 구현자가 runtime/policy 및 두 회귀 파일만 수정했다.

- DB의 확정 owner/generation/mode 상실과 typed CAS 충돌은 첫 응답에서 중단한다.
  일반 일시 갱신 오류는 기존 연속 2회 실패 정책을 유지한다.
- 성공 반환 전에 진행 중인 갱신을 기다리고 fatal/abort를 다시 확인한다.
- DB가 반환한 만료 시각으로 deadline을 두어 응답이 오지 않는 갱신도 중단한다.
  늦게 도착한 정상 응답이 이미 만료된 권한을 부활시키지 못한다.
- root는 첫 수정에서 timer callback이 지연되면 만료 뒤 성공할 수 있음을 추가 재현했다.
  이를 보완해 timer와 별개로 확정 만료 시각을 갱신 응답 수락 전·성공 반환 전에 비교한다.
- 외부 취소나 만료 이후 새 renew를 시작하지 않으며, 이미 생성된 promise의 뒤늦은 reject도
  관측한다. 정리는 최초 취득한 owner/generation으로만 실행한다.
- root 두 독립 재현은 최종 `returned=false`, release 1회로 통과했다.
  `/tmp/cunote-root-lease-audit.mts`, `/tmp/cunote-root-lease-deadline-audit.mts`.
- 구현자 관련 검증: `lab:experiment:test`, `lab:batch-runner:test`,
  `test:deep-analysis-runtime-control`, `lab:launch:test` PASS.
  공용 R1 수정이 진행 중이므로 최종 출고 소스의 집계 gate는 별도로 남는다.

이는 실제 DB lease 장애를 주입하거나 모델을 재실행한 증거가 아니다. live 분석·worker 모드·
서비스 DB는 변경하지 않았다. 일반 batch의 다른 실행 경로까지 모두 수정했다고 확대하지 않는다.

### R1/R5 중간 독립 감사

- root core source 검사 15개 PASS: 검수된 human/launch 질문 결속만 `needs_profile_input`으로
  열고 자격은 conditional로 유지했다. 결속 부재·legacy·다른 조건·잘못된 scope/형식/검수·
  원천 미확인·미검수 조건·근거 부재·source dispute·고위험·첨부 불완전은 계속 보류했다.
  도구 `/tmp/cunote-root-matching-binding-audit.mts`. 제품 loader/실제 화면 인수와는 별개다.
- R5는 한 mount 내 회사별 최초 정상 응답만 자동 프로필 진입을 결정한다. 이후 재조회는
  닫은 패널을 다시 열지 않고 명시적 버튼/해시는 계속 열린다. 실제 회사 선택 UI의 reload를
  넘어서 방문 이력을 영속한다는 뜻은 아니다.
- 안내는 공식·공유 정보를 포함한 profileView 전체가 아니라 **여기서 직접 입력한 답변**의
  개인 계정 저장/비공유 범위만 설명한다. 기존 데이터 공유·권한 정책은 변경하지 않았다.

### R12 빈도 탐색의 범위

관측된 kind 차이 36개에 한정해 원본 dimension은 premises 24·export_performance 12였다.
원본 근거/메모 문자열의 겹침 허용 탐색에서는 지역 표현 26, 본사/공장 등 대안 사업장 표현 12,
이전/입주예정 표현 5, 공고일/신청일/전년도 등 기준일 표현 12를 찾았다.
이는 서비스 502개 조건 전체의 빈도나 자동 정답 분류가 아니다. 현재 시도 코드만 지원하는
지역 필드에 시군구·시설 유형·미래 이전 약속을 뭉뚱그려 저장하지 않는다. 좁은 구조화 범위는
원문 기대값·회사 사실의 출처/기준일을 분리하는 후속 회귀를 먼저 마련해 정한다.

### R3-A 신청기간 admission 보완

- `loadCurrentGrantEvidence`는 마감일만 검사하고 시작일을 놓치고 있었다. 기존 공용
  `notice-period`의 KST 달력일 규칙을 사용하는 pure 판정으로 교체했다.
- 미래 시작·지난 마감·역전 기간·invalid 날짜/현재 시각은 보류하며 시작/마감 당일은 포함한다.
  기간 null은 기존 미지정 정책을 유지한다. K-Startup 원천의 모집종료 표시는 계속 우선 차단한다.
- 문자열/Date 입력은 기존 저장 규약(달력 날짜 또는 해당 날짜의 UTC 자정)으로 제한한다.
  root가 2026-09-09 06:02 KST 운영 read-only 조회에서 open/visible 2,030개를 확인한 결과,
  비자정 시작일 0·마감일 0·역전 기간 0이었다. 이 조사 집합에는 형식 강화로 인한 추가 보류가 없다.
  `/tmp/cunote-root-date-storage-audit.mts`, 모델/서비스 쓰기 0.
- root 직접 `notice-period.test.ts`, `deep-repair-promotion.test.ts` PASS, scoped diff 검사 PASS.
  전체 release suite/typecheck는 아직 동시 R1 테스트 수정의 영향을 받으므로 최종 집계 후 확정한다.

### R9 외부 인증 경계 추가 확인

GCP `cunote-codex-dev`의 base account `sw@noten.im`, project `changupnote-com`, 전용
impersonation 서비스 계정 설정은 맞다. 토큰 발급은 대화형 재인증 요구로 실패했다. 다른 계정이나
JSON key로 우회하지 않았고 토큰은 출력하지 않았다. 사용자에게 재인증을 비동기 요청했으며,
현재 Cloud Run worker 상태/배포는 미확인이다. Vercel 인증 성공과 이 상태를 혼용하지 않는다.

### R1-A 매칭·작성 준비도 분리 — 고정 소스 검증

- 매칭 사용 가능성과 신청서 작성 준비도를 별도로 계산하고, 작성 보류만으로 검수된 매칭
  결과를 잃지 않도록 launch/status/release 계약을 연결했다. 실패·미검수·원천 불일치는 계속 보류한다.
- 사용자 확인 질문은 현재 serving run·원천 SHA·검수된 v2 계약이 결속된 경우에만 core의
  `needs_profile_input`으로 노출한다. 자격 판정은 conditional로 유지하며 ruleset은 v13이다.
- root 감사에서 중복 option 값, 음수 criterion index, 정규화가 숨기는 잘못된 네 번째 option,
  공백 option 값의 누락을 발견해 수정했다. 기존 답변 읽기 호환성과 신규 질문 admission을 분리했다.
- 27개 소스 manifest `/tmp/cunote-matching-r1a-source.manifest` SHA
  `6c45339b4d54d2b43ac44de881845ba4827eba7bfe8d8e0f7167c82c61549e35`를 집계 전후 재검증했다.
- 구현자 관련 gate(core build, web typecheck, matching-unit, launch, release, promote,
  service-data) PASS. `/tmp/cunote-matching-r1a-related.log` SHA
  `2dd0e00804cdf7dc5ded676f96f437078c2d455f79de296e71b5e6c9437e6a4f`.
- root 독립 readiness/core/question 계약 23개 검사 PASS 및 루트 `pnpm test` exit 0.
  `/tmp/cunote-r1a-root-aggregate.log` SHA
  `2ad53a43e906fcfff443e5389682ec594c9194faa567fed8990df1754a4c2b0f`.
  앞선 R3-A의 동시 편집 중 typecheck/release 미확정 상태는 이 고정 소스 검증으로 해소했다.
- 이 증거는 로컬 코드/집계 검증이다. fixture DB fallback 경고를 실제 운영 DB 검증으로 세지 않는다.
  실제 앱 UAT, R1-B의 수동 서식 편집/자동채움 안내 구분, 서비스 배포·승격은 아직 남아 있다.

### R1-A 격리 DB 및 HTTP 인수 진행

- 고정 snapshot `113fc3043df0497f270c9d1da80903ec8f6223f3441c19b47b4577e9dd22cf01`에서
  `pnpm test:product-postgres` exit 0. migration 83개·일반 역할 RLS·회사 권한·동시 저장·답변
  revision·관리자 정정 검수·문서 DB/엔진 회귀를 새 Unix socket 전용 cluster에서 검증했다.
  `/tmp/cunote-r1a-product-postgres.log` SHA
  `f228fbe81f12d8b8f1e697b1ed00b721df79afd22f7335e0fbbac69ce240d4d9`.
- web/admin production build와 HTTP 로그인/답변 변경 앞단은 통과했지만 기존 runner의
  required 공고 제외 기대값에서 중단했다. 실제 응답은 새 의도대로 공고를 노출했다.
- 제외 기대를 조건부 노출로 갱신한 다음 실행은 A의 `conditional`/`needs_profile_input`을
  확인한 뒤 질문 수 기대 1과 실제 2의 차이로 중단했다. legacy 제외 질문도 남는 fixture이므로
  현재 질문·criterion 결속과 재확인 수의 정의를 대조 중이다. 실패를 통과로 기록하지 않는다.
- 반복 인수는 동시 편집 중 앱을 섞지 않고 위 고정 snapshot에 검증 도구만 갱신한다.
  build가 생성한 두 next-env와 runner 외 앱 소스가 동일함을 SHA로 확인했다.
  브라우저 인수/외부 OAuth·R2 검증/운영 반영은 이 결과에 포함되지 않는다.

### R3-B 원본 보존 현행 projection 진단

- 새 sidecar는 원 receipt→manifest/grant→run raw SHA를 검증하고, 원 run의 조건으로 현행
  converter/ruleset projection을 다시 계산한다. 역사 snapshot 부재/불일치와 현재 변환 상태를
  따로 보존한다. caller가 제공한 현재 관측값이며 DB 최신성 자체를 증명하는 도구가 아니다.
- source revision은 same/changed/historical_missing을 구분한다. 모델 호출·독립 검수·launch·
  승격 권한은 없으며 원본을 수정하지 않는다. 새 evidence/runtime은 새 content hash를 만들 수
  있고 동일 evidence는 idempotent하다. 새 승인/lease/일회성 claim gate를 추가하지 않았다.
- root가 2026-09-09 06:35 KST 실제 read-only DB/R2 관측을 재조회해 exact 승인 47개를 다시
  분류했다: 현행 원천 결속 변화 18, 모집/노출 부적합 8, 결속 유지 21.
- 21개는 local immutable sidecar 생성 후 다시 verify했고, **현재 projection 21개 verified**,
  역사 snapshot missing 21개, 원 run SHA 보존을 확인했다. 이는 독립 원문 검수 PASS도,
  실제 서비스 공급 회복도 아니다. release 직전 현재 상태/검수 재확인이 계속 필요하다.
- root 직접 module/CLI 회귀 exit 0. 구현자 web typecheck의 동시 R1 타입 오류는 최종 집계에서
  다시 확인한다. R3-B 4파일은 고정되었다.
- 도구 `/tmp/cunote-root-current-projection-supply-audit.mts` SHA
  `4388478c04259fb18532b26a97ada3c6cc56a1cd85248d1fd6d71b9560c0452f`.
  v2 결과 `/tmp/cunote-root-current-projection-supply-audit-v2.log` SHA
  `cbfb06060b61b4fb1fde531752d9f38c93bc3dc10b16b5e1e4bbb46aa3979daa`.
- 최초 root 필터는 같은 승인 release의 item 존재를 적용 완료와 혼동해 47개를 모두 제외했다.
  그 결과는 공급 판정 증거로 세지 않는다. v2는 승인과 적용을 분리하고 해당 존재 플래그도
  sidecar에 원래대로 보존했다. 관측은 multi-query read-only이며 원자 DB snapshot은 아니다.

### R1-B 작성 표시와 R3-B 최종 코드 집계

- 검증된 release의 작성 projection과 현재 신청서 evidence가 모두 준비된 경우에만 자동작성
  표시를 유지한다. held/unverified이고 HWPX를 확보했다면 `manual_form`으로 수동 편집만
  안내하며, 서식도 확인하지 못했으면 unknown이다. raw 서식 존재만으로 자동채움을 약속하지 않는다.
- root가 발견한 ready+evidence held 혼합, split 추가 전 legacy readiness의 undefined 접근을
  회귀로 고정했다. 원문으로 이동하지 않는 버튼의 문구도 실제 신청 준비 이동과 맞췄다.
- root 독립 8-case 표시 감사 PASS. 구현자 `test:authoring-readiness`의 5계층과
  `lab:launch:test`(R3-B module/CLI 직접 회귀 포함) exit 0; packages build/web typecheck PASS.
- R1-B/R3-B 20파일 manifest `/tmp/cunote-r1b-source.manifest` SHA
  `67f2faf41b12e83b367c5c491fff8bdae75b2a794890271881467338563ccd27`에서 root `pnpm test` exit 0.
  결과 `/tmp/cunote-r1b-root-aggregate.log`. 별도 배포·작성 화면 실제 인수는 아직 남는다.

### R8 실제 브라우저 중간 결과 — 전체 통과 아님

첫 browser 인수 runtime은 R1-A 앱 소스 기반이다. 도구의 DTO operator 가정, 구식 onboarding
접근 경로, session null 정규화 오류를 실제 실행으로 발견해 수정했다. 앱 동작 실패와 구분한다.

- owner 실제 로그인·required 질문 CTA·저장·재열기·문서 재방문 PASS.
- editor의 최초 미완성 프로필 자동 열림→닫기→질문 저장/매칭 재조회 뒤 미재열림 PASS.
- 실제 회사 전환은 **SettingsPageView dialog의 POST+router.refresh**다. 그 뒤 `/matches`로
  문서 navigation한 A→B→A 격리를 확인했다. 과거 CompanySettingsPanel의 full reload나
  같은 mount의 회사 전환으로 기록하지 않는다.
- 실제 GET 응답만 지연시킨 close/reopen 뒤 최신 답변 보호 PASS; payload를 대체하지 않았다.
- 로그아웃 API의 null은 확인했지만 도구 정규화 오류로 이후 중단했다. 재로그인/viewer는
  이 실행에서 미완료다. 마지막 재시도는 hold 잔여 120초 gate에서 앱 접근 전 거부했다.
  bounded 시간이나 보호 검사를 우회하지 않고 새 runtime에서 최종 반복한다.

### R8 최종 격리 인수 — R1-B 소스

- `3992961` 기반 snapshot manifest
  `b1b3a815391052983442fe057dc8050a3d6cf7ad59160aad50a5788be45c5bce`의 실제 web/admin 앱을
  새 Unix socket DB와 함께 빌드했다. 위 중간 실패와 다른 fresh runtime이다.
- 2026-09-09 06:45 KST owner/editor/viewer password 로그인, required text-only 자연 CTA,
  저장/재열기/문서 재방문, editor 프로필 닫힘 유지, 실제 Settings 회사 전환 뒤 route 이동,
  실제 늦은 GET 응답 보호, 로그아웃/재로그인, viewer 읽기 전용을 전체 PASS했다.
- root는 실제 DB도 read-only로 대조했다: A 필수 yes/satisfied revision 5·editor 작성,
  B 필수 no/unsatisfied revision 1 유지, B 우대만 revision 1·A 우대 무응답, viewer 작성 0.
- 영수증 `cunote-product-uat-pg-OB75nH/natural-confirmations-browser-receipt.json`(private tmp,
  0600), raw SHA `667ffa640e97579b89f56d7f44277a8a8bdc49d7850f26831e8ce5bd5323175c`.
  source 2,218개를 검증하고, Next build가 만드는 두 next-env 선언만 정확한 production template
  대조와 원본/현재 SHA 병기로 분리했다. connection·fixture·manifest·browser tool 해시도 결속했다.
- browser tool SHA `706fcae1c95bcfdd690b83378af99c67297c95962a4685b53a1eaca9c2b047a9`.
  root syntax/guard 31 checks PASS. 세션은 이름을 분리하고 종료했으며 비밀번호는 stdin으로만 전달했다.
- 같은 frozen source의 `test:confirmation-sheet-browser`도 exit 0: viewer/기존 답변/403 복원/
  재전송 방지/ABA stale 403/close-reopen stale GET/ABA stale GET의 7-case. 이는 실제 컴포넌트와
  합성 응답을 쓰는 별도 검사이며 실제 회사 UI 인수와 섞지 않는다.
- 격리 publisher fixture는 운영 release 승인을 뜻하지 않는다. 실제 OAuth·R2·외부 기관 갱신·
  운영 배포는 여전히 이 인수 범위 밖이다. R1-B root aggregate log SHA는
  `3eab122f1169af59939bdb045d7bd5220a58365b5cd408c0809042fdb7e3fef2`다.

### R2 새 기준선 — R1-B 고정 후

R2 직전 `3992961`의 별도 source snapshot에서 asOf `2026-09-08T21:43:00Z`를 고정했다.
actual read-only 조회 3회는 5,924/5,695/5,874ms, 공고 85·조건 502,
전체 결과 SHA `113f162325535672520c9aa936931bad2dc29c7f282ce2437f00c3d329bb409f`로 같았다.
작성 projection은 unverified 85개다. 기존 자료에 현재 작성 evidence가 없다는 뜻이지 수동
원본 편집 불가와 같은 뜻은 아니다. 조회 최적화의 비교는 이전 R1-A가 아니라 이 상태를 기준으로 한다.
이 3회는 원자 DB snapshot이 아니며 p95/cold DB 지표도 아니다.
`/tmp/cunote-root-serving-r1b-baseline-v2.log` SHA
`8ddc36191ecce3cd59b04e8fbe7273d8dbd4dfcf7c00d28cfdf9fc2699a647a3`.

### R9 기존 데이터 upgrade 리허설

- 별도 새 Unix socket cluster에 현행 journal 0000~0077을 적용한 뒤 legacy 질문/답변,
  회사 소속과 match_state를 넣고 0078~0082를 transaction으로 적용했다.
- 기존 ID·값·작성자·시각·소속 fingerprint가 유지됐고 legacy evaluation은 null,
  answer revision은 1로 유지됐다. 기존 match_state의 새 input binding/calculation 시각은 null,
  회사/공고 input revision 초기 생성 및 이후 답변 수정 trigger도 확인했다.
- 구버전 형태의 UPDATE는 이후에도 성공하며 input binding은 여전히 null이다. 따라서 이
  리허설은 구버전 writer 종료나 보호된 writer 배포를 대체하지 않는다.
- exit 0. 이는 canonical 0000~0077의 격리 upgrade이며 운영의 역사 원장 hash를 재실행한 것이 아니다.
  `/tmp/cunote-root-migration-upgrade-audit.mts` SHA
  `90e32b74a420bf53e7b66fa35dfbcfaa5473f2d933f48084e5c378170a60df5b`,
  결과 `/tmp/cunote-root-migration-upgrade-audit.log` SHA
  `6b39f8d99cd7848abf8a505b02c9e2aa8124afecd97addd17b8d84370a212b53`.
  운영 migration/배포 쓰기 0.

### R2 1차 구현 독립 실측

- `requireDeepAnalysisPromotion:true`를 R1-B와 같은 asOf에서 실제 read-only로 3회 조회했다.
  1,238/458/428ms, 공고 85·조건 502이며 전체 DTO SHA
  `113f162325535672520c9aa936931bad2dc29c7f282ce2437f00c3d329bb409f`가 기준선과 일치했다.
- 요청마다 repeatable-read를 사용하고, 작은 item 120행과 release 문서 9행을 읽어 manifest는
  9회 검증했다. 검증된 item은 118개다. application JSON 직렬화 합계 1,827,560B와 기존
  join의 JSONB text 중복 합계 40,573,725B는 표현이 다르므로 정확한 wire 전송률로 비교하지 않는다.
- root 실측 소스는 drizzle `cc586c25…`, promotionServing `084af227…`이며 전후 불변이었다.
  `requireDeepAnalysisPromotion:false` 경로의 후보별 범위 축소는 이 측정 뒤 별도로 보완한다.
- 로그 `/tmp/cunote-root-serving-r2-audit.log` SHA
  `403d73cbec4fa45f7776eec2910f126c9e20a28e96f158761186383272562ddc`.
  스크립트 SHA `807d14ab5dd948f4afb91d0a5b04a967e1cd63ee8c5e61286e1ca9f12bf3142c`.
  모델/서비스 쓰기 0, p95·동시 부하·cold DB 검증은 아니다.

### R9 cron·기능 플래그 읽기 확인

- 2026-09-09 07:10 KST Vercel project API에서 web의 cron 9개가 기존 production deployment를
  가리키고 있음을 확인했다. `grant-cycle-post`는 `40 0,6 * * *`이며 credit 정산 등 다른
  cron도 함께 있으므로 매칭 writer 정지 목적으로 프로젝트 cron 전체를 임의 비활성화하지 않는다.
- web/ops 프로젝트 환경 목록에 `CUNOTE_SOURCE_CORRECTIONS_ENABLED` 항목이 없었다.
  저장소 구현은 정확한 `true`만 활성화한다. 프로젝트 설정 조회이지 실제 배포 프로세스 환경
  전체나 기능 인수 성공을 증명하는 것은 아니다. 비밀 환경 값은 출력하지 않았다.
- exact web deployment에 대한 최근 2시간 `grant-cycle-post` 로그 검색은 0행이었다.
  endpoint를 호출하지 않았으며, 로그 부재만으로 수동 실행 불가나 writer drain을 선언하지 않는다.
- Vercel 공식 [cron 관리](https://vercel.com/docs/cron-jobs/manage-cron-jobs)를 확인했다.
  보호 writer 배포·신규 migration·원천 정정 기능 활성화 순서는 최종 코드 고정 뒤 결속한다.

### R2/R11 최종 코드 gate

- R2 false 경로는 실제 후보/dedup hydration ID 확정 후 필요한 release만 읽도록 보완했다.
  최종 소스의 true 경로 재실측은 656/447/610ms이며 DTO SHA/85공고/502조건이 다시 일치했다.
  로그 `/tmp/cunote-root-serving-r2-final-audit.log` SHA
  `fcefa058bcf6c75994765dc2b25a26762c2f26b904c2d71a90ef0b9192918f17`.
- R2 실제 PG same-snapshot rollback/next-request 반영/복구 후 metadata hydration PASS.
  `/tmp/cunote-r2-postgres-final.log` SHA
  `559f4aab6a3bac5d59b1b8e9fe53bc1830d1f399bfcac2f25ae6f9205b0432cf`.
- R11은 사람 correct 검수와 원문 결속을 가진 미질문 조건에 대해 모델 무호출 초안을 만든다.
  관리자 파일 편집 결과는 `{schema,draftPacket,manualInput}` envelope로 내보내며 기존 manual
  CLI가 원 run/review bytes SHA와 재생성 packet을 대조한다. schema 오타/삭제를 legacy 입력으로
  해석하는 우회, 다른 run의 같은 index, review drift는 artifact 생성 전에 거부한다.
- 파일 재업로드가 작성자·편집·포함/제외를 복원한다. A/B 비동기 import 경합과 import 중
  export를 차단하고, 질문/선택지 DOM event 값은 deferred updater 바깥에서 즉시 캡처한다.
  서비스 질문·selector·release·DB를 직접 바꾸거나 사용자/사람 검수를 대신하지 않는다.
- 2026-09-09 07:22 KST root `pnpm test` exit 0. 변경 소스 30파일의 전후 SHA가 동일했다.
  manifest `61581d840a245041f3002071c91546787bb5e6835479b4c750260bd540ed7247`.
  로그 `/tmp/cunote-r2-r11-root-aggregate.log` SHA
  `c111afe7143cade7bb382d8872fff3410aab62c89e8a9f3fdf5db94ab154fec2`.
- R6 직접 observation/fixture guard 및 browser guard 14개도 통과했다. fresh 격리 web/admin
  build PASS이나 R6/R11 자연 화면 전체는 아직 진행 중이다. 브라우저 open timeout/도구 selector
  실패를 앱 동작 실패나 인수 PASS로 기록하지 않는다. 외부/운영 변경 0.

### R11 격리 실제 화면 최종 인수

- fresh Unix socket DB와 frozen web/admin 소스 manifest
  `4f28b98dddc16b73faf68f3804c78582942e5f2541774249dfe0bdbcfa7e3c37`에서
  2026-09-09 07:31:52 KST 전체 PASS했다. 실제 관리자 password 로그인, 변조 packet 거부,
  미검토 후보 내보내기 차단, 질문·선택지 입력, 포함/제외, 실제 브라우저 파일 다운로드,
  다운로드 bytes의 기존 manual CLI binding 함수 재검증, route 재방문 후 파일 재열기를 확인했다.
- 다운로드 raw SHA `c38dadbfc7e4d22af5d1bfd371a596d6cb94f9a4c61ee8f0c171a8d69b9f3177`.
  화면에서 편집 문구·작성자·포함/제외가 복원됐으며 screenshot을 직접 확인했다.
- 입력은 합성 run/review fixture다. 실제 사람의 검수, live 질문 등록, release/promotion을
  증명하지 않으며 이 인수에서 모델/서비스 DB 쓰기는 없었다.
- 최초 도구 selector 실패와 기본 브라우저 open timeout 뒤, 다른 프로젝트 세션은 건드리지 않고
  명시적인 cached headless-shell 실행 경로로 재검증했다. HeadlessChrome `151.0.7922.34`,
  executable `chromium_headless_shell-1234/chrome-headless-shell-mac-arm64/chrome-headless-shell`.
  실행 override는 receipt에 명시했으며 브라우저 세션은 정상 종료했다.
- private tmp receipt: `cunote-product-uat-pg-jrs6Fd/root-r11-browser-receipt.json`.
  root 스크립트 `/tmp/cunote-root-r11-browser-audit.mjs`, 결과 `...-v4.log`.

### R3 보존 후보의 기존 독립 검수 결속 재확인

- 2026-09-09 07:51 KST, 위 06:35 read-only 관측을 보존한 diagnostic artifact 21개와
  기존 approved release의 source artifact를 현행 `verifyAnalysisLaunchPromotionSourceArtifactDetailed`
  함수로 오프라인 검증했다. 이전 관측값을 의존성으로 주입했고 서비스 DB/R2 재조회는 하지 않았다.
- 20건은 기존 독립 검수 manifest/aggregate, exact run/receipt, source, bundled application
  provenance 대조에 통과했다. `597cd497-8c05-43c7-8cc9-5ec595eab887` 한 건은
  `application_precompute_unavailable`로 실패했다. 따라서 21건 전체를 기존 release 그대로
  승격할 수 있다는 결론은 성립하지 않는다.
- 원 release bytes SHA `5352c149f23155741f2466ca80caaa771b3b568121458e18a9fd4395ebf30a46`
  전후 동일. 스크립트 `/tmp/cunote-root-retained-review-audit.mts`, 로그 `...-audit.log`.
- 이는 기존 검수 파일 그래프의 재확인이지 새로운 독립 내용 검수나 현재 시점의 서비스 admission이
  아니다. 새 projection 진단도 승인 권한이 아니며, approved release와 겹치는 다른 exact cohort는
  기존 원장을 덮어 준비할 수 없다. 교정 release의 정확 대상·현재 원천·승인 범위를 별도로 결속한다.

### R6 자연 인수의 표시 오류와 도구 진단

- 실제 owner 화면에서 정정 차원 Select가 `상시근로자` 대신 원시 값 `employees`를 표시하는
  오류를 발견했다. 해당 폼의 표시 함수/테스트만 보강했으며 공용 Select나 전역 token은 바꾸지 않았다.
- Ops dashboard의 주기적 요청 때문에 로그인 후 `networkidle` 대기가 적절하지 않았다.
  실제 route/session email 확인으로 로그인 준비도를 판정하도록 인수 도구를 수정했다.
- fresh AR8JC9에서 diagnostic/resume flag 없이 접수→관리자 검토→동일 관측 verify 거부→
  격리 원천 갱신→재확인→verify→매칭까지 진행했다. 마지막 helper inspect가
  `CONNECTION_ENDED`로 실패했으나 hold/HTTP/DB는 살아 있었다. 환경 종료로 확정했던
  이전 진단은 정정한다. root는 helper inspect 분기만 `return await` 없이 Promise를 반환해
  `finally`의 `sql.end`가 연속 조회 완료 전에 실행되는 lifecycle 경합을 발견했다.
  단일 ticket 조회로 축소하기보다 inspect 완료 뒤 연결을 닫도록 고치고 회귀 검증한다.
- root 독립 read-only repeatable-read DB 감사는 같은 runtime에서 PASS했다. correction
  `0f003385-2cef-4273-a840-e57a28594e2d`, revision 5, resolved, 이벤트
  `submitted→review→recheck→recheck→verify`, 연결 support ticket도 resolved다.
  shared profile ID는 그대로이며 registry v1 baseline과 v2 observation/공식 row를 대조했다.
  source manifest `035610fdcd8935adcd9c46c9a748072e26b808701b23748f5838fe39be15d0f6`.
  `AR8JC9/root-r6-db-audit-receipt.json`은 DB 증거만이며 browser runner exit 0을 주장하지 않는다.

### R12 보존 guardrail의 범위

- 서비스 원본 kind 차이 36건의 원래 차원은 premises 24건, export_performance 12건이었다.
  현재 회사 입력은 상세 사업장 유형·시군구·기준일·수출 실적 계약을 갖추지 않아 넓은 지역이나
  매출만으로 이 조건을 확정해서는 안 된다.
- actual immutable LabRun 4개(required 3/preferred 1)의 raw SHA, input/grant/run,
  criterion index/source span을 검증하고 현재 projection이 원래 kind·원문을 보존한 채
  `other/text_only/reserved_dimension`으로 남기는 것을 strict 모드로 확인했다.
  합성 exclusion unknown도 conditional이며 required/exclusion의 모름을 통과로 만들지 않는다.
- 최종 test SHA `5ea7d1b4a76bdd62a85f0a71e2ff2283f7b7cb5e50048eb4a2338c1c35fce8cb`.
  `CUNOTE_REQUIRE_SOURCE_ARTIFACTS=1` root 직접 실행 PASS. 모델/서비스 쓰기/새 구조화 축 활성화 0.
  독립 공식 원문 정확성 검수 또는 지역·사업장·기준일 입력 계약 구현 완료를 뜻하지 않는다.

### R4-A 실제 역사 실행 관측

- root가 exact grant `0626b472b25dd61d18eacc85104ee4464f7fbe4d5dbeb13b87be2425caf6b716`의
  50건 본 실행 `7b60ab9c…`와 같은 grant 재시도 `9f999e29…`를 신규 read-only CLI로 관측했다.
  고유 대상 50, 산출물 결속 실행 52, receipt target 행 100, skipped 48로 구분됐다.
- 두 receipt wall 합은 6,336,186ms, target wall 합은 12,325,648ms였다. primary/application
  작업은 병렬로 겹치므로 work 합을 실제 경과시간이나 처리량 분모로 쓰지 않는다.
- application request telemetry는 관측 12회분/미관측 40회분으로 구분됐다. 기록된 명목 비용
  소계는 USD 38.8674495지만 application 비용 26회분이 없어 coverage는 false다. 실제 구독/API
  청구 비용이나 전체 비용으로 해석하지 않는다. queue wait도 봉인된 시작 시각이 없어 미검증이다.
- launch grant/manifest/receipt/LabRun은 exact SHA 결속, application analysis/manifest는
  현재 로컬 파일 hash 관측으로 표시한다. 후자를 과거 immutable 봉인 증거로 부르지 않는다.
- `/tmp/cunote-root-r4-observation-v31-50.log`, CLI exit 0. 모델/서비스 DB 쓰기 0.
  B1/B2에서는 primary-only repair가 재사용할 문서 산출물의 exact source/파일/markdown까지
  결속하며, partial·source drift·중복 artifact를 임의 채택하지 않도록 감사 중이다.

### R9 출고 전 추가 읽기 확인

- 2026-09-09 07:59 KST read-only repeatable-read preflight에서 현행 설치 Drizzle migrator의
  `created_at` 선택 기준이 정확히 0078~0082 다섯 SQL만 선택함을 확인했다. 역사 원장 79행의
  SHA는 `587a4b33a9ad2f322635a2dbc5dbcf8548ed6ce37e18d4f91444369b9e68850d`,
  max created_at은 1787617922404다. 다섯 SQL의 exact hash는 모두 미적용이었다.
- 같은 snapshot에서 회사 136, profile 619, membership 136, 질문 97, 답변 0,
  match_state 1,126행이며 다른 active transaction은 0이었다. 특정 시점의 관측이며 future writer
  차단이나 백업 복구 성공의 증거는 아니다. `/tmp/cunote-root-migration-live-preflight.log`.
- [Drizzle 공식 migration 안내](https://orm.drizzle.team/docs/drizzle-kit-migrate)와 실제 설치
  `postgres-js/migrator`/`pg-core/dialect` 구현을 함께 확인했다. historical hash를 덮지 않는다.
- Vercel web 프로젝트는 GitHub `main` production branch와 자동 deployment 생성 enabled이며,
  ops는 Git 연결이 없다. push가 web 배포를 촉발할 수 있어 migration/출고 후보 고정 전에
  임의 push하지 않는다. 실제 migration·push·배포는 아직 0이다.
- GCP 전용 configuration의 재확인도 reauthentication required로 실패했다. 토큰은 출력하지
  않았고 대체 계정/JSON key를 사용하지 않았다. 기존 사용자 재인증 요청의 응답을 기다린다.

### R7 실제 기관 양식의 저장·재열기 무결성

- head revision 파일을 읽는 경로가 DB의 SHA/byteSize를 대조하지 않는 결함을 수정했다.
  해당 컬럼은 최초 0047부터 NOT NULL이므로 누락된 legacy 값을 임의 추정하지 않는다.
  파일 SHA·크기·형식 세 조건이 모두 맞아야 작업본을 반환한다. storage 주입은 테스트 seam이다.
- 기본 PostgreSQL suite의 합성 HWP/HWPX 양쪽에서 정상 head 재열기와 동일 길이 1-byte 변조,
  1-byte truncation 거부를 항상 검증한다. 실제 기관 fixture가 없는 CI도 새 무결성 계약을 보호한다.
- strict 실제 기관 인수는 보존 manifest의 raw SHA와 전체 원본 SHA가 일치하는 HWPX 6쪽,
  HWP 8쪽을 사용했다. 원본은 수정하지 않고 복사본에 합성 marker를 넣어 첫 `studio_manual`
  revision 저장→격리 PG/memory object-store→head 재조회→RHWP 재열기→export 후보 bytes
  재열기를 확인했다. 페이지 수·텍스트·marker와 원본 전후 SHA가 유지됐다.
- `CUNOTE_REQUIRE_INSTITUTION_FORM_FIXTURES=1 pnpm test:product-postgres` exit 0,
  83 migrations/RLS 포함. 최종 loader SHA
  `1a5f4679a7aac6ef78f6d3bf55c757b1bead78501979c9e5fec24771ece12e95`,
  integration SHA `4ad6551fd7f777936f9ca76f595edeabe62b101525406e8684f0b6dc834018f2`.
- 실제 R2 쓰기, 외부 OAuth, 브라우저 버튼 클릭/다운로드 파일의 인수는 아니다. 서비스 쓰기 0.
  도구 exit 0 영수증과 종료된 격리 cluster `/tmp/cunote-product-pg-IYlqZ1`가 있으며 별도 stdout
  로그 파일은 보존하지 않았다. 최종 코드 집계 gate에 함께 결속한다.

### R6 최종 단일 실행 인수 PASS

- helper lifecycle 수정 뒤 fresh WBZqSh 환경에서 2026-09-09 08:06:51 KST,
  diagnostic/resume flag 없이 전체 browser runner exit 0(18.8초)을 확보했다.
  앞선 재구성 combined receipt 대신 이 단일 실행 영수증을 최종 근거로 사용한다.
- frozen source manifest `0f2465b020bd3c5a3eff9eaf71225a533eeef46dcb871fc5e08e84470d462faf`,
  clean base `0c5c2ad` + exact R6 12-file overlay
  `e66a6209a6e1bf883533cd615bcfceed3887db04b8d75d396ff71c81bf8ced17`.
- 실제 owner/admin 로그인과 화면 조작으로 최초 employees 20의 fail, 정정 접수 후
  unknown/source_dispute, 관리자 검토, 동일 관측 verify UI disabled와 실제 POST 409
  `source_observation_required`, 동일 공식 row 20→8 갱신, owner 재확인 후에도 unknown 유지,
  관리자 verify 200 후 resolved/매칭 eligible·trace pass를 확인했다.
- 원천 갱신은 격리 registry simulation이다. ApplySheet에는 eligibility 필드가 없으므로
  실제 matches API의 eligibility와 ApplySheet의 employee trace를 각각 검증했다.
- root 별도 read-only RR DB 감사도 같은 runtime에서 PASS했다. correction
  `7245ee35-5178-409e-9713-6c66e82fc530`, revision 5, baseline v1 보존, observation v2,
  동일 shared profile ID, 이벤트 5개, support ticket resolved를 대조했다.
- `WBZqSh/source-correction-browser-final.raw.log` SHA
  `8b77d6d384efeac3d66d43563e61d228f49ad3da504cc8784355333927885863`,
  `source-correction-browser-receipt.json` SHA
  `1152427ce81691ca4099ab2f0775c25514625973d738afb8d237ca26c56977be`.
  두 파일 0600, root DB receipt도 동일 runtime에 보존했다. 모델/외부 쓰기 0.

### R9 구버전 writer race 발견과 출고 보강

- exact production `44fda4d`의 conflict update SQL은 새 input_binding/calculation_as_of를
  언급하지 않는다. 격리 PostgreSQL에서 새 bound row 뒤 구버전 upsert를 실행하자 옛 결과가
  덮어써지고 새 binding/asOf는 남는 오염이 실제 재현됐다. `/tmp/cunote-r9-writer-race.eDdyM2`.
- 따라서 `migration→new alias→800초 대기`만으로는 안전을 선언할 수 없다. 0083 전진 migration과
  current writer의 transaction-local contract marker로 old statement를 DB에서 거부하고,
  기존 cache/transition baseline도 현재 revision·topology에 맞지 않으면 신뢰하지 않도록 보강한다.
- 기존 0078~0082와 역사 migration hash를 수정하지 않는다. 기존 1,126개 match_state를 임의
  삭제하거나 원장을 덮지 않으며 실제 적용·재계산은 최종 검증된 출고 단계에서 별도 기록한다.

### R8 R6 통합 소스에서 실제 인수 재실행

- 같은 WBZqSh frozen manifest에서 R6 종료 뒤 기존 두 확인질문 공고만 사용해 R8 자연 흐름도
  단일 실행 PASS했다. owner CTA/저장/재열기, editor profile 패널 저장 후 닫힘 유지,
  실제 Settings A↔B 전환과 route 재진입, 이전 actual GET 지연 중 최신 답변 보존,
  재로그인 복원, viewer read-only를 확인했다. same-mount 회사 전환으로 확대해 보고하지 않는다.
- private `natural-confirmations-browser-final-explicit.raw.log` SHA
  `7824de50464c9b7fda0d17814d60814faa20b35bed75f2f1ceff0b60a05be3d5`,
  `natural-confirmations-browser-receipt.json` SHA
  `a3d2b95d3d9b824f19500f688ab3faf181a13103dbe0d07719d3b1684a087759`.
- root read-only RR DB 대조도 PASS: A required yes/satisfied/rev5/editor,
  B required unsatisfied/rev1 보존, B preferred rev1/A preferred 미답변, viewer 작성 0.
  R6 정정은 resolved/rev5로 유지됐다. `WBZqSh/root-r8-db-audit-receipt.json`.
  운영 계정/OAuth/R2 인수나 다른 source commit 전체의 배포 검증을 뜻하지 않는다.

### R3 기존 serving kind 교정 후보의 현재 원천 재확인

- 2026-09-09 08:14 KST, 원래 kind와 serving kind가 다른 30공고/36조건의 exact applied item,
  active release source artifact와 원 LabRun raw SHA를 다시 읽고 현재 DB/R2 input·attachment·
  source revision을 대조했다. 24공고는 원천 결속 유지, 6공고는 원천 변경으로 재검수가 필요했다.
  30공고 모두 해당 관측 시점에는 모집 중/visible이며 확인된 duplicate가 아니었다.
- source 유지 24건도 독립 원문 내용 검수나 실제 교정 승인 완료는 아니다. 현재 코드의 kind 보존
  수정이 이미 DB에 저장된 36조건을 자동으로 고치지 않으므로 exact corrective release가 남는다.
- `/tmp/cunote-root-serving-kind-current-audit.mts`, 로그 `...-audit.log`; 모델/서비스 쓰기 0,
  원 run bytes 보존. 대상별 read-only 관측이며 전체 30건을 묶은 원자적 DB snapshot은 아니다.

### R3 역사 not_applicable bundle의 엄격한 읽기 호환

- 21건 재검증 중 1건은 신청 양식이 없는 정상 종결을 새 authoring ready 요건 때문에 거부했다.
  bundle raw SHA/provenance 검증 뒤 legacy derived readiness, verified evidence, 명시적
  not_applicable, 동일 runId, application/field/materializable/review count 0, 실제 문서 역할과
  전체 source/document count 일치, error 없음이 모두 성립하는 경우만 허용했다.
  authoring은 계속 held이며 실제 작성 가능으로 올리지 않는다. 불일치 변형 회귀도 통과했다.
- root `/tmp/cunote-root-retained-review-final.log`: 21/21 통과, 원 release raw SHA
  `5352c149f23155741f2466ca80caaa771b3b568121458e18a9fd4395ebf30a46` 불변.
  06:35 read-only 관측을 재사용한 기존 검수 결속 검사이며 신규 독립 검수/현재 DB admission이 아니다.

### R4 primary-only 재시도의 exact 작성 분석 재사용

- 독립 검수 합의의 유효한 criterion/axis primary 결함만 허용한다. 원 LabRun에 실제 존재하는
  조건 index/axis인지도 확인하며 application/unknown/mixed 결함, unresolved, formal 신규 실행,
  authoring 재실행, 불완전 작성 분석은 재사용하지 않는다.
- source sequence, 원 receipt/run, 검수 aggregate/manifest/packet, 현재 input/attachment,
  작성 분석의 engine/model/effort/contract와 실제 analysis/manifest/parsed markdown SHA를
  manifest에 봉인하고 실행 직전 다시 대조한다. 파일 중복·경로 이탈·손상은 fail-closed한다.
  markdown도 검증한 bytes를 그대로 materialize하여 검증 후 비봉인 파일을 다시 읽지 않는다.
- 기존 grant의 retry-errors가 이전 attempt를 암묵적으로 최신 선택하지 않는다. 새 material
  runtime으로 실행하려면 현행 launch manifest 승인 계약을 따른다. 이 구현 자체는 모델을
  호출하지 않았으므로 실제 구독 시간 절감·신규 코호트 성공률 증거와 구분한다.

### R9 writer contract와 유효 캐시 검증

- 새 0083만 추가했다. transaction-local writer marker 및 JSON 원타입까지 엄격한 v1 binding,
  calculation_as_of를 DB trigger가 요구한다. 원 company/grant input revision CAS는 유지한다.
  과거 writer가 새 binding을 남긴 채 결과만 덮는 exact ON CONFLICT 문장은 거부된다.
- transition과 두 scoped refresh는 현재 회사/공고 revision·dedup topology·ruleset/scoring과
  일치하는 cache만 사용한다. 무효 cache는 삭제하지 않고 baseline 없음으로 재계산한다.
  transition은 유효성 검사 후 LIMIT을 적용하며 legacy 500개 뒤 정상 1개가 누락되지 않는
  실제 PostgreSQL 회귀를 포함한다. 관리자 역사 eval/report는 이 운영 cache 변경과 구분했다.
- root 독립 `/tmp/cunote-root-writer-fence-upgrade-audit.log` PASS: 0~82 적용 뒤 seed한
  기존 bound row가 0083 이후 완전히 보존됨, exact old upsert 거부, 새 marked writer 허용,
  transaction 종료 후 marker 누출 없음. 0083 SHA
  `fd60e8867615b2f74816b3d544008d4c2df8abb0a8d136ce1deb548171b3bf01`.
  Unix socket 격리 DB만 사용했으며 서비스 migration/배포는 아직 하지 않았다.

### R3/R4/R9/R12 통합 소스 최종 gate

- root `pnpm test`, `pnpm lab:launch:test`, `pnpm lab:release:test` 모두 exit 0.
  `CUNOTE_REQUIRE_INSTITUTION_FORM_FIXTURES=1 pnpm test:product-postgres`도 exit 0이며
  실제 기관 HWP/HWPX 저장/head 재열기와 84 migration·RLS·old writer 경합을 포함했다.
  격리 DB는 종료했고 `/tmp/cunote-product-pg-leLXjl`에 테스트 데이터/로그만 보존했다.
- 변경 소스 28개 SHA를 실행 전후 대조해 변경 0을 확인했다. 사용자 next-env 변경과 문서만
  소스 봉인 대상에서 제외했다. source manifest SHA
  `827d6db2b298345cf923c994c4f528a1de050b93e5a6e66f2ef09169a26972e8`.
- 로그와 SHA:
  - `/tmp/cunote-root-r4-r9-final-aggregate.log`:
    `a8afc7f08911c8549d2529f981d524a615122eaf5f24cacbb554492bfb475003`
  - `/tmp/cunote-root-r4-r9-final-product-postgres.log`:
    `95c5f29277111cfe6a3033d54ddf173bb5bf990982e1f86033285fc82748d54f`
  - `/tmp/cunote-root-r4-final-launch.log`:
    `76a30c4054903833f621eb00a7bd32d6bf64bc7202e0d54ca45dd0b26edfef15`
  - `/tmp/cunote-root-r3-final-release.log`:
    `7ed865c01dcb094bd9d51710144826d7609da48e8d77cc4ebb3106abbd2c6d0e`
- 위 결과는 코드·격리 검증의 출고 gate다. 실제 서비스 migration/배포/승격, 외부 OAuth/R2,
  새 구조화 입력 축과 신규 live 분석 성능 측정을 완료했다고 해석하지 않는다.

### R9 28627c3 프리뷰 빌드

- exact source `28627c36c47f4fba69e2e42a81057385afe437bb`의 Git archive만 사용했다.
  사용자 next-env, `.git`, `.env*`, 기존 node_modules/build 산출물은 포함하지 않았으며
  web/ops 모두 monorepo 루트에서 지정 project.json을 결속했다.
  archive SHA `b5cde3b7f2aefe947e6681c6f35c0e1153e8a9b9a0e79f251e8805ee4ebc5da5`.
- web 프리뷰 `https://changupnote-p0kht2nt2-noten.vercel.app`,
  ops 프리뷰 `https://changupnote-pow9zdcrz-noten.vercel.app`: 두 remote build exit 0.
  해당 프리뷰에만 원천 정정 flag를 true로 전달했다. production alias나 DB는 이 단계에서
  변경하지 않았다. 두 로그인 페이지 HTTP 200, 두 원천 정정 API 익명 접근 401을 확인했다.
- 프리뷰 빌드/익명 스모크는 실제 계정 로그인이나 DB 적용 증거가 아니다.
  production DATABASE_URL은 Vercel Secret이므로 env run이 값을 제공하지 않았고,
  로컬 정본 DB URL과의 exact 비교는 미확인이다. 다른 URL로 대체하거나 Secret을 덮지 않는다.
  migration 대상은 프로젝트 drizzle.config의 정본 env 선택과 기존 ledger/schema 결속으로
  별도 검증한다. `.env.local`에는 DB override가 없고 셸의 DB override도 없음을 확인했다.

### R12 구조화의 다음 최소 단위와 미완료 경계

- 원본 4건은 현재 사업장과 과거 기준일, 미래 이전 약속, 전년도 USD 수출액,
  국가별 수출·샘플·MOU 우대로 서로 다르다. province region이나 KRW revenue를
  더 구체적인 사실로 간주하지 않는다. 현행 3상태 per-notice 확인 질문을 계속 사용할 수 있다.
- 최소 자동판정 후보는 현재 사업장 premises-v1 한 축이다. 시도/시군구의 버전 고정 코드,
  시설 유형(본사/공장/연구소), 유효 시작/종료일, 관측 시각과 근거 종류를 명시적으로 저장해야 한다.
  공고 조건도 exact 시설 유형·시군구·기준일·근거를 구조화해야 하며, 목록 완결이 확인되지
  않은 no-hit은 fail이 아니라 unknown이다. 기존 자유문자 premises 질문은 이 계약을 대체하지 않는다.
- 개인 profile과 회사 공유·공고 한정 confirmation의 저장 경계는 유지해야 한다.
  미래 이전 약속은 현재 사업장으로 저장하지 않으며 시간 만료/재확인 정책이 필요하다.
  수출은 기간·산정 범위·통화·metric이 일치할 때만 비교하고 환산·매출 대용·누락=0을 금지한다.
- 새 자동판정 축·폼·공식 행정코드 catalogue는 아직 구현하지 않았다. 원본 guardrail 통과를
  이를 완료한 것으로 기록하지 않는다. 근거 인정/행정코드 버전/시간 만료가 정해진 작은 단위로
  계약→matcher→입력/저장→실제 인수 순서로 진행한다.

### R7 실제 R2 + 격리 PostgreSQL 저장 왕복

- root가 검토한 prefix adapter로 실제 R2 PUT 2회/GET/HEAD를 실행했다. 기존 bucket의
  새 `cunote-audit/r7/1e8f2024-45f2-46bd-a115-e00ed47b1a27/` 아래 생성한 키만 허용하며
  기존 객체 접근·덮어쓰기·bucket 생성·presigned URL 발행은 하지 않았다.
  공개 기관 양식 원본 2개에 합성 marker만 넣은 사본이며 원본 로컬 SHA는 불변이다.
- exact 28627c3, journal 84와 runtime source/script/SQL 전후 SHA 불변을 확인했다.
  실제 saveStudioSnapshot → R2 → 격리 DB head load → RHWP 재열기 → export 재열기에서
  SHA/bytes/format/page count/text/marker를 대조했다. HWPX 6페이지, HWP 8페이지 모두 PASS.
  시각적 레이아웃 전수 보존이나 제품 다운로드 버튼/OAuth 인수로 확대하지 않는다.
- private receipt `/tmp/cunote-r7-r2-audit-IMtf2B/receipt.json`, mode 0600, SHA
  `c81e54a467ed401f19a06da16d02efb7b5473eccebde4cbea9b9d083a35b5bf7`.
  script `/tmp/cunote-r7-r2-document-audit.mts` SHA
  `f4975139bfb416c032c4c6fe73fc7c200256031a9b16638f2fe02800d0a91d10`.
  raw log `/tmp/cunote-root-r7-r2-execute.log` SHA
  `35e4f75618149d78d3f43b2a0c8c4ada8c689e76b59f5e33386485057227945d`.
- 격리 PG `/tmp/cunote-r7-r2-pg-zEcDXo`는 종료했고 서비스 DB 행은 변경하지 않았다.
  감사 사본 2개(합계 290,541 bytes)는 위 R2 prefix에 남겼다. 실제 삭제는 하지 않았다.
- 후속 read-only 비교에서 web production 환경의 R2 account/bucket/base URL/access key/secret
  및 endpoint override가 root 정본 env와 같음을 **값 출력 없이** 확인했다. `.env.local`과
  셸의 R2 override는 0이며 원 receipt account/bucket hash도 root env와 일치했다.
  원 receipt의 당시 미확인 표시는 수정하지 않는다. production DB Secret URL 비교와는 별개다.
- [Cloudflare 공식 S3 SDK 예제](https://developers.cloudflare.com/r2/examples/aws/aws-sdk-js-v3/)와
  [S3 호환 API](https://developers.cloudflare.com/r2/api/s3/api/)를 확인하고 기존 adapter를 사용했다.

### R9 production 환경 flag 준비

- 두 지정 Vercel 프로젝트에 없던 `CUNOTE_SOURCE_CORRECTIONS_ENABLED=true`를 production
  Config로 추가하고 env run으로 true를 확인했다. 다른 Secret/설정은 덮지 않았다.
  이 단계의 설정은 다음 배포용이며 기존 production deployment/alias가 바뀌었다는 뜻이 아니다.

### R9 첫 적용 시도 롤백과 연결 풀 설정 복구

- 첫 exact 0078~0083 적용은 첫 CREATE FUNCTION에서 PostgreSQL 25006(read-only transaction)으로
  실패했고 전체 transaction이 롤백됐다. 재조회 ledger SHA와 보호 대상 6개 테이블의 count/hash가
  사전 조회와 모두 같았다. 이 시도를 migration 적용 성공으로 기록하지 않는다.
- DB는 standby가 아니며 postgres 역할의 설정 변경도 없었다. 연결 풀 backend의 session
  override(default_transaction_read_only=on/reset off)를 확인했다. 이전 진단의 세션 설정이
  풀에 남은 것으로 판단하며, 이후 진단은 BEGIN READ ONLY와 SET LOCAL만 사용한다.
- 확인한 4개 backend 중 해당 override 1개를 configured default로 복구했다. 조사 실행기가
  남긴 statement_timeout 15/20/60초 session override도 reset_val 120초와 일치하는 범위만
  복구했다. 후속 fresh 조회는 read_only off/default, timeout 120초/configuration file이다.
  역할/DB 전역 설정·schema/data는 이 복구에서 변경하지 않았다.
- 실행기는 명시 READ COMMITTED READ WRITE와 transaction-local timeout을 사용하도록 보강한다.
  transaction pool에서 서로 다른 backend가 배정될 수 있는 commit 전후 session 비교를
  사후 실패 gate로 삼지 않으며, 설정 보존은 격리 direct connection 회귀에서 검증한다.

### R9 실제 서비스 migration 적용·독립 사후 대조

- 수정 실행기 SHA `a969d82d49b65af68eafe4eb2e0576bd6595f9517c19595c0ea7e6cbe2ab68fe`를
  root가 독립 격리 실행했다. 정상 6개 적용, 중간 실패 전체 rollback, 동시 실행 거부,
  company/grant 동시 INSERT의 trigger 설치 대기, direct connection read-only 기본값 보존 PASS.
  `/tmp/cunote-root-migrate-0078-0083-final-audit.log` SHA
  `93fca7e89a995ab83a6c17382c5ddc6c022686f96059ef96a1cf09b56f1be2ad`.
- exact 28627c3·6개 SQL manifest·현재 ledger SHA를 다시 대조한 뒤 0078~0083을 단일
  transaction으로 실제 적용했다. ledger는 역사 79→85행이며 현행 journal 84와의 차이는
  앞서 식별한 역사 항목 때문이므로 행 개수를 억지로 맞추지 않았다.
- 적용 receipt `/tmp/cunote-root-migrate-0078-0083-applied-final.json` SHA
  `7cca6880566f8ead2544b1780727fd3643bf4120a1fff16b556340f319b6ffd8`, commitCompleted=true.
  after ledger SHA `dbad794f0ef3a7d0955993a2d4d85a74fe4b3996cf968e86376d46a5638f242c`.
- root 별도 read-only RR 사후 감사에서 6개 보호 테이블의 기존 열 전체 count/hash 불변,
  회사 136/공고 32,537 revision 누락 0, 새 RLS와 주요 trigger enabled를 확인했다.
  `/tmp/cunote-root-migration-postcheck.json` SHA
  `4b9cceb12ee9cd2c04f2d76a22400a75f63d678ee6fb627f54f905e51af3f702`.
- 후속 fresh session의 read-only off·statement timeout 120초 configured default도 확인했다.
  exact 28627c3을 main에 push했으며 web Git production build와 ops production build가 진행 중이다.
  배포 완료/실제 공급 승격으로는 아직 기록하지 않는다.

### R9 web/ops exact production 배포

- main 원격은 `44fda4d`에서 검증 소스 `28627c36c47f4fba69e2e42a81057385afe437bb`로
  fast-forward push했다. Git 연동 web build가 production READY이며 metadata.githubCommitSha가
  해당 exact SHA다. `dpl_4bfSZ5P7HHRmQsprKmFgtcYLMuXr`,
  `https://changupnote-px5yxxnlh-noten.vercel.app`, aliases changupnote.com/www.changupnote.com.
- ops는 앞서 봉인한 clean monorepo snapshot에서 production 배포했다. metadata.sourceCommitSha
  exact 동일, `dpl_EbdbcMxYA7NsZGJhyfpnGfytJ7pw`,
  `https://changupnote-1i995ehsy-noten.vercel.app`, alias ops.changupnote.com, READY.
- 두 deployment URL의 login 200, source-corrections 익명 API 401을 확인했다.
  두 custom alias login도 Cloudflare 정책 변경 없이 200이었다. 관리자/소유자 실제 OAuth
  또는 변경 기능의 production 인증 인수 성공으로 확대하지 않는다.
- 사용자 next-env 변경과 미커밋 원장 문서는 배포 소스에서 제외했다. GCP 인증은 별도이며
  Cloud Run 배포나 observe_only 변경은 하지 않았다.
- web production repository adapter=drizzle와 source correction flag=true를 값 제한 출력으로
  확인했다. TTL cache 없는 public `/api/web/stats`의 실제 deployment URL 및 custom alias가
  모두 200/ok=true였고 openCount 36, deadlineSoonCount 18이었다. 범위·타입 검사를 통과했다.
  이 endpoint의 기본 조회 limit 40 내 집계이며 서비스 공급 총량 85와 혼용하지 않는다.
  `/tmp/cunote-root-web-production-stats.json`, `/tmp/cunote-root-web-alias-stats.json`.
  로그인 전 app→service DB 읽기 증거이지 인증된 변경 저장 성공의 대체 증거는 아니다.
- 배포 후 전용 GCP configuration 토큰 발급을 15초 상한으로 다시 확인했으나
  `requiresReauth`/exit 1이었다. tokeninfo와 세 Job 조회는 실행하지 못했다.
  다른 identity·키·권한 추가로 우회하지 않았으며 현재 Cloud Run 상태는 여전히 미검증이다.

### 후속 실행 순서 조정

- 캐시 재계산 wrapper 감사에서 새 binding/asOf 열이 비대상 hash에서 빠진 점과 pair별
  사후 검증 이전 commit 가능성을 발견했다. 전 열 보존·pair transaction rollback·현재 입력
  재검증으로 보강하며 기존 68쌍 manifest는 실행하지 않는다.
- R12의 작은 premises/current-province 계약을 구현하면서 matcher ruleset도 전진하므로,
  현재 v13으로 재계산한 직후 다시 무효화하는 중복 작업을 피하도록 **R12 최종 검증·배포 후**
  현행 exact 대상과 runtime을 다시 봉인해 한 번만 재계산한다. 아직 실제 cache 쓰기 0이다.
- 오래된 `/tmp/cunote-root-serving-baseline.mts`, `cunote-root-serving-r2-audit.mts`,
  `cunote-root-serving-kind-audit.mts`의 session-level SET은 pool에 설정을 남길 수 있다.
  역사 SHA 증거를 보존하되 이 파일들을 그대로 재실행하지 않는다. 새 읽기 진단은 explicit
  read-only transaction과 SET LOCAL로 구성하고 top-level serving snapshot 계약도 보존한다.
- root의 보강 wrapper 독립 격리 재실행 PASS: 실제 production serving reader는 top-level
  read-only snapshot으로 사용하고, 봉인 객체/입력 revision을 pair별 repeatable-read 쓰기
  transaction 안에서 검증한다. 공용 `planScopedMatchStateRefresh`와 guarded repository
  save를 재사용하며 기존 grant-scope refresh의 dry-run 판정과 대조했다.
  저장 직후 dedup 변경 주입은 해당 pair 전체 rollback, 전 열 보존, 실패 receipt의
  rolled_back 구분을 확인했다. 기존 core의 nested serving snapshot 거부를 우회하지 않았다.
  wrapper SHA `86e15a4a3412903dbf814fc1486152eabc9f69b25b6fbff1dd9f7f415416046e`,
  `/tmp/cunote-root-match-state-rebind-final-postgres.log` SHA
  `ec6cd44abb6f2121b0cb0ca425934e57a23ddeaa7c912e6b729da40f61880be1`.
  이는 R12 전 maintenance 실행기 검증이고, 실제 68쌍 쓰기나 R12 최종 소스 gate가 아니다.

### R12 좁은 수직 구현과 진행 중 감사

- 추가 사용자 결정을 요구하지 않는 범위는 premises-v1의 현재 등록 사업장·시도·시설 유형·
  절대 기준일이다. 개인 답변 범위를 유지하고, 기존 region 추정/공유화/DB backfill은 하지 않는다.
  시설 종류와 날짜 구간에 결속한 목록 완전성을 사용하며 빈 목록·만료 기록만으로 탈락시키지 않는다.
- 지정 sol/xhigh 구현을 core/profile, standalone UI, 분석 projection/prompt로 분리했다.
  공용 source compatibility helper를 함께 소비해 프롬프트·검증기·matcher가 서로 다른
  조건을 허용하지 않도록 한다. 원문에 없는 날짜·시설·지역을 모델 note로 보충하지 않는다.
- root 24개 실제 원본 premises/text_only를 오프라인 재검사해 72개 profile 조합에서 기존
  other downgrade·kind/span/value 보존·unknown을 확인했다. 새로운 공급 승격이 아니다.
  `/tmp/cunote-root-premises-legacy-regression.mts`, `...-before.log`.
- root 09:30 KST 실제 serving 읽기 기준선은 85공고/502조건, DTO SHA
  `113f162325535672520c9aa936931bad2dc29c7f282ce2437f00c3d329bb409f`로 이전과 동일했다.
  `/tmp/cunote-root-serving-r12-before.json`은 mode 0600이다. session SET을 쓰지 않고 기존
  top-level read-only serving snapshot을 사용했으며 서비스 쓰기 0이다.
- 초기 감사에서 partial 범위의 실제 positive hit 거부, 평가시점 이후 evidence 사용,
  시군구/이전약속 누락 허용을 발견했다. 1차 보강 후 추가 11-case 검증 중 절대일자 불일치,
  상대일자 추정, 원문 대안 누락, 부정/AND를 positive any로 해석, note의 근거 대체 등
  7개가 RED여서 공용 helper를 보강 중이다. R12 구현/배포 완료로 기록하지 않는다.
- 공용 helper 보강 후 root가 동일 11-case를 다시 실행해 모두 PASS를 확인했다.
  `/tmp/cunote-root-premises-adversarial-third.log`. 손상된 저장 JSON이 assembly에서 예외를
  발생시키는 경로도 지적해 stored parser로 방어하도록 했다. 단위 통과와 실제 저장/재진입
  통합 인수는 분리하며, R12가 적용된 새로운 모델 호출·원천 승격은 아직 0이다.

### R12 최종 검증 — 10:35 KST

- [개인 사업장 매칭 계약](../implementation/2026-09-09-사업장-개인입력-매칭계약.md)을 정리했다.
  premises-v1, matcher v14, 분석/검수/변환 material version을 함께 전진시켰다.
  기존 19축 자동 채움 분모와 기본 4개 완료 조건은 바꾸지 않았다.
- root 감사에서 metadata-only/shared 행이 개인 사업장 상태로 복원될 수 있는 codec fallback을
  발견해 차단했다. 실제 userId, 유효 compound value와 private evidence를 모두 요구한다.
  unrelated 저장에서 위치 ID·typed value·embedded evidence/coverage asOf가 유지된다.
  기존 codec의 transport row as_of는 전체 scope 재직렬화 시각이므로 의미 관측 시각과 구분한다.
- `pnpm test` 최종 PASS. 새 premises/pipeline 및 profile product-path gate를 최상위 집계에
  연결했다. 기존 offline matching-reprojection의 정확한 파일/호출 2개만 tripwire fixture에
  명시했고 analysis-lab 전체 제외 같은 guard 완화는 하지 않았다.
  로그 `/tmp/cunote-root-r12-final-aggregate-v3.log`, SHA
  `9da8ee38519dbebf8afea596ed0ef8bc0e50d44daba53f60232b8eaa7690d8d4`.
  실행 전후 2,208개 source/config/test manifest SHA
  `fa75f14e26cc94ab13fa70a66aa85d6da5f16aa143c7109df89ce4e5e2bf81a7` 일치.
- 실제 기관 양식을 필수로 둔 `test:product-postgres` PASS: 84 migrations, RLS/CAS/구버전 writer,
  개인 사업장 저장·격리·보존을 포함했다. 로그 `/tmp/cunote-root-r12-final-product-postgres.log`,
  SHA `6d1b39d21f6b2d55208a39a020734b1e45e9463f7fe55359a19c0d1e691ce415`.
  launch/release/deep-contract/matching 추가 gate도 PASS다. 이 runtime 소스 v2에서 최종 v3로
  바뀐 것은 tripwire fixture/test와 package.json test scripts뿐이다. runtime 동작 증거를
  재사용하되 전체 소스가 동일했다고 주장하지 않는다.
- 격리 production UAT WFI3oZ에서 실제 password 로그인·owner UI로 사업장 2개 저장 후
  재진입·수정·재저장, 위치 ID/값 유지, unrelated revenue 저장 뒤 의미 관측 시각 보존 PASS.
  회사 A→B→A route 전환에서 개인 답변 격리, editor에게 owner 답변 비노출도 확인했다.
  viewer는 읽기 전용 inspector와 UI POST 0, 별도 직접 API probe는 403
  company_write_forbidden이었다. 실제 OAuth/production 인증 인수로 확대하지 않는다.
  `/tmp/cunote-root-r12-browser-v4.log`, runtime의 `root-r12-browser-receipt.json` SHA
  `c4b575aff37bd60704be4a175d553fe61130800cd088210f90edc1f86edb83b7`.
- root 별도 DB read-only RR 대조 PASS: owner/company A 개인 premises 정확히 1행,
  editor/viewer/company B/shared 0행, typed JSON·evidence와 browser receipt 일치.
  `/tmp/cunote-root-r12-db-v2.log`, runtime의 `root-r12-db-receipt.json`.
  브라우저 도구의 portal selector 및 DB 감사의 transport timestamp 가정을 수정한 재실행이다.
  background refresh 중 dirty draft 보존은 코드/회귀 증거이며 별도 브라우저 인수는 하지 않았다.
  UAT는 제한 시간 종료 후 exit 0으로 정리됐고 사용자 개발 서버를 교체하지 않았다.
- root source guard 11-case, 실제 legacy 24조건×3프로필 72-case PASS. 실제 serving 재조회는
  85공고/502조건, 같은 asOf의 DTO SHA `113f162325535672520c9aa936931bad2dc29c7f282ce2437f00c3d329bb409f`
  그대로다. 새 모델 호출·분석 승격·기존 kind 교정은 0이다.
- cache wrapper는 실제 사용하는 core/contracts dist와 first-party server runtime까지
  binding에 포함하도록 보강했다. 최신 wrapper SHA
  `1633078c854476778c07968aef593b0395d048f1c9cb4f66c7b82036a0aafbb5`.
  독립 격리 PG 정상/현재 입력 drift/저장 직후 dedup 변경 rollback 회귀 PASS,
  로그 `/tmp/cunote-root-r12-final-rebind-postgres.log` SHA
  `4692806942e7853b2aad7558be6c46b11f7ac99cf51b1594eebbf1f483be4204`.
  실제 cache 쓰기는 아직 0이며 최종 배포 후 새 manifest로만 실행한다.

### R12 출고와 기존 캐시 한정 반영

- `b6e1c5f90a088dcd2f5bf8cf87f92e26f1e668b1`을 커밋·push했다. 정본 Vercel 인증으로
  noten-dev/NOTEN·양쪽 project ID·Root Directory를 재확인했다.
- web Git production `dpl_HQZ4tnxME6XP6hwkHKXKwB6sFnvg`,
  `https://changupnote-bqmbwd8m5-noten.vercel.app` READY, metadata.githubCommitSha exact 일치,
  changupnote.com/www alias 확인. ops는 해당 commit의 clean monorepo snapshot에서 배포했다.
  `dpl_3amnDfKCQicVRTRLghbuszJDKA4r`, `https://changupnote-ql9770z1q-noten.vercel.app` READY,
  metadata.sourceCommitSha exact 일치, ops.changupnote.com alias 확인.
- 양쪽 정식 도메인 login 200, web `/api/web/profile/source-corrections` 익명 401 auth_required,
  ops `/api/admin/source-corrections` 익명 401 admin_auth_required PASS.
  web deployment/custom alias `/api/web/stats` 200/ok=true, openCount 33/deadlineSoon 11.
  해당 API의 제한된 현재 조회 집계이며 serving 85와 구분한다. 잘못 적은 web API 경로의
  최초 404는 올바른 route를 확인해 재검증했고 인증 성공으로 세지 않았다.
- source와 실제 core/contracts dist를 포함한 3,240파일 manifest SHA
  `cbb4561fa555f2c80e5c390f2b452a097f36df5ecf746ef1e3e627fd607a48ec`를 출고 후 봉인·재검증했다.
  사용자 next-env SHA와 기존 4010/PID 60300 개발 서버를 보존했다.
- 최종 배포 뒤 새 read-only cache manifest `8c2f0af23866deec9341d9e4aefa7c89ad9fabafcb07af638bc4aec7372f57d7`
  를 준비했다. asOf 2026-09-09T01:39:34.245Z, ruleset-kstartup-spine-v14,
  runtime 1,069파일, 기존 1,142행/serving 85/대상 68쌍·68공고·1회사/비대상 1,074행이다.
  이전 1,126행 snapshot이나 오래된 manifest를 재사용하지 않았다.
- exact 68쌍 저장을 마친 뒤 root 별도 read-only RR 조회에서 전체 행 수 유지,
  68행의 ruleset/scoring/inputBinding/calculationAsOf 결속 일치, 비대상 전체 열 hash 불변을
  확인했다. `/tmp/cunote-root-r12-cache-postcheck.json` SHA
  `a2094ddd259feccb3aa0c897ec4ef88a55689a1b3af1c91fc0053799d1214a3b`.
  비대상 hash `c51ba5ecfaa31033a8ab4dac07851b04c071b7762bb29bfb6dd3a872d6f42c0c`.
  전체 회사×공고 확장, 개인 답변 공유화, 신규 분석 결과 승격은 하지 않았다.
- 캐시 실행기의 정식 planner 전수 사후 대조도 PASS, 10:43:55 KST에 complete로 종결됐다.
  68/68 commit, 실패 0, 현재 입력 결속 68/68, 비대상 1,074행 불변이다.
  receipt `cunote-match-state-rebind-receipt-XHjfZo/receipt.json` SHA
  `0a0f841eb18e96a19fd893a8f6508d1b2802c88c305011f52743163e9f2cf11c`.
  실행 로그 `/tmp/cunote-root-r12-cache-write.log`. 대상별 transaction이며 코호트 전체
  단일 transaction이라고 주장하지 않는다. 이번에는 부분 실패 없이 전부 완료됐다.
- 미승격 21건의 기존 source/review 결속은 R12 코드에서도 오프라인 21/21 PASS다.
  `/tmp/cunote-root-r12-retained-review.log` SHA
  `d7fc24c2ddf663046342fccf9b87877abfd982316919065a8b6f043fc0a0ee00`.
  이전 06:35 read-only snapshot을 소비한 결과이며 현행 live admission이나 새 독립 검수가 아니다.

### 아직 닫히지 않은 목록

- **R3/R10 데이터 교정·공급 승격:** 미승격 47건의 21건 결속 유지 분류와, 기존 serving의
  kind 불일치 30공고/36조건(당시 원천 유지 24공고)은 서로 다른 대상이다. 현재 원천을 다시
  대조하고 exact 교정 release·독립 검수·승격 권한을 결속해야 한다. 직접 DB kind 수정이나
  과거 immutable receipt 수정으로 우회하지 않았다. deep-v34 held 13건도 자동 승격하지 않는다.
- **GCP:** 전용 configuration의 사용자 재인증이 필요하다. 현행 세 Cloud Run Job의
  image/SHA/mode 읽기와 배포가 미완료다. 다른 계정·키·권한으로 우회하거나 observe_only를
  해제하지 않았다. 마지막 DB paused 기록을 현재 Cloud Run 확인으로 대체하지 않는다.
- **실계정 인수:** 실제 OAuth, production 인증 저장, 양식 제품 다운로드 클릭 및 최종 시각
  보존은 별도 인수다. 격리 계정·R2 두 객체 저장/재열기·CLI 검증으로 확대 주장하지 않는다.
- **실행 후 실측:** 새 승인 모델 cohort의 실제 오류/효율, 운영 부하 및 실제 승격 후 노출·
  rollback 측정은 아직 없다. 오프라인 lease/재사용 회귀·명목 비용 통계와 구분한다.
- **R12 후속 확장:** 시군구·미래 이전·기간/증빙 결합·수출 정밀 계약은 이번 최소 버전의
  지원 밖이다. 기존 text-only/unknown 보존을 완전 구조화 완료로 기록하지 않는다.

따라서 목록 전체 완료가 아니라, 검증된 구현·출고와 남은 권한/인수/확장 gate를 분리한 상태다.
