# 창업노트 — 설계 인덱스 (마스터)

최종 갱신: 2026-06-25 · 단계: **구현 착수 준비**

## 📁 설계 문서 맵
| 문서 | 내용 | 상태 |
|---|---|---|
| `창업노트_스택구성_제안.md` | 인프라·아키텍처·플라이휠·데이터모델 | ✅ 확정 v2 |
| `창업노트_정규화스키마_설계.md` | grant/grant_criteria 계약 스키마 | ✅ 완료 |
| `창업노트_데이터표준화_프로토콜.md` | 차원별 표준값·resolver·extractor·comparator | ✅ 완료 |
| `공고API_정리_및_필드분석.md` | 수집 소스 실응답 분석 | ✅ 완료 |
| `창업노트_매칭스코어_모델.md` | 적합도·선정가능성·가치 + 정보이득 | ✅ 완료 |
| `창업노트_수집파이프라인_설계.md` | 크롤→변환→추출→정규화·dedup·backfill | ✅ 완료 |
| `창업노트_플라이휠_운영설계.md` | 골든셋·라벨링큐·임계값·평가·backfill SOP | ✅ 완료 |
| `창업노트_사용자플로우_로드맵_설계.md` | 진입·매칭화면·2D 로드맵·알림 | ✅ 완료 |
| `창업노트_상세구현계획.md` | 단계·스파이크·리스크·성공지표 (통합) | ✅ 완료 |
| `창업노트_계정권한_개인정보_설계.md` | 계정·회사·권한(RLS)·소유권검증·신용정보법 | ✅ 완료(검증 반영) |
| `S0_Popbill_CheckBizInfo_게이트.md` | 팝빌 CheckBizInfo 게이트·env 요구사항·통과 기준 | 🟡 진행중 |
| `창업노트_설계검수_개선결과.md` | 2차 검수 결과·개선사항·잔여 게이트 | ✅ 완료 |
| 독립 검증 | 일관성·누락·리스크 점검 | ✅ 2차 완료 |

## 🔎 검증 반영 로그 (1차 독립검토)
- ✅ 계정·회사·권한·개인정보 누락 → 신규 문서로 보완(companies·user_company·RLS·인가·신용정보법 동의).
- ✅ 소유권 검증 자기참조 모순 → 국세청 진위확인 3요소로 재설계.
- ✅ company_profiles 스키마·명칭 통일 → 계정권한 문서에 정의.
- ✅ 의존성 순서 → S0-A(K-Startup) 선행, S0-D(팝빌 커버리지) 게이트 명시.
- ✅ 매칭 conditional 하방·콜드스타트 prior·match_state/events 분리·재매칭 무효화 → 매칭 문서 반영.
- ✅ 보강 자동 미성립 대비 자가신고 폴백 명시.
- 🟡 잔여(후속): HWP 표추출 실패 fallback(OCR), 버전변경 시 사용자 노출 회수 알림, 상태머신 재오픈/강등 전이.

## 🔎 검증 반영 로그 (2차 검수, 2026-06-25)
- ✅ K-Startup 구 엔드포인트/미결정 표기 정리 → 신엔드포인트·파서 우선 추출 프로파일로 통일.
- ✅ Upstash Redis 단일 결정을 스택·파이프라인·상세계획에 반영(Pub/Sub 병용 제거).
- ✅ `match_state`(현재 상태)와 `match_events`(행동 이력) 분리 표현을 스택/매칭/DB 문서에 맞춤.
- ✅ Flutter 앱 토큰 레이어의 서버 상태 누락 보완 → `app_refresh_tokens` 해시 저장·회전·폐기 원장 추가.
- ✅ 팝빌 재과금 방지 캐시를 `company_enrichment_cache`로 분리하고 `company_profiles`는 canonical 투영으로 명확화.
- ✅ 완료된 S0-B/S0-C1과 남은 S0-C2(엄밀 P/R)를 구분해 구현 착수 게이트를 정리.

## 🔎 팝빌 문서 반영 로그 (2026-06-25)
- ✅ 팝빌 기업정보조회 Node SDK 호출 계약(`checkBizInfo`)과 필수 입력(`CorpNum`, `CheckCorpNum`, 선택 `UserID`)을 외부연동 문서에 반영.
- ✅ `BizCheckInfo` 응답 필드(`corpScaleCode`, `industryCode`, `establishDate`, `addr`, `closeDownState` 등)를 `company_profiles` canonical 투영 기준으로 정리.
- ✅ 팝빌 성공/실패 처리와 `result/resultMessage/checkDT` 메타 저장을 `company_enrichment_cache`에 반영.
- ✅ Vercel 서버리스 IP 제한 리스크를 S0-D 게이트로 추가. 실패 시 GCP 고정 egress 경유를 대안으로 명시.

## 🔎 팝빌 S0-D 진행 로그 (2026-06-26)
- 🟡 `.env`의 `POPBILL_API_KEY`는 SecretKey 별칭으로 사용 가능해 보이나, 실제 `checkBizInfo` 호출에는 `POPBILL_LINK_ID`, `POPBILL_CORP_NUM`, `POPBILL_CHECK_CORP_NUM`이 추가로 필요함을 확인.
- ✅ `poc/popbill_checkbizinfo_probe.mjs` 추가. 값 노출 없이 주요 응답 필드 존재 여부만 출력하도록 설계.
- ⬜ 남은 작업: 추가 env 입력 후 실제 호출, 필드 채움률·Vercel IP 제한·캐시 약관 확인.

## 🧪 스파이크 결과  (상세: `S0_KStartup_PoC_결과.md`, 코드: `poc/kstartup_normalizer_poc.py`)
- ✅ **S0-A K-Startup 실응답** — 신엔드포인트 `nidapi.k-startup.go.kr/api/kisedKstartupService/v1/getAnnouncementInformation`. total 29,187건. 샘플 `samples/`.
- ✅ **S0-C(구조화 커버리지)** — 표본 500: **지역·업력·연령·분야·대상·일정 100% 파서 자동 산출**(LLM 0). LLM 잔여는 scoped 텍스트 한정(업종 56%·규모 28%·인증 17%·지역배제 20%). 예비창업 네이티브. → **K-Startup은 LLM 없이도 척추 매칭 가능 = MVP 우선 wedge 확정.**
- ✅ **엔드투엔드 매칭 데모** (`poc/kstartup_match_demo.py`) — 가상 사업자(경기·2년2개월·35세) × open 215건: 적격 63·조건부 88·부적격 64. 필수/배제 평가·rule_trace·점수·수도권 제외까지 실데이터 동작. **매칭 엔진 설계 전체 실증.**
- 보정점: 지역 판정 = supt_regin + 제외텍스트 결합 필요(20%). 신선도: 표본 57% 마감 → active만 서빙.
- ✅ **S0-B 기업마당 HWP 변환** — HWP 5.0 바이너리 → **pyhwp `hwp5html`이 표 보존**(hwp5txt는 표 손실). 자격요건 표가 LLM-ready 마크다운으로 추출됨(`samples/bizinfo_hwp_converted.md`). **HWP 표 리스크 해결**, Rust/markitdown-hwp 불필요. 스캔본만 OCR 잔여.
- ✅ **S0-C LLM 추출 PoC** (claude-sonnet-4-6, 실공고 5건) — 품질 높음: nuance(업력 완화)·배제업종·채무/체납 배제·OR 트랙 포착, 자동판정 불가는 text_only로 정직 보류, 날조 없음. **교훈: tool-use(구조화출력) 필수**(raw JSON ~1/3 실패→0), operator enum 고정, 공고당 ~센트. `poc/llm_extract_poc.py`.
- ⬜ 잔여: 도메인 전문가 골든셋 50~100건으로 **엄밀 P/R**(팀 작업) · S0-D(팝빌, 키 대기) · S0-E(dedup).

## ✅ 확정 결정 로그
- 그린필드 재설계 (Lovable 코드 폐기)
- 프론트: Next.js(TS)/Vercel · DB: Supabase Postgres 전용 · 캐시/큐: Upstash Redis · 워커: Python/GCP(Cloud Run+Scheduler+Jobs) · 엣지/저장: Cloudflare+R2 · LLM: Claude 직접
- 인증: NextAuth, 외부의존 최소, 1차 이메일/PW·Google·Kakao
- 2-레이어(Ingestion/Serving) + 계약=정규화 grant 스키마
- 추출(AI)/판정(규칙엔진) 분리, 결측=unknown(거짓탈락 금지)
- 스코어링·라벨링·버전태깅 1일차부터 (플라이휠)
- 업종 KSIC+정책태그 2층 / 업력 공고기준일 계산 / 지역 시도+시군구
- **예비창업 1차 포함** — 번호 없는 별도 *수기 진입경로*, 같은 매칭엔진 재사용. 기창업(번호 자동보강)이 메인, 예비는 얇은 서브. 예비 프로필=자가신고라 confidence 낮게 반영.

## 🟢 스택 확정 (lock)
- **기업마당 Phase 1 동반** → Python/GCP 워커 1일차 포함(HWP가 유일한 Python 강제 요인). 수집 런타임 단일(Python).
- 큐 = **Upstash Redis 단일**(Pub/Sub 미사용). 지오코딩 = **행안부 도로명주소 API**.
- 풀스택: Next.js/Vercel · Supabase Postgres · Upstash Redis · Python/GCP(Cloud Run+Scheduler+Jobs) · Cloudflare+R2 · NextAuth(이메일·Google·Kakao) · Claude.
- **기업 기본정보 = 팝빌 고정**(기업정보조회+사업자등록상태조회 선택, 홈택스수집은 Phase 2). **moneypin 등 제3자 크롤링 배제**(봇차단·ToS·추정부정확·신용정보법 리스크).

## 📦 구현 착수 문서 (문서 감사 보충, 2026-06-24)
설계 12종 + 스파이크 결과는 체계적으로 완비됨을 확인. 구현 착수에 빠졌던 3종 보충:
- `창업노트_DB스키마_통합.md` — 흩어진 테이블의 단일 SSOT(ERD·RLS).
- `창업노트_외부연동_환경변수.md` — 연동 명세·`.env`·데이터소스 결정(구 인수인계 최신판).
- `창업노트_구현착수_가이드.md` — 레포 구조·BFF API 표면·Phase 1 태스크 분해·착수 체크리스트.
- `창업노트_검수가이드.md` — **★검수팀 엔트리(여기서 시작)**: 시스템 의도·철학(7원칙) + 전체 읽기 순서 + 문서별 검수 포인트·담당·통과기준.
- `창업노트_레포전략_배포토폴로지.md` — **클라이언트 확장 반영**: 웹+Flutter, 단일 Next.js(웹+어드민+앱API), 3 repo + 계약 seam, 모바일 토큰 인증, admin/API 분리 마일스톤.
- `창업노트_설계검수_개선결과.md` — 2차 검수에서 발견한 구현 착수 리스크와 반영 내역.

## 🟢 클라이언트·레포 확정 (2026-06-24)
- 클라이언트 = **웹(Next.js) + Flutter(iOS/Android)**. 앱 전용 서버 없음 → 단일 Next.js가 앱 API 제공.
- **단일 Next.js = 웹+어드민+앱API+BFF**, 단 `packages/core`(Next 비의존)·라우트 그룹·`/api/app/v1` 버전드로 **분리 가능하게** 설계. admin/API 별도 인스턴스 분리는 **안정화 후 마일스톤**.
- 레포 = **3개**(cunote-web · cunote-app(Flutter) · cunote-ingestion(Python)) + 계약 seam(OpenAPI·criteria JSON Schema·SQL, web 단일 소유).
- 인증 = **NextAuth(웹 세션) + 우리 API 토큰 레이어(앱)**, 외부 의존 없음. 마이그레이션 web 단일 소유.

## 🟢 결정 확정 (2026-06-24, 열린 결정 마감)
- **도메인 = `changupnote.com`**(구매완료), 한글명 **창업노트**. Cloudflare→Vercel, NextAuth/OAuth/앱API base 동일 도메인.
- **본인인증 = PASS(휴대폰)** · **마이그레이션 = Drizzle** · **모바일 소셜 = 시스템 브라우저 PKCE**.
- **팝빌 기업정보 캐시** = `company_enrichment_cache(provider,biz_no,scope)`에 원 조회 결과와 `result/resultMessage/checkDT` 저장, `company_profiles`는 canonical 투영. TTL(기업정보 30~90일·상태 7일급)로 재과금 차단. 약관 허용범위와 Vercel IP 제한 확인.
- **학습형 매칭 구조 확정** — 적격 판정은 결정론·설명가능 유지, 그 위의 *추출·매핑·스코어 가중치(learn-to-rank)·선정가능성*이 데이터로 정교화(매칭 §9.5).
- 마스터 기본값 채택: RLS(PII 전면+비PII는 BFF), 동의철회=필드파기+매칭 unknown 재평가, 스코어 초기값(base 100·우대 +15·conditional 60~95), dedup 보수적(cos>0.95), 샘플링 5~10%, 표-인지 2차추출 보류, 선정가능성은 outcome 30+/유형 후.
- 잔여 외부대기: **S0-D 팝빌**(테스트계정), **도메인 전문가 골든셋**(엄밀 P/R).

## 🗺 사용자 플로우 (합의)
- **기창업**: 번호입력 → **소유권 검증** → 팝빌 1차보강 → 1차매칭(eligible+조건부) → progressive 추가입력 → 상세매칭 → 스코어(적합도→선정가능성→가치) → **2D 로드맵(시간×조건, 미래적격 포함)**
- **예비창업**: 번호 없이 예정 업종·지역·대표자속성 수기입력 → 동일 엔진 매칭 → 로드맵

## ▶ 다음 실행 순서 (권고)
1. **외부 게이트**: S0-D 팝빌 env 보강 후 `checkBizInfo` 실제 호출·캐시 약관 확인, S0-C2 골든셋 50~100건 착수.
2. **구현 착수**: `창업노트_구현착수_가이드.md`의 T0 → T1·T4 순서로 K-Startup 척추 매칭부터 빌드.
3. **운영 정확도 게이트**: 골든셋 P/R과 평가 하니스가 준비되기 전에는 "정확도 검증 완료"로 표현하지 않는다.
